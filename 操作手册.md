# RAG 知识库项目 · 逐步操作手册

> 配套：`E:\AI-learning\RAG项目-技术方案.md`（做什么、为什么）
> 本手册：**怎么做**，一步一条命令，带易错点和验收标准。
> 起笔 2026-09-16。所有命令都在你这台机器上验证过或标注了"待验证"。

---

## 怎么用这份手册

1. **从上往下做，别跳步。** 每一步都有"验收"——没通过就别往下走，后面全靠前面。
2. **命令一行一行复制。** 手打容易错，尤其中文路径。
3. **卡住了把报错原文整段发我**，别只说"报错了"。报错第一行和最后一行最关键。
4. 标记说明：
   - `[坑]` = 这台机器上真踩过的坑，一定会遇到
   - `[注意]` = 不会报错但会做错，更危险
   - `[验收]` = 通过标准，看到这个才算做完

**两个终端概念先分清：**

| 用哪个 | 什么时候 |
|---|---|
| **CMD / PowerShell** | 你自己敲命令（下面默认给 CMD 写法） |
| 不用管 | 我帮你跑的时候用 Git Bash，命令略有不同 |

---

## 全景图：12 步

```
第 0 步  环境准备（JAVA_HOME、目录）        ← 10 分钟
第 1 步  装依赖                             ← 15 分钟
第 2 步  下载 embedding 模型（1.3GB）        ← 10 分钟
第 3 步  下载维基数据（3.2GB，多线程约 18 分钟）← 不用挂机了
第 4 步  解析 XML → JSONL                   ← 5 分钟（小样本）
第 5 步  Spark 清洗管道 → Parquet            ← 10 分钟
第 6 步  数据质量审计                        ← 20 分钟
第 7 步  向量化建索引                        ← 20 分钟（M2）
第 8 步  检索器（向量 / BM25 / 混合）         ← M2
第 9 步  生成与引用                          ← M3
第 10 步 评测集与消融实验                    ← M4
第 11 步 Gradio 界面 + FastAPI               ← M5
```

**这周你只需要做到第 6 步。** 第 7 步往后等前六步验收通过我再带你做——那种"一次给 500 行代码"的教程对你没用，你现在的状态需要的是每一步都能自己跑出结果。

---

# 第 0 步 · 环境准备

## 0.1 认清这台机器上的两个 Python（关键）

`[坑]` 本机有 3 个 Python（3.11 / 3.13 / 3.14），但它们**分工不同**——这不是故意搞复杂，是被 PySpark 逼的：

| Python | 完整路径 | 负责什么 |
|---|---|---|
| **3.13** | `C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe` | 解析、向量化、检索、生成、评测（第 4 步 + M2 之后）。torch / transformers 都在这 |
| **3.11** | `C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe` | **只跑 Spark 清洗管道（第 5 步）** |

### 为什么 Spark 必须用 3.11（实测踩出来的，不是理论）

PySpark 3.5.8 在 Python 3.13 上**会崩**，报错长这样：

```
OSError: [WinError 10038] 在一个非套接字上尝试了一个操作。
org.apache.spark.SparkException: Python worker exited unexpectedly (crashed)
```

**症状特别迷惑**：Spark 能正常启动、能创建 DataFrame、`pythonVer` 也能打印，**但一执行 `count()` 这种触发计算的动作就崩**。我第一次跑就被这个骗了。

根因：Python 3.13 改了 socket 模块的内部行为，而 PySpark 3.5 用 `BufferedRWPair` 包装 socket 来跟 worker 进程通信，在 3.13 上直接炸。

**三个选项都比过了：**

| 方案 | 结论 |
|---|---|
| 升 PySpark 到 4.x | 不行。Spark 4 要求 Java 17，本机只有 JDK 1.8，还得装 JDK |
| 换 Python 3.11 | **选这个**。一行命令，且和 `D:\spark-3.5.8` 版本完全对齐 |
| 放弃 Spark 改 pandas | 不行。那样就丢了简历上最值钱的一段 |

### 你要记住的规则

- 跑 `wiki_extract.py`、下载模型、向量化、检索 → **用 3.13**
- 跑 `build_pipeline.py`（Spark 管道）→ **用 3.11**

**先验证 3.13 的 GPU 是好的**（CMD 里粘贴）：

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" -c "import sys, torch; print(sys.version.split()[0], torch.__version__, torch.cuda.is_available())"
```

`[验收]` 输出类似 `3.13.12 2.14.0+cu126 True` —— 结尾必须是 `True`。

`[注意]` 如果输出 `False`，先别继续，告诉我（可能驱动或 CUDA 环境变了）。

## 0.2 设 JAVA_HOME（`[坑]` 必做）

**这是这台机器最大的一个坑**：Spark 必须有 `JAVA_HOME`，但你这台机器的 `JAVA_HOME` 是**空的**（之前修环境时被误删了）。

先看一眼确认：

```cmd
echo %JAVA_HOME%
```

如果输出 `%JAVA_HOME%` 或者空行，就是没设。设置成永久的：

```cmd
setx JAVA_HOME "C:\Program Files\Java\jdk1.8.0_171"
```

`[注意]` **`setx` 只对新开的终端生效。** 设完必须**关掉当前 CMD 窗口，重新开一个**，否则 Spark 还是找不到 Java。

重新开窗口后验证：

```cmd
echo %JAVA_HOME%
java -version
```

`[验收]` 第一行输出 `C:\Program Files\Java\jdk1.8.0_171`，第二行输出 `java version "1.8.0_171"`。

**为什么必须是 Java 8？** 你机器上只有 JDK 1.8。Spark 4.x 要求 Java 17+，所以**我们只能用 Spark 3.5.x**——这也是后面装 pyspark==3.5.8 的原因，别装最新版。

## 0.3 建目录

```cmd
mkdir E:\AI-learning\data
mkdir E:\AI-learning\projects\rag-kb
mkdir E:\AI-learning\projects\rag-kb\data\raw
mkdir E:\AI-learning\projects\rag-kb\data\interim
mkdir E:\AI-learning\projects\rag-kb\data\processed
mkdir E:\AI-learning\projects\rag-kb\src
mkdir E:\AI-learning\projects\rag-kb\eval\results
mkdir E:\AI-learning\projects\rag-kb\app
mkdir E:\AI-learning\projects\rag-kb\notebooks
```

`[注意]` 目录**必须建在 E 盘**。C 盘只剩几十 G，维基 dump 解压后要 15–20 GB，放 C 盘直接爆。

`[验收]` 打开资源管理器，`E:\AI-learning\projects\rag-kb` 下能看到 `data / src / eval / app / notebooks` 五个文件夹。

---

# 第 1 步 · 装依赖

## 1.1 装（两条命令，别合起来）

**第一条：给 Python 3.13 装 RAG 相关（向量化 / 检索 / 服务）**

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" -m pip install --target="C:\Users\搞懂\AppData\Local\Programs\Python\Python313\Lib\site-packages" -i https://pypi.tuna.tsinghua.edu.cn/simple FlagEmbedding chromadb rank_bm25 pypdf python-docx jieba
```

**第二条：给 Python 3.11 装 Spark（就这一个包）**

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple "pyspark==3.5.8"
```

`[坑]` **绝对不要用 `pip install --user`。** 这台机器上它会把 user base 解析成 `C:\Users\搞懂\Python\Python313`（一个不存在的路径），报"成功"但包根本没装上。之前 gradio 就吃过这个亏，白折腾半小时。

`[坑]` **pyspark 必须锁 3.5.8**，别让它装成 4.x（原因见 0.2：你只有 Java 8）。

`[注意]` 下载量大概 500MB 上下（pyspark 自带一堆 jar），清华源大概 3–8 分钟。慢是正常的，别以为卡死了。

## 1.2 `[坑]` 如果 chromadb 装失败

chromadb 在 Windows + Python 3.13 上依赖 `onnxruntime`，偶尔装不上。报错长这样：`ERROR: Could not build wheels for hnswlib`。

**别跟它死磕**，直接换 FAISS：

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" -m pip install --target="C:\Users\搞懂\AppData\Local\Programs\Python\Python313\Lib\site-packages" -i https://pypi.tuna.tsinghua.edu.cn/simple faiss-cpu
```

功能上没损失——FAISS 做十万级向量的检索绰绰有余。我在代码里会把两条路都留好。

## 1.3 自检（两个 Python 各跑一次）

```cmd
cd /d E:\AI-learning\projects\rag-kb

"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\check_env.py

"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" src\check_env.py
```

`[验收]` **两次都没有 `[FAIL]`。** 3.13 那次会看到 torch/CUDA 通过、pyspark 通过；3.11 那次会看到 pyspark 通过、torch 报警告（正常，3.11 里没装 torch，Spark 也不需要它）。

`[注意]` 判断标准是**没有 FAIL**，不是"全 OK"。两个 Python 分工不同，各有各的警告很正常。

`[坑]` 如果有 `[FAIL] java 命令`：`JAVA_HOME` 设了但 `PATH` 里没有 java。补一条：

```cmd
setx PATH "%PATH%;C:\Program Files\Java\jdk1.8.0_171\bin"
```

（同样要重开终端）

---

# 第 2 步 · 下载 embedding 模型

**要下什么**：`AI-ModelScope/bge-large-zh-v1.5`（中文检索最强的小模型之一，约 1.3GB）

**为什么用它**：中文检索质量在开源模型里第一梯队；1.3GB 在 4060 上跑得飞快；ModelScope 上 210 万下载，稳。

## 2.1 脚本已就绪

**`src\download_models.py` 我已经建好了，不用你手打。** 直接跳到 2.2 跑。

脚本做的事：从 ModelScope 拉 `AI-ModelScope/bge-large-zh-v1.5` 到 `E:\AI-learning\ms-cache`。中断后重跑会续传。

`[注意]` 想顺便把消融实验要用的模型也下了，加 `--all` 参数（多下 5.4GB）。**建议先只下必装的那个**，1.3GB 就够 M2 用。

## 2.2 跑

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\download_models.py
```

`[验收]` 最后打印出类似
`E:\AI-learning\ms-cache\models\AI-ModelScope\bge-large-zh-v1.5`，并且那个目录里有 `pytorch_model.bin` 和 `config.json`。

`[坑]` 如果卡在 0%：ModelScope 偶尔抽风，Ctrl+C 后重跑，它会断点续传。
`[坑]` 千万别改成从 huggingface 下。这台机器上 HF 是死的，别浪费时间。

---

# 第 3 步 · 下载维基数据

**要下什么**：中文维基百科完整 dump（`zhwiki-latest-pages-articles.xml.bz2`）

## 3.1 先说两个反直觉的发现（这一步的全部理由）

### 发现一：单连接被限速，必须并发

我原本给你写的是 `curl` 单线程下载。实测发现那是**错的做法**：

| 方式 | 实测速度 | 3.2GB 要多久 |
|---|---|---|
| curl 单连接 | **107 KB/s** | **8 小时** |
| 4 个并发连接（短测 28 秒） | 428 KB/s | 2 小时 |
| 12 个并发连接（短测 28 秒） | **3.2 MB/s** | **约 18 分钟** |

单连接被限在 107 KB/s，多开连接能线性叠加 —— **并发是有效的**。

`[坑]` 顺手把另外三个镜像也测了，**全是废的**，别去试：`mirror.accum.se`(32 B/s)、`ftp.acc.umu.se`(280 B/s)、`dumps.wikimedia.your.org`(8 KB/s)。就用官方 `dumps.wikimedia.org`。

### 发现二：并发开太大，会被反滥用限流 —— 这个坑我踩了

**"并发越多越好"是错的。** 我按 12 并发正式跑了一次，15 分钟后开始收到：

```
HTTP Error 429: Too Many Requests
```

结果 **203 个分片里失败 107 个**，一半数据没拿到。

根本原因：**我之前的并发测试只跑了 28 秒，而限流是按时间窗口算的。** 短测拿到的是"瞬时速度"，完全看不出持续压力下的限流。这和前面"32MB 样本估规模"是同一类错误 —— **短期观测外推长期行为，不可靠**。

修法三件事（已改进下载器）：

1. 并发降到 **4**
2. 收到 429 时读响应头 `Retry-After` 做**全局冷却**（关键是让所有 worker 一起停 —— 只停一个没用，其余仍在冲限流）
3. 429 用**独立的重试预算**，不占用普通网络错误的重试次数

改完之后的效果：**3.2 GB / 18 分 9 秒 / 平均 3.0 MB/s，被限流 34 次但全部 203 个分片都拿到了。**

**结论：懂得退避的 4 并发，实际吞吐和硬冲的 12 并发一样，但不会失败。** 这是这类"下载/抓取"任务的通用原则。

`[坑]` 另一个副产品发现：**分片失败是乱序的**。曾经出现"从头连续的分片只有 2 个，但总进度已经 96/203"的情况 —— 所以不能"只取前面的连续前缀当下采样"，**必须补齐缺口**（重跑同一条命令即可，已下好的分片自动跳过）。

## 3.2 开下

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\download_wiki_mt.py --full --workers 4
```

**跑起来就不用管了，约 18 分钟。** 下载期间可以正常用电脑。

`[注意]` 屏幕上的进度每 3 秒刷一次，形如 `分片 202/203 已下 3.2GB 速度 3.0MB/s 剩余约 0 分钟 [被限流 34 次]`。**只要分片数在涨就没卡**；`[被限流 N 次]` 出现是正常的，说明退避机制在工作。

`[坑]` **不要用 `curl`，也不要用我早先写的 `download_wiki.py`（单线程那个）。** 两个都会让你多等 8 小时。

`[坑]` **`--workers` 别调超过 4。** 我实测 12 并发会被 429 打回一半分片，见 3.1 发现二。

`[坑]` 如果中途断了（网络抽风、关机、Ctrl+C、429 打满）—— **重跑同一条命令**。已经下完的分片会自动跳过，不会重下。这是分片下载的红利。

`[验收]` 结尾出现：

```
全部分片就绪，开始拼接 ...
  拼接到第 203/203 片
拼接完成：3.2GB  用时 3s
校验 bz2（读前 500 行）...
  OK，能正常解压（读到 500 行）

完成 → E:\AI-learning\data\zhwiki-latest-pages-articles.xml.bz2
```

### `[验收]` 文件大小必须精确等于 3,400,882,048 字节

这是**唯一可靠的完整性判据** —— 直接跟服务器声明的 `Content-Length` 比：

```cmd
curl -sIL "https://dumps.wikimedia.org/zhwiki/latest/zhwiki-latest-pages-articles.xml.bz2" | findstr /I "content-length last-modified"
```

对得上就说明**一个字节都没少**（实测：本地 3,400,882,048 = 远端 `Content-Length: 3400882048`，`Last-Modified: 2026-09-01`）。

`[注意]` 光看"3.2 GB"不够 —— 3.19GB 和 3.17GB 都长得像"约 3.2GB"，但后者说明少了 2 个分片。**必须比字节数。**

## 3.3 磁盘预算（看一眼，别让 E 盘爆）

| 东西 | 大小 |
|---|---|
| 压缩的 bz2 | 3.2 GB |
| 解压出的 XML | 约 20 GB（**不落盘**，我们是流式解析，直接读 bz2） |
| 提取出的 JSONL | 约 8–12 GB（含全部条目正文） |
| 清洗后的 Parquet | 约 5–8 GB（压缩后） |

总共约 20 GB。E 盘现在剩 162 GB，够。

`[坑]` **别把这些放 C 盘。** C 盘只剩 77 GB，而且 Spark 的临时目录默认也在 C 盘 —— 后面第 5 步有个 `spark.local.dir` 要改，到那步我会提醒。

## 3.4 语料到底有多大（我实测了两次，第一次测错了）

**这一节的数字我改过两次，把过程留下来，因为"怎么估规模"本身就是你要学的东西。**

### 第一次：拿 32MB 估 → 结论错了

下到 2 个连续分片（32MB）时我先跑了一遍解析：

| 项 | 32MB 样本 |
|---|---|
| 合格条目 | 2,906 条 |
| 条目密度 | **91 条/MB** |
| 外推全量 | 约 29.7 万条目 |

### 第三次：全量真跑完 —— 又翻了一倍（这次是实测，不是估）

| 项 | 432MB 样本外推 | **全量实测（第 4 步跑完）** |
|---|---|---|
| 合格条目 | 约 79 万 | **1,507,969 条** |
| 读入 `<page>` | — | 4,954,112 |
| 正文总字符 | 约 63.8 亿 | **66.629 亿** |
| 平均长度 | 8,109 字符 | **4,418 字符** |
| 最长条目 | — | 523,288 字符（2022年香港） |
| JSONL 体积 | 约 9.6 GB | **9,385 MB** |
| 解析耗时 | 约 12 分钟 | **601 秒（2,509 条/秒）** |

**外推低了 1.9 倍。同一个数字上，我错了第三次。**

原因看解析过程的进度就是 —— `page` 计数在 40 万条采纳附近发生了跳变：

```
采纳 400,000 条 | page   943,142
采纳 420,000 条 | page 1,766,214     ← page 计数一步涨了 82 万
```

页面 ID 中段有一大段**编号密集、正文很短**的页面。所以「条目密度随位置变化」
**不是单调的 —— dump 中段还有一个台阶**，只覆盖开头的 432MB 样本根本看不到。

`[教训]` 三次错误（凭感觉 400 万 → 32MB 外推 29.7 万 → 432MB 外推 79 万 → **实测 150.8 万**）
共同点都是**拿部分数据推整体，而 dump 的分布并不均匀**。

**正确做法只有一个：跑完全量再报数。** 外推可以做容量规划（留足磁盘），
但**不许写进简历、不许当结论**。

### 所以预算到底是多少（实测版）

| 步骤 | 数字 |
|---|---|
| 合格条目 | **1,507,969 条** |
| 正文总字符 | **66.629 亿** |
| 清洗后（去掉模板/链接标记，通常剩 5–7 成） | 约 33–47 亿字符 |
| 切块（800 字符 / 重叠 100，步长 700） | **约 470–670 万 chunk** |
| 向量化（**实测**吞吐，见 3.5） | bge-large **18–26 小时** / bge-small **1.6–2.3 小时** |

⚠️ **"向量化耗时"这个数我也改过两次**：先凭感觉估 400 chunk/s，实测只有 71 chunk/s（差 5.6 倍）。
**规模预算压在估值上，整个计划都会歪** —— 所以 3.5 那一步的实测别跳过。

最终方案（3.5 有推导过程）：**小模型全量索引 + 大模型抽样对比**，合计约 4 小时。

`[面试话术·基础版]` 「原始 dump 3.4GB，解析出 **1,507,969 条**合格正文
（读入 495 万页，过滤掉 195 万非正文命名空间、145 万重定向、4.5 万残页），合计 66.6 亿字符。」

`[面试话术·加分版]` 「这个规模数字我**外推错了三次**：凭感觉猜 400 万，用 32MB 样本估 29.7 万，
用 432MB 样本估 79 万，最后全量真跑是 **150.8 万**。原因是一直拿部分数据推整体，
而 dump 按页面 ID 排序、**分布并不均匀**（中段有一大段编号密集的短页面）。
**结论：外推只用来做容量规划，报数必须跑完全量。**」

**为什么加分版更值钱**：基础版只证明"我跑完了数据"；加分版证明
**你知道自己为什么会算错、怎么避免**。面试官问的从来不是"你那有多少条"，
而是"**你怎么确定这个数是对的**」。能主动讲出自己错在哪的人，可信度远高于只报数字的人。

第 5 步会用到这个参数：

```cmd
:: 先跑 2 万条，把管道跑通（约 1 分钟）
"...python.exe" src\build_pipeline.py --limit-docs 20000

:: 跑通了再放开（清洗阶段不慢，慢的是第 7 步的向量化）
"...python.exe" src\build_pipeline.py
```

## 3.5 下载向量化模型，然后实测吞吐（这一步别跳）

### 下模型

```cmd
cd /d E:\AI-learning\projects\rag-kb
"...python.exe" src\download_models.py --only bge-large-zh-v1.5
```

`[坑]` **必须带 `allow_patterns` 过滤** —— 脚本里已经加好了，但你要知道为什么：
我查了 ModelScope 上 `AI-ModelScope/bge-large-zh-v1.5` 的仓库文件清单，它同时放了

```
model.safetensors    1,302,138,752 字节
pytorch_model.bin    1,302,220,525 字节
```

**两份内容完全等价的权重。** 不加过滤会白下 1.21GB。脚本里的规则是：

```python
BASE_PATTERNS = ["model.safetensors", "*.json", "*.txt", "1_Pooling/*"]
```

`[坑]` **`1_Pooling/*` 别漏。** 那是 sentence-transformers 的池化层配置，
FlagEmbedding 读取时会用到，漏了会报找不到池化层。

`[验收]` 输出里会打**实际落盘体积**，应该是 `1200 MB 左右`。如果看到 2400MB，说明过滤没生效。

### 实测吞吐（关键，2 分钟搞定，别跳）

```cmd
"...python.exe" src\bench_embed.py
```

**我实测的结果**（2026-09-16，1500 个真实 chunk，平均 787 字符，RTX 4060 Laptop 8GB）：

| 模型 | 参数量 | 最优 batch | 吞吐 | 索引约 600 万 chunk 要多久 |
|---|---|---|---|---|
| `bge-large-zh-v1.5` | 326M | 32 | **71 chunk/秒** | **23.5 小时** |
| `bge-small-zh-v1.5` | 24M | 32 | **813 chunk/秒** | **约 2 小时** |

**差了 11.4 倍。**

`[这张表的 chunk 数怎么来的]` 第 4 步全量解析的实测结果（2026-09-16）：

```
1,507,969 条 / 正文 66.629 亿字符 / 平均 4,418 字符/条
```

切块参数是 `max_chars=800, overlap=100`（步长 700），所以：

```
66.629 亿字符 ÷ 700  =  9,518,428 ≈ 952 万 chunk   ← 原始上限（未清洗）
                          清洗（去模板/去重/去短）通常掉 30~50%
                      →  实际入库约 500~670 万，取 600 万做计划
```

`[易错点]` **别拿"字符数 ÷ max_chars"估 chunk 数。** 那是 `6.663e9 ÷ 800 = 832 万`，
和真实值（步长 700）差 15%。重叠部分是重复烧 GPU 的部分，必须算进去。

⚠️ 我之前一直跟你说"约 400 chunk/s"，**实测只有 71，我高估了 5.6 倍**。

`[为什么不是 bug]` 我一开始也怀疑没吃上 GPU，查了：
- `torch.cuda.is_available()` = True，显存峰值确实涨（0.87GB），GPU 在用
- 算一下就知道这个数合理：BERT-large 每个 batch（32 条 × 512 token）约 **11 TFLOP**，
  4060 Laptop 的 fp16 有效算力大约 **40 TFLOPS** → 理论极限 ~114 条/秒。
  实测 71 条/秒，差距主要来自**真实文本长度参差，padding 浪费了约 25%**
  （我用等长合成文本测是 95 条/秒，用真实变长数据是 71 条/秒）

**所以 71 条/秒就是这张卡的物理上限，不是配置问题。** 调大 batch 没用（实测 batch 32 最快，
64 和 128 反而更慢 —— padding 和显存压力吃掉了收益）。

`[教训]` **吞吐这类数字，估不得，必须测。** 我估 400、实测 71，误差 5.6 倍 ——
放到 600 万 chunk 上就是"4 小时"和"23.5 小时"的区别。规模决策压在错数字上，整个计划都会歪。

### 根据实测调整方案（重要）

既然小模型快 11 倍，最优解不是"选一个"，而是**两个都用**：

| 用途 | 模型 | 规模 | 耗时 |
|---|---|---|---|
| **全量索引**（主力知识库） | `bge-small-zh-v1.5` | 全部 **1,507,969 条**（约 600 万 chunk） | **约 2 小时** |
| **质量对比**（消融实验） | `bge-large-zh-v1.5` | 抽样 10%（约 15 万条 / 60 万 chunk） | 约 2.4 小时 |

两个加起来约 4.5 小时，而且**同时产出了 M4 消融实验的第一组数据**（模型大小对检索质量的影响）。
这比"用大模型索引一小部分"强得多 —— 你得到的是一个**完整**的知识库，
而不是一个残缺的。

```cmd
:: 下载对比模型（92MB，20 秒）
"...python.exe" src\download_models.py --only bge-small-zh-v1.5
```

`[面试话术]` 「我实测了两种 embedding 模型的吞吐：4060 上 bge-large 是 71 chunk/s、
bge-small 是 813 chunk/s，**差 11.4 倍**。据此我调整了架构 —— **小模型做全量索引
（150 万条目 / 600 万 chunk、约 2 小时），大模型只在 10% 抽样上做质量对比**。
理由是：索引覆盖率对检索召回的影响，远大于 embedding 模型本身的几个点差异。」

`[面试话术·进阶版]` 如果对方追问"数据规模怎么定的"，别答"估算的"，答：
「全量解析完我拿到的是**实测值** —— 1,507,969 条、66.6 亿字符。chunk 数不是拿字符数除
800 估的，是按真实切块步长 700 算的，再给清洗损耗留了 30~50% 的余量。
**吞吐和规模两个数都是测出来的，方案才敢往下压。**」

**这段话的说服力在于：你先测、再用测出来的数改方案，而且还知道为什么这么权衡。**

## 3.6 顺便挂上重排序模型（可选，M2 用）

M2 要用重排序模型，顺手下了（约 2.3GB）：

```cmd
"...python.exe" src\download_models.py --only bge-reranker-v2-m3
```

`[注意]` 这一步可以和第 3.2 的下载**同时进行**，互不影响。

---

# 第 4 步 · 解析 XML → JSONL

**这一步干什么**：把 20GB 的 XML 流式解析成一行一条的 JSONL，每条是一篇文章的标题 + 正文。

**为什么用 JSONL**：Spark 读 JSONL 最省事，而且可以直接抽查。

---

## 4.0 🔥 先讲一个坑，它差点毁掉整个第 4 步

**这一节请认真看。它是本项目技术含量最高的一段，也是你面试时可以讲 10 分钟的素材。**

### 症状

我用 `xml.etree.ElementTree.iterparse` 跑全量，它**每次都在完全相同的位置崩**：

```
xml.etree.ElementTree.ParseError: out of memory: line 28421794, column 44
328,850 页 / 已读 1.35 G字符 / 只产出 153,123 条（预期约 79 万）
```

`[注意这个"预期约 79 万"]` 那是当时基于 432MB 样本**外推**出来的数（本身就是错的，见 §3.4）。
后来全量真跑完才知道真实规模是 **1,507,969 条**。所以那次崩溃的**实际损失是 90%**，
比当时以为的 80% 更惨 —— **连"损失有多大"都算错了，因为基数就是错的**。
这也顺带说明：**错误的估算会同时污染结论和风险判断**。

**诡异的地方有三点：**

1. 报的是 **"out of memory"（内存不足）**，可是那一刻进程**只占 60 MB**，机器有 **18 GB 空闲**
2. 崩点**完全固定**，重跑两次一模一样 —— 内存压力导致的崩溃不会这么规律
3. 把崩点那一段内容（第 2700 万 ~ 3000 万行）单独切出来跑，**完全正常**

### 排错过程（每一步都是实测，不是猜）

| # | 做法 | 结果 | 排除掉什么 |
|---|---|---|---|
| 1 | 每 2 万页打印一次 RSS | 全程 **25 → 60 MB** | ❌ 内存泄漏 |
| 2 | 量前 2.0 G字符里最大的 `<text>` | 最大仅 **46.6 万字符**（0.02%） | ❌ 超大节点 |
| 3 | 把崩点所在 300 万行切出来独立跑 | **完全正常** | ❌ 内容触发 |
| 4 | 把切片重复 8 份拼成 1.46 GB | **也不崩** | ❌（量还不够，见 5） |
| 5 | 放开 `SetAllocTracker*` / `SetBillionLaughs*` 两项 Expat 保护 | **仍然崩** | ❌ 反滥用限流 |
| 6 | **量崩溃时的精确字节偏移** | **2,148,056,599 字节** | ✅ **命中了** |

第 6 步的数字是关键。喂入块是 1,048,576 字符 ≈ 1,553,000 字节，
超出的 572,951 字节正好落在最后一个块内部 —— 也就是说：

> **崩溃精确发生在累计输入 = 2,147,483,648 字节 = 2^31 的那一刻。**

### 根因：Expat 在 Windows 上的 32 位计数器溢出

Expat 官方手册原文：

> **`XML_LARGE_SIZE`** — If defined, causes the `XML_Size` and `XML_Index` integer types to be
> at least 64 bits in size. This is intended to support processing of very large input streams,
> where the return values of `XML_GetCurrentByteIndex` ... could overflow.
> It may not be supported by all compilers, and **is turned off by default**.

**这个宏默认是关的**，所以在 Windows 上（`unsigned long` = 4 字节）字节计数器只有 32 位。
Expat 2006 年的缺陷记录里直接点了名：「Expat supports > 2GB files, as long as each chunk passed
to XML_Parse is smaller than 2GB ... I think we are talking about `m_parseEndByteIndex`」。
VTK、ParaView 等大型项目都因为同一个宏在 Windows 上栽过。

而 Expat 溢出后对外报的错误恰好是 `XML_ERROR_NO_MEMORY`，翻成中文就是 **"out of memory"** ——
所以它**是在撒谎**：不是内存不够，是一个 32 位计数器翻车了。

`[面试话术]` 「我用 iterparse 解析 3.4GB 的维基 dump，它稳定在 1.35 G字符 处报 out of memory，
但进程只占 60MB。我依次排除了内存泄漏、超大节点、内容触发、Expat 反滥用限流，
最后**量出崩溃点在累计输入 2^31 字节**，对应 Expat 在 Windows 上 `XML_LARGE_SIZE` 未开启导致的
32 位计数器溢出。**结论是换掉 XML 解析器，改成逐行流式解析。**」

**这段话的价值**：它展示的不是"我会调库"，而是**遇到无法解释的现象时，能设计实验逐步缩小范围，
最后落到一个可验证的精确数字上**。这比会写 for 循环值钱得多。

### 顺带修的一个更危险的 bug

旧版脚本把**任何** `ET.ParseError` 都当成「文件被截断，属正常现象」打印，然后**照常报"完成"**。

于是**真实规模 150.8 万条里只活下来 15.3 万条（丢了 90%）**，脚本还说一切正常。
**绿色的谎言比红色报错危险得多** ——
你看到"完成"就以为数据齐了，后面所有指标都建立在残缺数据上。
而更阴的是：**你连自己丢了 90% 都不知道，因为基数（79万）本身就是错的。**

新版处理：
- 只有真正的流末尾（`EOFError`）才算正常结束
- **其他任何异常 → 打印 `[致命错误]` + 退出码 1，绝不报成功**
- 输出文件会被明确标注「不完整，不要使用」

---

## 4.1 两种解析引擎

脚本现在有两个后端，**默认用 line**：

| 引擎 | 实现 | 什么时候用 |
|---|---|---|
| `line`（默认） | 逐行状态机，**完全不经过 XML 解析器** | 全量跑。不受 Expat 的 2^31 限制 |
| `xml` | `iterparse` | 只用于**小样本交叉校验**，证明 line 引擎没写错 |

为什么逐行解析是安全的？因为媒体维基 dump 的排版**严格稳定**（我实测确认过）：

```
      <page>                                        ← 缩进 2
        <title>浙江省</title>                         ← 缩进 4
        <ns>0</ns>
        <id>12345</id>
        <revision>
          <id>82009188</id>
          ...
          <text bytes="1081" sha1="..." xml:space="preserve">{{存档页}}   ← 正文紧跟在开标签后
    &lt;ul&gt;&lt;li&gt;...                            ← 正文行无缩进，< 全被转义成 &lt;
    [[Category:...]]</text>                         ← ⚠️ 结束标签接在最后一行末尾，不单独成行
          <sha1>...</sha1>
        </revision>
      </page>
```

三个关键点，行解析器必须处理对：
1. **正文紧跟在 `<text ...>` 开标签后面**（同一行）
2. **`</text>` 接在正文最后一行的末尾**，不单独成行 —— 这是最容易写错的地方
3. 正文里的 `<` 全部转义成 `&lt;`，所以**正文内部不可能出现 `<page>` 或 `</text>`** ——
   这就是行解析安全的根本原因（处理正文时一律当文本，不做标签匹配）

`[注意]` 自闭合的 `<text bytes="0" xml:space="preserve" />` 表示空正文，也要处理。

---

## 4.2 自带完整性自检（这是你最该学的部分）

`<text>` 标签带一个 `bytes="N"` 属性，声明这段正文的**原始字节长度**。
所以每解析一条，脚本都会做一件事：

```python
if len(text.encode("utf-8")) != declared_bytes:
    st.bad_bytes += 1
```

**这是免费的、逐条的、可量化的正确性证明。** 实测结果：

```
bytes 属性核对   100.00% 一致（20,000/20,000）
```

**20,000 条全中。** 意思是逐行解析器的输出和 MediaWiki 自己声明的字节数**完全吻合** ——
没漏行、没多算、没截断。

`[面试话术]` 「我用 dump 里 `<text bytes=...>` 属性做了解析器的逐条自检，2 万条抽样 100% 一致。
**这说明我的解析器不是'跑通了'，是被源数据验证过的。**」

**这个思路是通用的**：数据管道里只要源数据自带校验字段（长度、hash、条数），就拿它做自检，
别只靠"没报错"。

---

## 4.3 交叉校验：两个引擎互证

只做自检还不够 —— 万一是我的"自检"本身有问题？所以再做一次**独立实现互证**：

```cmd
:: 引擎 A（逐行）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\wiki_extract.py --engine line --limit 20000 --output "E:\AI-learning\data\_x_line.jsonl"

:: 引擎 B（iterparse）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\wiki_extract.py --engine xml --limit 20000 --output "E:\AI-learning\data\_x_xml.jsonl"
```

实测结果：

```
line 引擎: 20000 条
xml  引擎: 20000 条
**逐条完全一致：doc_id / title / text 三字段全部相同**
合并内容 SHA256: 5253dde4a02c14690a9a7e989866ade2
```

**两套完全不同的实现（一个自己写状态机、一个用 Expat），20,000 条逐条一致。**

`[面试话术]` 「我给清洗管道写了两套独立的数据源实现，然后交叉验证它们逐条一致
（doc_id/title/text 三字段 + 内容 SHA256）。**数据管道的正确性不能只靠'能跑通'，
要有可比对的基准。**」

---

## 4.4 先小样本试跑

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\wiki_extract.py --limit 20000
```

`[验收]` 参考我实测的输出：

```
输入：E:\AI-learning\data\zhwiki-latest-pages-articles.xml.bz2  (3,243.3 MB)
输出：E:\AI-learning\projects\rag-kb\data\raw\wiki.jsonl
引擎：line    上限：20000
--------------------------------------------------------------------
完成：20,000 条 → ...\data\raw\wiki.jsonl
耗时：19.9 秒（1004 条/秒，14.4 M字符/秒）
文件大小：397.8 MB

── 过滤明细 ──
  读入 <page>            36,926
  采纳（正文条目）        20,000
    ├ 非正文命名空间       6,482
    ├ 空标题/空正文            0
    ├ 标题前缀命中             0
    ├ 重定向              10,115
    └ 正文过短(<200)    329

── 数据自检 ──
  正文总字符              2.563 亿
  平均每条               12,817 字符
  最长条目              354,954 字符  清华大学
  bytes 属性核对         100.00% 一致（20,000/20,000）
  含替换符 U+FFFD              1 条
```

`[要点]` 注意「过滤明细」这一块 —— **每一条被丢掉的记录都归了类**。
数据管道必须能回答"丢了多少、为什么丢"。只会说"清洗掉了脏数据"的人在面试里会被追问到崩。

`[注意]` `含替换符 U+FFFD` **理想是 0**。实测有 1 条，说明源 dump 里有个别坏字节
（`errors="replace"` 把它变成了 `�`）。1 条 / 2 万条可以忽略，但**这个数要记录下来**，
它是数据质量审计的一项（第 6 步会用到）。

---

## 4.5 抽查数据（必做）

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" -c "import json,itertools; f=open(r'E:\AI-learning\projects\rag-kb\data\raw\wiki.jsonl',encoding='utf-8'); [print(json.loads(l)['title'],'|',json.loads(l)['text'][:150].replace(chr(10),' '),'\n---') for l in itertools.islice(f,3)]"
```

## 4.6 ⚠️ 你会看到一堆垃圾 —— 这是正常的，而且是重点

**实测数据**（逐条正则统计）：

| 维基标记 | 32MB 样本(2906条) | 432MB 样本(10.4万条) |
|---|---|---|
| 内链 `[[...]]` | **100.0%** | **100%** |
| 模板 `{{...}}` | 99.6% | 99.2% |
| 标题 `==...==` | 98.0% | 91.7% |
| HTML 标签 | 87.1% | 74.3% |
| 文件/图片链接 | 67.0% | 44.8% |
| 外部链接 | 63.6% | 49.4% |
| 表格 `{\|` | 37.2% | 22.8% |
| 残留注释 `<!--` | 27.5% | 14.1% |
| **完全无标记的条目** | **0.0%** | **0%** |

`[注意]` **两个样本的数字不一致 —— 这本身就是个教训。** 前 432MB 的条目密度只有整体的 38%
（dump 按页面 ID 排序，ID 小的是创建最早、编辑最充分的条目，平均 2.2 万字符；越往后越短）。
**小样本可能严重不具代表性，而且它自己内部完全自洽、看不出异常。** 详见 §3.4。

平均每篇约 **480 处**维基标记 —— **没有一条是干净的**。

看一个真实例子。抽到「浙江省」（17.3 万字符），它的开头是：

```
{{redirect|浙江}}
{{中华人民共和国一级行政区信息框
| 名称 = 浙江省
| 行政区类型 = [[中国一级行政区|省]]
| 图片 = {{multiple image| border = infobox| total_width = 290| ...
|image1              =20100719 Leifeng Pagoda and West Lake.jpg
|image2              = Juncture of three main rivers in Ningbo China.jpg
...
```

**这就是整个清洗管道存在的理由。** 如果直接拿这个原文切块：

1. 「浙江省」的第 1 个 chunk 是**图片文件名和模板参数**，不含任何可检索的知识
2. 用户问「浙江省的省会是哪里」，检索可能召回 `20100719 Leifeng Pagoda and West Lake.jpg` 这种块
3. 生成模型看到一堆 `| 名称 = 浙江省 | 行政区类型 = [[中国一级行政区|省]]`，**会照抄格式，输出脏答案**

`[注意]` 所以这一步**不需要把数据洗干净** —— 分层设计的意义就在这儿：解析只负责"提取"，
清洗是第 5 步 Spark 的活。解析和清洗分开，你才能在不重新解压 20GB XML 的情况下反复调清洗规则。

`[面试话术]` 「我先统计了原始 wikitext 的标记污染率：100% 的条目含内链、99% 含模板、
0% 是干净的，平均每篇 480 处标记。**这组数据是我做清洗策略的依据** —— 先量化问题，
再决定正则规则和清洗顺序，而不是凭感觉写几个 replace。」

---

## 4.7 全量跑

小样本验收通过后放开上限（实测 **19.9 秒 / 2 万条**，全量约 **12 分钟**）：

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\wiki_extract.py
```

`[坑]` **磁盘**：全量 JSONL 约 10 GB，加上 3.2GB 的 bz2，E 盘要留 20 GB 以上。

`[验收]` 结尾必须看到：

```
bytes 属性核对   100.00% 一致（XXX,XXX/XXX,XXX）
含替换符 U+FFFD        X 条
```

`[注意]` **如果 `bytes` 一致率不是 100%，别继续往下走。** 说明解析器漏行或错位了，
后面所有清洗和评测都会建立在错误数据上。

---

## `[坑]` 这一步的坑（脚本里已处理，但你要知道原理）

**坑 A：`</text>` 不在单独一行。**
正文结束标签是**接在最后一行正文末尾**的（`[[Category:...]]</text>`），
不是在下一行。按"遇到 `</text>` 行就结束"写会**多吃一行到正文里**，
或者干脆永远匹配不到、把后面所有内容都当正文吞掉。

**坑 B：正文里的 `<` 全是转义的。**
所以正文内部不可能出现字面量 `<page>` / `</text>` —— 行解析器处理正文时**一律当文本收集、
不做任何标签匹配**，这才是安全的根本原因。写的时候别"聪明"地在正文里找标签。

**坑 C：截断文件报 EOFError。**
读截断的 bz2 到末尾会抛 `EOFError`，这是正常的。**脚本已捕获**。
但**别的异常不许当截断处理** —— 见 §4.0 最后那段血的教训。

**坑 D：命名空间（只在用 xml 引擎时才遇到）。**
标签的真实名字是 `{http://www.mediawiki.org/xml/export-0.11/}page`，
按 `'page'` 直接匹配**永远匹配不到，一条数据都出不来**，得先剥命名空间。
（这也是换行解析的一个附带好处：根本没这个问题。）

**坑 E：自闭合的 `<text ... />`。**
`bytes="0"` 的空正文页面用自闭合写法，要单独判断，否则会把它后面的内容当正文吞掉。

---

# 第 5 步 · Spark 清洗管道

**这一步干什么**：这是整个项目最核心的一段，也是你简历上"数据工程"能力的证据来源。

管道做了 7 件事：

```
读 JSONL
  → 清洗（去 wiki 标记、HTML、模板、链接）
  → 文档级去重（MD5）
  → 切块（标题感知，800 字/块）
  → 质量过滤（中文占比、乱码检测、长度）
  → chunk 级去重
  → 写 Parquet + 输出统计
```

## 5.0 动手前必须先做一件事：装 winutils.exe（`[坑]` 必做）

`[这一步不做，你会白跑一两个小时]`

Windows 上 **读** jsonl 不需要 winutils，**写** Parquet 需要。所以缺了它会发生最恶心的那种情况：

```
[1/7] ~ [6/7]  全绿，数字都对，你以为成了
[7/7] 写 Parquet → 炸
Exception: HADOOP_HOME and hadoop.home.dir are unset
```

**前面一个多小时的计算全部作废。** 所以先把它装上：

```cmd
cd /d E:\AI-learning\projects\rag-kb
mkdir hadoop\bin

curl -sL -o hadoop\bin\winutils.exe ^
  https://raw.githubusercontent.com/cdarlint/winutils/master/hadoop-3.3.5/bin/winutils.exe

curl -sL -o hadoop\bin\hadoop.dll ^
  https://raw.githubusercontent.com/cdarlint/winutils/master/hadoop-3.3.5/bin/hadoop.dll
```

`[为什么是 3.3.5]` pyspark 3.5.8 内置的是 Hadoop 3.3.4 客户端，winutils 用同小版本的 3.3.x 即可。
`cdarlint/winutils` 这个仓库只有 3.3.5 / 3.3.6 可选，取 3.3.5。

`[验收]` 两个文件的大小：

```
winutils.exe   112,640 字节
hadoop.dll      84,992 字节
```

`[注意]` `hadoop.dll` **不是可选项**。少了它，就算 `HADOOP_HOME` 设对了，
写文件时仍会报 `UnsatisfiedLinkError`。脚本会把 `hadoop\bin` 自动加进 `PATH`。

`[我实测过]` 装完后 `HADOOP_HOME` 不用手动设环境变量 —— `build_pipeline.py` 里的
`ensure_hadoop_home()` 会在 4 个候选位置里自己找，找到就自己设好并加进 `PATH`：

```
%HADOOP_HOME%  →  <项目>\hadoop  →  E:\AI-learning\hadoop  →  C:\hadoop
```

`[实际落地]` 我把它装在 **`E:\AI-learning\hadoop\bin\`**（跟数据集放一起，项目目录重建也不丢），
所以启动日志里会显示 `Hadoop  : E:\AI-learning\hadoop`。
上面 `cd` 那两行的写法是装到项目内的版本，两个位置脚本都认，任选。

## 5.1 小样本跑通

`[坑]` **这一步必须用 Python 3.11**（原因见 0.1）。用 3.13 跑会在 `count()` 时崩，报 `Python worker exited unexpectedly`。

`[坑·实测踩出来的]` **别用 `--limit-docs` 当试跑。** 它是在 `spark.read` **之后**才 `limit` 的，
Spark 仍然会把整个 9.4GB JSONL 读进来再丢掉 —— 试跑 2000 条和跑全量一样慢。

正确做法是**先切一个真文件出来**：

```cmd
cd /d E:\AI-learning\projects\rag-kb
head -n 20000 data\raw\wiki.jsonl > data\_smoke\smoke.jsonl
```

`[注意]` 这 20000 行是**文件开头**，也就是最热门的大条目（平均 20KB/条），
比全量平均（4.4KB/条）重 4 倍多。所以**这里的吞吐不能直接外推到全量**，
只会偏保守。想更贴近整体，切文件中间一段。

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" src\build_pipeline.py ^
  --input data\_smoke\smoke.jsonl ^
  --output data\_smoke\chunks.parquet
```

`[注意]` 第一次跑会输出一堆 `WARN` 日志，**只要不是 ERROR 就别管**。Spark 启动时话特别多。

`[注意]` 第一次启动要等 30–60 秒，看起来像卡住了，其实在启动 JVM。

`[验收]` 最后看到这张表（这是 2026-09-16 我在本机上实测的真实输出，你的数字应该接近）：

```
================================================================
Spark 数据清洗管道
  Java    : C:\Program Files\Java\jdk1.8.0_171
  Hadoop  : E:\AI-learning\hadoop
  Python  : C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe
  master  : local[4]
================================================================
[1/7] 读入文档            : 20,000
[2/7] 清洗后（长度>=200） : 18,695  (剔掉 1,305)
[3/7] 文档去重后          : 18,695  (重复率 0.0%)
[4/7] 切块后              : 205,851 chunks (平均 11.0 块/篇)
[5/7] 质量过滤后          : 190,129  (剔掉 15,722, 7.6%)
[6/7] chunk 去重后        : 189,619  (剔掉 510)
[7/7] 写出 Parquet → data\_smoke\chunks.parquet

统计（这三个数写进简历）
  raw_docs                  : 20000
  clean_docs                : 18695
  dedup_docs                : 18695
  chunks_before_filter      : 205851
  chunks_final              : 189619
  avg_chunks_per_doc        : 10.14
  doc_dedup_rate            : 0.0
  chunk_filter_rate         : 0.0789
  elapsed_sec               : 93.9
  throughput_docs_per_sec   : 212.9
  stats 已存 → eval\results\m1_stats_YYYYMMDD_HHMMSS.json
================================================================
```

`[怎么读这几个数]`
- `clean_docs / raw_docs = 93.5%` → 6.5% 的条目清洗后不足 200 字（大多是列表、消歧义页），该丢
- `chunk_filter_rate = 7.9%` → 中文占比 <30% 或单字符重复率 >40% 的块被剔掉，正常水平
- `throughput 212.9 docs/s` → **注意这是这 20000 条大条目的值，不是全量的**

## 5.2 检查产出的 Parquet

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" -c "from pyspark.sql import SparkSession; s=SparkSession.builder.master('local[2]').getOrCreate(); s.sparkContext.setLogLevel('ERROR'); df=s.read.parquet(r'E:\AI-learning\projects\rag-kb\data\processed\chunks.parquet'); df.select('title','section','char_len','chunk_text').show(3, truncate=80); print('总块数:', df.count())"
```

`[验收]` 能看到 3 条 chunk，`chunk_text` 是**干净的中文**（没有 `[[` `{{` `<ref`），`char_len` 在 50–800 之间。

`[易错点]` 检查产物也别忘了 `HADOOP_HOME` —— `read.parquet` 在 Windows 上同样会碰 winutils。
如果你在别的终端里手敲这段，先 `set HADOOP_HOME=E:\AI-learning\projects\rag-kb\hadoop`。

## 5.3 `[坑]` 这一步会遇到的坑

**坑 D：`JAVA_HOME` 没设 → Spark 直接起不来。**
报错 `JAVA_HOME is not set` 或 `Cannot find java`。回到 0.2 步。

**坑 E：worker 的 Python 版本不一致。**
报错 `Python in worker has different version 3.11 than that in driver 3.13`。
原因：Spark 在 worker 进程里跑你的 Python UDF 时用错了解释器。脚本里已经强制设了 `PYSPARK_PYTHON`，如果你还是遇到，手动补：

```cmd
set PYSPARK_PYTHON=C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe
set PYSPARK_DRIVER_PYTHON=C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe
```

**坑 F：输出目录已存在 → 报 `path already exists`。**
脚本里用了 `mode("overwrite")`，已经处理。如果你手改过代码碰到了，删掉 `data\processed\chunks.parquet` 重跑。

**🔥 坑 G（旧版手册在这里写错了，已订正）：`winutils.exe` 相关报错 —— 不是 WARN，是致命的。**
报错信息：
```
java.lang.RuntimeException: java.io.FileNotFoundException:
java.io.FileNotFoundException: HADOOP_HOME and hadoop.home.dir are unset.
```
网上和旧版手册都说"通常只是 WARN 不影响" —— **那是读文件的情况。写 Parquet 一定会失败。**
修法见 **5.0**。这是我在本机上实测撞出来的，不是抄来的。

**🔥 坑 H（新增）：Windows 上用 `local[*]` 会崩 Python worker。**

这是我这次踩到的最阴的一个坑。症状：

```
ERROR PythonUDFRunner: Python worker exited unexpectedly (crashed)
  org.apache.spark.api.python.PythonException: Traceback (most recent call last):
    File ".../pyspark/worker.py", line 1225, in main
    File ".../pyspark/serializers.py", line 596, in read_int
      raise EOFError
  EOFError
Caused by: java.net.SocketException: Connection reset by peer: socket write error
```

**阴在哪：**
1. **看不到任何 Python 异常**。Python 侧的栈只有一句 `EOFError`，真正的死因没有。
   worker 是被硬杀的，不是抛异常退出的 —— 就像第 4 步那个 Expat 一样，"真正的错误被伪装成了别的东西"。
2. **数据是干净的**。我把同一条管道里的 `clean_wikitext` / `split_chunks` 抽出来，
   在纯 Python 里对同 20,000 条跑了一遍：**0 个异常**。所以不是数据问题。
3. **它在第 1 次 UDF 执行时是好的，第 2 次才崩**（`[2/7]` 打印出来了，`[3/7]` 没打印）。看起来像"偶发"。

**根因**：`local[*]` 在这台机器上是 **`local[24]`（24 逻辑核）**，意味着 Spark 同时拉起
**24 个 Python worker 进程**，每个都要把一整批（几十 KB × N 行）数据反序列化进自己的地址空间。
瞬时内存压力把某个 worker 直接干掉，OS 层面杀掉进程 → JVM 写 socket 时拿到 RST →
报成 `Connection reset by peer`。

**解法**：`--master "local[4]"`

```cmd
:: 会崩
"...python.exe" src\build_pipeline.py --master "local[*]"

:: 稳
"...python.exe" src\build_pipeline.py --master "local[4]"
```

`[已经帮你处理了]` 脚本现在**在 Windows 上默认就是 `local[4]`**（`os.name == 'nt'` 判断），
Linux/Mac 上仍是 `local[*]`。你不用手动传，但要知道为什么。

**🔥 坑 I（新增）：`spark.read.json` 自动推断 schema 会把整个文件读一遍。**

`spark.read.json(path)` 的 `samplingRatio` 默认是 **1.0** —— 意思是它会先**解析整个 9.4GB 文件**
来猜字段类型，然后才开始真正干活。白读一遍，十几分钟。

```python
# 慢：先全量扫一遍猜类型
df = spark.read.json(path)

# 快：显式声明（JSONL 的字段是固定的，见 wiki_extract.py）
JSON_SCHEMA = StructType([
    StructField("doc_id", StringType(), True),
    StructField("title",  StringType(), True),
    StructField("text",   StringType(), True),
])
df = spark.read.schema(JSON_SCHEMA).json(path)
```

`[前提]` 用显式 schema 前必须确认字段类型一致。我实测校验过：
20,000 行里 `"doc_id"` 带引号的 20,000 行、数字型的 **0** 行。类型不齐就会出现一堆 null。

**🔥 坑 J（新增）：不清洗结果，UDF 会被执行 5 遍。**

Spark 是惰性的。`df.filter(...)` / `df.withColumn(...)` 都只是**记录计划**，不计算。
只有 `count()` / `write()` 这些 action 才触发。所以：

```python
df = df.withColumn("clean_text", f_clean(F.col("text")))   # 还不算
df = df.filter(F.length("clean_text") >= 200)
n = df.count()          # ← 这里才真的把 150 万条全清洗了一遍
df2 = df.withColumn("doc_hash", F.md5(F.col("clean_text")))
n2 = df2.count()        # ← 又洗了一遍！（因为 clean_text 是 UDF 算的，没存）
```

管道里有 7 次 action → **UDF 会跑 7 遍**。本机实测：20,000 篇多花 3 倍时间。

**解法**：清洗完立刻 `cache()`，让它只算一次。

```python
df = df.filter(F.length("clean_text") >= 200)
df.cache()              # ← 关键这一行
n_docs_clean = df.count()
```

`[注意]` cache 的是**内存+磁盘**。全量 150 万条清洗后约 6.7GB，8g driver 装不下，
会溢写到磁盘 —— 所以脚本把溢写目录从 C 盘 Temp 挪到了 `data\_spark_tmp`。
溢写是正常的，别看到磁盘灯一直闪就以为卡住了。

**坑 K（新增）：重定向到文件后看不到进度 —— 这是 Python 的块缓冲，不是卡住。**

你肯定会像我一样这么跑：

```cmd
"...python.exe" src\build_pipeline.py --input data\raw\wiki.jsonl > data\processed\full.log 2>&1
```

然后 `tail` 日志，发现**半小时都不动**，以为卡死了。其实不是：

> Python 的 stdout 在**重定向到文件/管道时默认是块缓冲**（约 4–8KB 才落盘一次），
> 交互式终端下才是行缓冲。所以 `print` 的内容全卡在缓冲区里，进程结束才吐出来。

`[三个解法，任选]`
1. `python -u src\build_pipeline.py ...`（`-u` = unbuffered）
2. 环境变量 `set PYTHONUNBUFFERED=1`
3. 脚本里自己改（**本脚本已经这么做了**）：

```python
sys.stdout.reconfigure(line_buffering=True)
```

`[更重要的教训]` **一个要跑一小时的管道，你必须能监工。**
否则它跑到最后一步才炸，你白等一小时 —— 和第 5.0 节的 winutils 是同一类问题。
判断"到底有没有在动"的正确方法（不依赖 stdout）：
- 看溢写目录 `data\_spark_tmp` 的体积在不在涨
- 看 `data\processed\chunks.parquet` 有没有出现 part 文件
- 看日志文件的**行数**在不在涨（WARN 也算）
- 看有没有 java 进程

**坑 L：`[注意]` 内存设太大反而更慢。**
脚本里 driver 内存默认 4g。本机实测 31.7GB 总内存 / 15.7GB 可用，全量跑用 `--driver-memory 8g` 是安全的。
但**如果你同时开着游戏或别的吃内存的程序，调大了会触发 swap，慢十倍**。宁可分片跑。

**坑 M：`[注意]` 切块参数别乱调。**
`max_chars=800` 是基线值，M4 阶段你会系统性地测 256/512/1024。**现在别动它**——先把基线跑出来，有了对照才知道改得对不对。

## 5.4 🔥 全量跑完之后，抽样自检发现的清洗 bug（这一段最值钱）

`[这一步千万别跳过]` 管道跑绿了不等于数据干净。**跑完之后必须反过来查产物。**
我就是这么发现清洗器漏了 2.9% 的块。

### 怎么查：用"残留标记计数"当体检指标

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" -c "
from pyspark.sql import SparkSession
s = SparkSession.builder.master('local[2]').getOrCreate()
s.sparkContext.setLogLevel('ERROR')
df = s.read.parquet(r'E:\AI-learning\projects\rag-kb\data\processed\chunks.parquet')
print('总块数    :', df.count())
print('唯一 doc  :', df.select('doc_id').distinct().count())
print('残留 [[   :', df.filter(\"chunk_text like '%[[%'\").count())
print('残留 {{   :', df.filter(\"chunk_text like '%{{%'\").count())
print('残留 <ref :', df.filter(\"chunk_text like '%<ref%'\").count())
print('含 U+FFFD :', df.filter(\"chunk_text like '%\ufffd%'\").count())
"
```

`[全量实测结果（第一版清洗器）]`

```
总块数    : 3,946,101
唯一 doc  : 1,004,163
残留 [[   : 115,409   ← 2.9%，超标 20 倍
残留 {{   : 21,142
残留 <ref : 10
含 U+FFFD : 19
```

**2.9% 的块带 `[[`，这不能接受。**（第 6 步的抽样验收标准是"无残留标记"，
2.9% 意味着你抽 50 条会撞到 1~2 条。）

`[结果预告]` 这一轮修完降到 1,499 块（0.038%），**但没归零** ——
下面 §5.4.1 是第二轮，最后压到 **366 块（0.009%），累计降 315 倍**。
两段连着看，才能看到"修 bug"这件事真实的样子：**它是一层套一层的。**

### 定位：不是正则写错了，是**执行顺序**错了

先看样例，不要改代码：

```python
rows = df.filter("chunk_text like '%[[%'").select('title', 'chunk_text').limit(6).collect()
```

拿到三条典型：

```
[TITLE: 圣海伦火山]  [[File:Mount St Helens erupting at night by Paul Kane.jpg|left|thumb|[[保羅·凱恩]]畫筆下夜中爆發的圣海伦火山，...]]
[TITLE: 鲁杰罗·里奇]  [[File:Bundesarchiv ... Ruggiero Ricci und Gerhard Hauptmann.jpg|thumb|里奇（居中者）與作家格哈特·霍普特曼，1932年]]
[TITLE: 亚瑟小子]     ...瓊塔·巴頓（Jonetta Patton，本姓 {{lang|en|O-{’}-Neal}}）和亞瑟·雷蒙德三世...
```

**第一条就暴露了真相。** 去原始 JSONL 里把这个 File 链接捞出来看原文：

```cmd
grep -a -o -m1 "\[\[File:Mount St Helens erupting at night[^\"]\{0,400\}" data\raw\wiki.jsonl
```

输出（注意加粗的嵌套部分）：

```
[[File:Mount St Helens erupting at night by Paul Kane.jpg|left|thumb|[[保羅·凱恩]]畫筆下夜中爆發的圣海伦火山，...]]
                                                                ^^^^^^^^^^^^ 说明文字里嵌了一个普通内链
```

而我的正则长这样：

```python
RE_FILE = re.compile(r"\[\[(?:File|Image|文件|图像|圖片)\s*:[^\[\]]*\]\]", re.I)
#                                              ^^^^^^^^^ 不允许出现 [ 或 ]
```

**`[^\[\]]*` 跨不过嵌套的 `[[保羅·凱恩]]`，所以第一次替换必然失配。**

那为什么后来还是没被清掉？因为执行顺序是：

```
1. RE_FILE        ← 失配（嵌套括号挡路），File 链接活下来了
2. RE_LINK_BARE   ← 把内层的 [[保羅·凱恩]] 拆成 保羅·凱恩
                    拆完之后，那个 File 链接就"变得可以匹配"了
3. ...            ← 但 RE_FILE 已经跑过了，不会再跑第二遍
```

> **结论：不是正则写错，是"剥离内链"和"剥离 File"之间有依赖关系，
> 而我把它们写成了一次性顺序执行。嵌套结构必须迭代到不动点。**

### 第二个 bug：语言变体标记 `-{...}-`

```
-{zh-cn:域;zh-tw:體}-
-{zh-hans:信息; zh-hant:資訊;}-
-{于}-
-{}-
```

这东西在中文维基里满地都是，抽样 200MB 就捞出一堆。而且它是**双杀**：

1. 它自己带 `{}`，所以 `RE_TEMPLATE` 的 `[^{}]*` 也跨不过它 →
   `{{lang|en|O-{’}-Neal}}` 整块残留（这就是 `{{` 残留 21,142 的来源）
2. 就算模板被删掉了，`-{zh-tw:支援; zh-cn:支持}-` 这种也会留在正文里，读起来像乱码

### 修复（三处，都在 `clean_wikitext` 里）

```python
# ① 语言变体：优先取简体中文变体
RE_LANGVAR = re.compile(r"-\{([^{}]*)\}-")

def _pick_langvar(m):
    body = m.group(1).strip()
    if not body:
        return ""
    for key in ("zh-cn", "zh-hans", "zh", "zh-hant", "zh-tw", "zh-hk"):
        mm = re.search(re.escape(key) + r"\s*:\s*([^;}]+)", body, re.I)
        if mm:
            return mm.group(1).strip()
    if ":" not in body:
        return body                      # -{于}-
    return body.split(":", 1)[1].split(";")[0].strip()

# ② {{lang|en|Foo}} 的真实内容有意义，整块删掉会丢信息
RE_LANG_TPL = re.compile(r"\{\{\s*lang(?:ue)?\s*[-|]\s*[a-zA-Z-]+\s*\|\s*([^{}|]+?)\s*\}\}", re.I)

# ③ File/内链剥离改成迭代到不动点（关键）
for _ in range(4):
    before = t
    t = RE_FILE.sub("", t)
    t = RE_LINK_PIPE.sub(r"\2", t)
    t = RE_LINK_BARE.sub(r"\1", t)
    if t == before:      # 不动点 → 再跑也没变化，退出
        break
```

新顺序：

```
注释 → <ref> → 表格 → 语言变体 -{}- → {{lang|xx|}} 抽内容 → 删模板(循环)
     → 【File/内链：循环到不动点】 → 外部链接 → HTML → 引号 → 空格
```

### 怎么验证修复（**这一步是重点**）

`[别急着跑 12 分钟全量]` 改完正则就上全量是最贵的调试方式。
先写个 A/B 脚本，**拿真实中段样本离线对比新旧两版**（`src/diag_clean.py`）：

```cmd
python src\diag_clean.py --n 20000
```

实测输出：

```
样本：20,000 条（取自文件 50% 位置，比开头更有代表性）
======================================================================
指标                                  旧版            新版
----------------------------------------------------------------------
双开方括号 [[                     823 ( 4.12%)      30 ( 0.15%)
双开大括号 {{                     448 ( 2.24%)      71 ( 0.36%)
<ref                           0 ( 0.00%)       0 ( 0.00%)
表格 {|                         12 ( 0.06%)      11 ( 0.06%)
-{ 语言变体                      885 ( 4.42%)      14 ( 0.07%)
======================================================================
```

**`[[` 残留降了 27 倍（4.12% → 0.15%），`-{` 降了 63 倍。** 方向确认了，才值得上全量。

`[方法论]` **改数据处理逻辑的正确节奏**：
```
看产物 → 抓样例 → 读原始数据 → 定位根因 → 离线 A/B 验证 → 上全量
```
`看产物` 必须在 `改代码` 之前。跳过它就等于凭感觉调正则。

### 顺带把"32.5% 淘汰率"也查清楚了

第一版全量跑完时我发现 **32.5% 的文档被清洗后不足 200 字剔掉了**，
比冒烟测试的 6.5% 高 5 倍。当时怀疑"是不是清洗太狠"。

`diag_clean.py` 顺手分解了原因：

```
淘汰原因分解（按新版）：
    5,016  (25.08%)  旧版也 <200（清洗前就没什么正文）
    1,807  ( 9.04%)  原文本身就很短（<400 字符）
       69  ( 0.34%)  旧版够长但新版被削短   ← 清洗造成的，只占 0.34%
```

**真相：不是清洗太狠，是中文维基本身就有一大堆短条目。**
25% 的条目**清洗前就没什么正文**（消歧义页、列表页、小作品、纯信息框）。

`[又一次样本教训]` 冒烟测试（文件开头 20000 条）淘汰率 6.5%，全量 32.5% ——
**差 5 倍**。因为文件开头放的是全站最热门的**大条目**。
这是本项目第 5 次"样本不具代表性"：

| 次数 | 拿什么估什么 | 偏差 |
|---|---|---|
| 1 | 凭感觉 400 万条 | — |
| 2 | 32MB 样本估规模 | 29.7 万 vs 150.8 万 |
| 3 | 432MB 样本估规模 | 79 万 vs 150.8 万 |
| 4 | 466 万 chunk 线性外推耗时 | — |
| 5 | 开头 2 万条估淘汰率 | 6.5% vs 32.5% |

`[怎么记这件事]` **文件开头的样本永远偏"好"。** 无论是网页、日志还是维基 dump，
开头往往是最大、最完整、最热门的条目。要估整体，**必须取中段，而且至少取三处看方差**。

`[面试话术]` 「我全量跑完后做了产物自检，发现 2.9% 的块残留 `[[File:` 标记。
我去原始数据里把出问题的链接捞出来看，发现是**图片说明文字里嵌了普通内链**，
而我的正则用 `[^\[\]]*` 不允许嵌套 —— 更关键的是，我的剥离顺序是
『先删 File、后拆内链』，导致内链拆掉之后 File 已经不再被匹配。
修复是把这一步**迭代到不动点**。顺带发现语言变体标记 `-{}-` 是另一个污染源。
修完我先用中段样本做了离线 A/B，`[[` 残留从 4.12% 降到 0.15%，才上全量重跑。」

**这段话的价值**：它证明你会**用产物反查自身逻辑**，而且知道"改逻辑前先离线验证"。
比"我写了个清洗脚本"高一个层次。

---

## 5.4.1 🔥🔥 第二轮：残留没有归零，于是又挖出一层（这一段比上一段更值钱）

第一轮修完（迭代到不动点）我以为收工了。**上全量一验，还有 5,651 块残留**：

```
含 [[  的块 : 1,499  (0.038%)
含 {{  的块 : 3,402  (0.086%)
含 -{  的块 :   649  (0.016%)
含 {|  的块 :   252  (0.006%)
```

已经降了两个数量级，很多人到这儿就收手了。但**"已经迭代到不动点"却还有残留，
本身就是个矛盾信号** —— 迭代次数够了，那就是规则本身漏了。

### 关键判断：加迭代次数是没用的

`for _ in range(6)` 改成 `range(20)` 一点用都没有。因为出问题的不是"轮数不够"，
是**字符类排除错了**：

```python
# 旧写法（错）
RE_LINK_PIPE = re.compile(r"\[\[([^\[\]|]+)\|([^\[\]]+)\]\]")
#                                     ↑ 排除了 [ 和 ]
```

只要链接的显示文字里出现**一个**方括号，这条永远失配，迭代一万轮也是零：

```
[[SMTOWN LIVE 2025|SMTOWN LIVE 2025 [THE CULTURE, THE FUTURE]]]
                                   ↑ 就是这个单个 [ 卡死
```

**正解叫「缓释点号」(tempered dot)：以真正的定界符为界，单个括号放行。**

```python
# 新写法（对）
RE_LINK_PIPE = re.compile(r"\[\[((?:(?!\[\[|\]\]).)+?)\|((?:(?!\[\[|\]\]).)+?)\]\]", re.S)
#                                     ↑ 只排 [[ 和 ]] ，单个 [ ] 放行
```

一句话记法：**`[^X]+` 是"排除字符"，`(?:(?!X).)+` 是"排除模式"。**
嵌套结构要的是后者。

### ⚠️ 但缓释不能乱用 —— 表格上它反而更差

这是本轮最有价值的一条。我顺手把表格也改成缓释，结果 `{|` 残留**涨了 8 倍**。

逐步骤追踪才看清原因（`probe_table2.py`）：

```
+RE_TABLE(旧)   表格残留数=0    ← 全删干净
+RE_TABLE2(新)  表格残留数=1    ← 只删了内层，外层壳留下
```

真实维基里是**嵌套表格**：

```
{| 外层
|
{| 内层      ← 这里
|}
|}
```

- 旧写法 `\{\|.*?\|\}`：从**最外层** `{|` 一路扫到第一个 `|}`，**整块端掉** ✓
- 缓释写法：一遇到内层 `{|` 就退缩，从内层重新开始匹配 → 只删内层，**留下一个空壳** ✗

**结论：`\|}` 这类"结束标记"的语义是"整块吃"，不是"以定界符为界"。**
两种技巧长得像，适用场景相反。**改之前必须逐条 A/B，不能"顺手统一风格"。**

### 第三个坑：同一个结构可能需要在**不同时机**跑两遍

`-{zh-hant:...; zh-hans:...}-`（语言变体）的残留 649 块，根因是这样的原文：

```
-{zh-hant:[[File:A.svg|thumb|<ref/>{{rp|2}}]]; zh-hans:[[File:B.svg|...]]}-
```

`RE_LANGVAR` 的 `[^{}]*` 跨不过里面的 `{}`（来自 `{{rp|2}}`），而且此刻 File 链接还没剥。

**试过的错误做法**：直接把 `RE_LANGVAR` 换成缓释版。结果它太早跑，
一口吃掉整个 `-{...}-`，然后 `_pick_langvar` 把 `zh-hans` 变体**内联回正文** ——
而那个变体里还带着完整的 `[[File:...]]` 标记。**等于把刚删掉的东西又搬回来了**，
实测就是：旧版没有的 `[[` 残留，新版反而多出来（A/B 里的"回归样例"）。

**正解：两遍，两套正则。**

| 时机 | 用哪个 | 为什么 |
|---|---|---|
| 第 1 遍（删模板之前） | `RE_LANGVAR` 严格版 `[^{}]*` | 只处理纯文字变体，把 `{}` 清掉免得挡住模板 |
| 第 2 遍（链接/模板/HTML 全剥完之后） | `RE_LANGVAR_ANY` 缓释版 | 此时内部已只剩纯文本，再缓释也不可能内联出标记 |

再加一道**安全阀**（便宜且必须）：

```python
def _dirty(s):
    return ("[[" in s) or ("]]" in s) or ("{{" in s) or ("}}" in s)

def _pick_langvar(m):
    ...
    if _dirty(v):
        return ""          # 宁可丢空，绝不把标记内联回正文
    return v
```

### A/B 结果（终版，20,000 篇中段样本）

```
标记              v1(改前)          v2(改后)       降幅
[[              85 ( 0.43%)      19 ( 0.10%)     4.5x
{{             315 ( 1.57%)      45 ( 0.22%)     7.0x
-{             107 ( 0.53%)      22 ( 0.11%)     4.9x
{|              22 ( 0.11%)      19 ( 0.10%)     1.2x
]]              97 ( 0.48%)      32 ( 0.16%)     3.0x
}}             540 ( 2.70%)     358 ( 1.79%)     1.5x
}-             170 ( 0.85%)      85 ( 0.43%)     2.0x
⚠️ 回归样例：0 条        ← 这一行是上全量的前提
```

`[纪律]` **A/B 报告里必须有一栏叫"回归样例"。** 只报"改善了多少"是不够的 ——
你改成缓释版那次，"改善"的数字很好看，但同一份报告里藏着 1 条回归。
**零回归才允许上全量。**

### 全量验收 · 第二轮修复后（3,934,758 块）

```
含 [[     的块  :   366  (0.009%)     ← 第一版是 115,409 (2.9%)，降了 315 倍
含 {{     的块  :   702  (0.018%)     ← 第一版是 21,142，降了 30 倍
含 -{     的块  :    95  (0.002%)
含 {|     的块  :   230  (0.006%)
含 <ref   的块  :    10
含 U+FFFD 的块  :    19
------------------------------------------------
残留合计        : 1,409 / 3,934,758 = 0.036%
```

**剩下的 0.036% 是源数据缺陷，不是清洗器的锅。** 抽出来看，长这样：

```
{{le|半古典|First quantization]]      ← 原文括号就不配对
{{Clade| ... |1={{Clade  ...          ← 原文模板没闭合
```

`[怎么判断"够了"]` 把残留样例逐个打开看，**如果全是"源数据本身坏"，
就说明清洗器到头了**。继续调只会开始误删正常内容。

### 收尾：这一轮还踩了两个"工程流程"的坑

**坑 X：`rm ... 2>/dev/null` 会把"删除失败"也一起吞掉。**
我删旧产物时加了 `2>/dev/null`，结果删除静默失败、旧目录还在。
后果很隐蔽：新建的 `_SUCCESS` 没生成，但**旧的那个还在**。

**坑 Y：判"跑完了"不能只看 `_SUCCESS` 文件存不存在。**
我等的就是它 —— 一看存在就退出等待，结果读到的是**上一轮**的产物，
验收数字和上一轮一模一样（这个"一模一样"就是唯一的线索）。

`[正解]` 判完成要**三件事同时满足**：

```cmd
:: 1. 日志里出现收尾统计行（脚本自己写的，最可靠）
findstr /c:"stats 已存" data\processed\full.log

:: 2. 产物 part 文件的 UUID/时间戳是新的
dir /o-d data\processed\chunks.parquet\*.parquet

:: 3. java 进程已退出
tasklist | findstr /i java
```

`[一句话]` **"数字和上一轮完全一样"是危险信号，优先怀疑读错了文件，
而不是"改动没效果"。**

### `[面试话术·这一段的加分版]`

> 「清洗器上线后我做产物自检，发现 2.9% 的块残留 `[[File:`。第一轮我定位到是
> **执行顺序**问题 —— 先删 File 后拆内链，内链拆完 File 已经错过时机，就改成
> 迭代到不动点，降到 0.038%。
>
> **但我没停在这儿。**"已经迭代到不动点了却还有残留"本身是矛盾信号，
> 说明规则漏了。第二轮我抽残留样例出来看，发现是**字符类写错**：
> `[^\[\]]+` 遇到"链接文字里含单个方括号"就永久失配，加迭代次数救不了。
> 改成**缓释点号** `(?:(?!\[\[|\]\]).)+?` 之后降到 0.009%。
>
> 中间我还犯了个错：顺手把表格规则也改成缓释，结果 `{|` 残留**涨了 8 倍**。
> 逐步骤追踪才明白，表格是**嵌套结构**，语义上必须"从最外层整块吃"，
> 跟内链要的"以定界符为界"正好相反。**同一种技巧在不同数据结构上是反效果** ——
> 这条我是拿 A/B 打出来的，不是想出来的。
>
> 最终残留 0.036%，剩下的我逐条打开看过，**全是源数据本身括号不配对**，
> 所以我判断清洗器到头了，继续调只会开始误删正常内容。」

**这段话为什么值钱** —— 它同时展示了四件事：
① 你会用产物反查自身逻辑；② 你知道"停手"的判据（不是"看起来干净了"，是"错误全来自上游"）；
③ 你不迷信技巧，用数据判断技巧该不该用；④ 你敢在面试里讲自己走错的那一步。

## 5.4.2 🔥🔥🔥 第三轮：人工审计发现，我一直在防「没删干净」，从没防过「删过头」

前两轮我盯的都是同一件事：**该删的删掉了吗**。
到第 6 步抽 50 条人工读，**第一条就撞车**：

```
基里尔·帕夫洛夫（）是一名哈萨克斯坦举重运动员。
文之里站（）是位於日本大阪府大阪市阿倍野區，屬於…
C·J·克蕾格在第一季第18集《》中對嘴演唱的《》…
```

`（）` `《》` —— **里面是空的，壳还在**。

### 根因

维基原文长这样：

```wikitext
'''基里尔·帕夫洛夫'''（{{lang-kk|Кирилл Павлов}}）是一名哈萨克斯坦举重运动员。
```

`{{lang-kk|...}}` 被 `RE_TEMPLATE` 整段删掉，但**外面的括号是独立字符**，于是剩下 `（）`。
删模板的逻辑没错，错在「删完之后没人回头看括号」。

### 一量化，规模吓人

给 `verify_chunks.py` 加了一栏「清洗副作用」（注意：这跟「残留标记」是**两类不同问题**，
前者是删过头，后者是没删干净，混在一起看必然抓瞎），扫全量 3,934,758 块：

| 项 | 块数 | 占比 |
|---|---|---|
| **空括号 `（）`** | **310,941** | **7.902%** |
| 连续标点 `，。；、` | 76,587 | 1.946% |
| 空书名号 `《》` | 49,986 | 1.270% |
| 空引号 `「」『』` | 14,532 | 0.369% |
| —— 对照：残留 `{{`（前两轮的主战场） | 702 | 0.018% |

**「删过头」比「没删干净」严重 440 倍。** 而前两轮我把精力全花在了后者 ——
因为残留标记「有目共睹」（`[[` 肉眼就能看见），空壳却**长得像正常的中文标点**，
不做专门检测就永远发现不了。

`[一句话]` **一个 bug 值不值得修，取决于你会不会想到去检测它。没有量化就没有优先级。**

### 修复：兜底删空壳，别去穷举模板

第一反应是"把模板名单补全"（`{{lang-xx}}` / `{{IPA}}` / `{{jpn}}` / `{{nihongo}}` /
`{{transl}}`…）。**这条路必死**：几十种模板还带地区变体，穷举一定漏，漏一个就留一个空壳。

正解是**兜底**——不管哪个模板删空的，空壳一概不要：

```python
RE_EMPTY_SHELL = re.compile(
    r"[（(]\s*[）)]"          # （） ()
    r"|[《]\s*[》]"           # 《》
    r"|[「『]\s*[」』]"       # 「」 『』
    r"|[【]\s*[】]"           # 【】
    r"|[“]\s*[”]"            # “”
)

# 跑 3 轮：(（（））) → (（）) → 空，一轮不够
for _ in range(3):
    t, n = RE_EMPTY_SHELL.subn("", t)
    if n == 0:
        break
```

### ⚠️ 第二个坑：统计口径 ≠ 修复口径，指标会骗你

连续标点我第一版写成「只压**同一个**标点重复」：

```python
RE_DUP_PUNCT = re.compile(r"([，。；、])\1+")     # 只吃 、、 。。
```

A/B 一跑，**这一项从 301 涨到 1,493**。像是改坏了，其实是：
`verify_chunks.py` 里的检测用的是 `[，。；、]{2,}`（**任意**标点相邻），
而我只修了**相同**标点相邻。真正多的是 `。、` `，。` 这种不同标点相邻 ——
来源是"原本被链接隔开、清洗后接到一起"，比如：

```
[[淮阳]]、[[登封]]、、[[郾城]]、[[辉县]]        ← 中间某个链接被整个删掉，留出 `、、`
```

修法（口径对齐 + 保留规则）：

```python
RE_DUP_PUNCT = re.compile(r"[，。；、]{2,}")

def _squash_punct(m):
    s = m.group(0)
    return "。" if "。" in s else s[0]      # 有句号就留句号（断句优先级最高）
```

不收录 `？！` —— "真的吗？！" 是正常中文，压掉就是误伤。
`……`(U+2026) 和 `——`(U+2014) 不在字符集里，天然不受影响。

`[一句话]` **改之前先确认「检测用的口径」和「修复用的口径」是同一个。
两者不一致时，指标上涨可能不是改坏了，而是你终于看清了问题。**

### A/B 结果（20,000 篇样本，含回归检查）

| 项 | 清洗前 | 清洗后 |
|---|---|---|
| EMPTYPAREN 空括号 | 849 | **0** |
| EMPTYBOOK 空书名号 | 5 | **0** |
| EMPTYQUOTE 空引号 | 14 | **0** |
| DUP_PUNCT 连续标点 | 301 | **0** |
| 残留 `[[` / `{{` / `-{` / `{\|` | 19 / 45 / 22 / 19 | **19 / 45 / 22 / 19**（逐项相同） |

**最后一行是关键：零回归。** 修 bug 最怕按下葫芦浮起瓢 ——
上一轮我就因为顺手把表格正则也改成缓释版，让 `{|` 残留涨了 8 倍。
**每次改清洗器，A/B 报告里必须同时有「改善栏」和「回归栏」。**

### 全量复验（四轮修复累加后的最终结果）

| 指标 | 第二轮后 | 第三轮（空壳） | 第四轮（命名空间） | **第五轮（空壳口径）** |
|---|---|---|---|---|
| 空括号 `（）` | 310,941（7.902%） | **0** | **0** | **0** |
| **括号里只剩标点 `（，）`** | **未检测** | **未检测** | **89,143（2.685%）** | **0** |
| 连续标点 `，。；、` | 76,587（1.946%） | **0** | **0** | **0** |
| 空书名号 `《》` | 49,986（1.270%） | **0** | **0** | **0** |
| 空引号 `「」『』` | 14,532（0.369%） | **0** | **0** | **0** |
| `Category:` 残留 | 492,872（12.553%） | 492,872 | **14** | **14** |
| `File:` 行残留 | 12.8% 文档（样本实测） | 12.8% 文档 | **129 块** | **129 块** |
| 残留 `[[` | 366 | 365 | 397 | **397** |
| 残留 `{{` | 702 | 702 | 675 | **675** |
| 残留 `-{` | 95 | 95 | 94 | **94** |
| 残留 `{\|` | 230 | 230 | 224 | **224** |
| 总块数 | 3,934,758 | 3,926,261 | 3,319,841 | **3,316,395** |

`[看这张表的方法]` **注意「未检测」那两格。** 第三轮我以为空壳已经清零了，
因为验收表报的是 `空括号 = 0`。真实情况是 **89,143 块（2.685%）** ——
不是没修，是**检测规则写窄了，看不见**。

所以这张表要看的不只是"哪些归零了"，还要看**"哪一列是空的"**：
**一个指标如果从头到尾都是 0，先怀疑它是不是没被真正检测过。**

**最后两行是代价**：块数从 3,934,758 一路掉到 3,316,395（**−15.7%**）。
**不是误删** —— 分类标签密集挂在短条目末尾，删掉后整个条目跌破 200 字门槛被剔。
完整排查过程见 §5.5 的「我实测排查过」。

残留 `[[` 从 365 反弹到 397（+8.8%）也是真实代价：不再"抢剥"之后，
少数嵌套 File 从「变成 `File:` 文本」改成了「留着方括号」。
**两项都是必须写出来的，不能只报喜。**

残留标记逐项持平或微降 = 零回归；块数掉 8,497（-0.2%）= 删空壳的真实代价。
`[一句话]` **修 bug 的正确验收姿势，不是"看改善了多少"，而是"改善 + 没伤到别处"两件事同时成立。**

### 第三类：验收表报「0」的假绿 —— 检测规则写窄了（第五轮）

**这一轮不是我发现的，是人工抽样的第 4 条样本逼出来的。**

```text
山田宗昌（やまだ むねなさ，），號匡得，戒名學翁匡德。
                 ↑ 这个逗号后面本该有生卒年（1562年—1636年）
```

原文是 `山田宗昌（{{lang-ja|やまだ むねなさ}}，1562年—1636年）`：
模板删了、生卒年也被删了（生卒年在模板外面但被别的规则吃掉），**逗号留在括号里**。

**而我第三轮修的"空壳"检测，写的是这个：**

```python
"EMPTYPAREN": r"[（(]\s*[）)]"     # ← `\s*` 只认「括号里什么都没有」
```

`（，）` 里还剩一个逗号，`\s*` 匹配不上。于是 **验收脚本连报好几轮 `EMPTYPAREN = 0`，
给了我一个假的"全清干净"**。全量量化后真身是 **89,143 块 = 2.685%**。

`[血泪教训]` **「修过了」和「检测过了」是两件事。**
检测规则写窄，比不写检测更危险 —— 不写你至少还心虚，写了你会拿到一个
**带数字的假绿**，然后放心地把带病数据交出去。

### 修法：检测口径和修复口径必须逐字符对齐

```python
# build_pipeline.py —— 一处定义，两处引用
SHELL_INNER = r"[\s，。；：、,;:\-–—]*"     # 空白 + 中文标点 + ASCII 标点 + 连接号
RE_EMPTY_SHELL = re.compile(
    r"[（(]" + SHELL_INNER + r"[）)]"       # （） （，） （；） （、） （-）
    r"|[《]" + SHELL_INNER + r"[》]"
    r"|[「『]" + SHELL_INNER + r"[」』]"
    r"|[【]" + SHELL_INNER + r"[】]"
    r"|[“]" + SHELL_INNER + r"[”]"
)
```

```python
# verify_chunks.py —— 用同一个字符集，从 build_pipeline 拿常量最稳
"PAREN_PUNCT": r"[（(][\s，。；：、,;:\-–—]*[）)]",
```

`*` 允许零长度，所以原来的空括号 `（）` 仍被同一条覆盖 —— 两条规则合一，不会打架。

`[注意]` **刻意不收 `.` 和 `…`。** `（...）` 是合法的省略用法（"他写了《…》"），
收进来就是误伤，而且会让这一项**永远报红、永远修不干净**。
所以字符集不是"越大越好"，是**够用且不伤人**。

### 第五轮 A/B（20,000 篇，含回归栏）

| 项 | 清洗前 | 清洗后 | 判定 |
|---|---|---|---|
| `PAREN_PUNCT`（括号里剩标点） | 930 | **0** | 归零 |
| `EMPTYPAREN` / `EMPTYBOOK` / `EMPTYQUOTE` | 849 / 5 / 48 | **0 / 0 / 0** | 归零 |
| `EMPTYBRACKET` / `EMPTYDBLQUOTE` / `DUP_PUNCT` | 4 / 36 / 301 | **0 / 0 / 0** | 归零 |
| 残留 `[[` / `{{` / `<ref` / `-{` / `<div` / `{\|` | 23 / 44 / 0 / 22 / 0 / 19 | **23 / 44 / 0 / 22 / 0 / 19** | **逐项相同 = 零回归** |
| 清洗后仍含问题的文档 | 2,175（10.88%） | **19（0.10%）** | ↓115× |

**顺手补了三个同类检测项**（`EMPTYBRACKET` 空【】、`EMPTYDBLQUOTE` 空“”），
它们跟主问题同源，一起收掉，免得下次又变成"漏检"。

`[面试话术]` 「我的验收脚本连报了三轮 `空括号 = 0`，但人工抽样时发现真实占比是 2.685%。
根因是**检测正则写窄了**——只匹配『括号里什么都没有』，漏掉了『括号里还剩一个标点』。
我后来把检测口径和修复口径**抽成同一个常量**，从结构上消灭这类偏差。
这件事教会我：**自动化检查报出的『0』比人工的『看起来没问题』更危险，
因为它带数字、看起来权威。**」

### 同一次人工审计挖出的第二类：命名空间被当正文（比空壳更脏）

翻到 50 条样本的第 2 条，整块就是这个：

```
中国丝绸城步行街  ·  56 字
Category:杭州步行街
Category:拱墅区建筑物
Category:丝绸
Category:杭州市场
```

**一个字正文都没有。** 全量实测：含 `Category:` 的块 **492,872 条 = 12.553%**，
其中整块只有分类标签的 **143,683 条 = 3.660%**。

根因：`[[Category:X]]` **不是链接，是元数据**，必须整块删掉。
旧规则把它当普通内链，只剥掉方括号，把 `Category:杭州步行街` 当正文留下了。

修法一条正则，但**位置是关键**：

```python
RE_CATEGORY = re.compile(
    r"\[\[\s*:?\s*(?:Category|分類|分类|CATEGORY)\s*:[^\[\]]*\]\]", re.I)

t = RE_TABLE.sub("", t)
t = RE_CATEGORY.sub("", t)      # 必须赶在 RE_LINK_* 之前！被剥成纯文本就认不出来了
t = RE_GALLERY.sub("", t)       # 同理，必须赶在 RE_HTML 之前
```

### 第四类：`File:` 残留 —— 问题出在「正则之间抢活」

顺着 Category 查同类，发现 `File:` / `Image:` 残留 **12,644 处 / 20,000 篇**
（约 12.8% 的文档中招）。翻原文，**两个完全不同的来源**：

**来源 1：`<gallery>` 里的文件行本来就没有方括号**

```wikitext
<gallery>
File:Delphi amphitheater from above dsc06297.jpg|位於[[希臘]]的古代劇場
File:Theater Orange.jpg|位於[[法國]]的羅馬劇場
</gallery>
```

这是 gallery 自己的语法。旧版只有 `RE_HTML` 删掉 `<gallery>` `<gallery>` 两个标签本身，
里面的文件行**全部变成正文留下**。

修法：整块删，而且**必须在 RE_HTML 之前**（RE_HTML 先吃掉标签后，这条就匹配不到了）。

```python
RE_GALLERY = re.compile(r"<gallery[^>]*>.*?</gallery\s*>", re.S | re.I)
```

**来源 2：嵌套 File 链接被「抢剥」—— 这个最值得学**

原文：

```wikitext
[[File:maya.svg|thumb|200px|right|[[玛雅文明#数学|玛雅]]数字，黑點代表一，黑直線代表五]]
```

逐轮追踪看到的过程：

```
① RE_FILE 失配        ← 说明文字里嵌了 [[，缓释点号跨不过去
② RE_LINK_PIPE 把内层 [[玛雅文明#数学|玛雅]] 剥成 `玛雅`
③ 外层此刻变"干净"了 → RE_LINK_BARE 抢先把它当普通链接处理
   → 剥成 `File:maya.svg|thumb|200px|right|玛雅数字，黑點代表一，黑直線代表五`
   → 方括号没了，**整块内容变成了正文**
```

**问题不在任何单独一条正则，在它们之间的相互关系。**

修法：给内链正则加**命名空间负向前瞻**，带前缀的一概不碰，交给专门的规则。

```python
_RE_NS = r"(?!\s*(?:File|Image|文件|图像|圖片|Category|分類|分类)\s*:)"
RE_LINK_PIPE = re.compile(r"\[\[" + _RE_NS + r"((?:(?!\[\[|\]\]).)+?)\|((?:(?!\[\[|\]\]).)+?)\]\]", re.S)
RE_LINK_BARE = re.compile(r"\[\[" + _RE_NS + r"((?:(?!\[\[|\]\]).)+?)\]\]", re.S)
```

这样嵌套 File 会在**下一轮迭代**里被 RE_FILE 正确整块删掉。

`[一句话]` **多条正则依次作用于同一段文本时，要把它们当一个「系统」看，
而不是一堆互相独立的规则。** 一条正则的表现，取决于前面几条把它改成了什么样。

### 诚实记录：这一轮**不是**零回归

| 项 | 修前 | 修后 |
|---|---|---|
| `Category:` 残留 | 111,201 | **5** |
| `File:` 行残留 | 12,644 | **19** |
| 残留 `[[` | 19 | **23**（↑ 4） |

`[[` 涨了 4 —— 因为不再"抢剥"之后，极少数嵌套 File 从「变成 `File:` 文本」
改成了「留着方括号」。**绝对量 12,644 → 42，净收益明确，但形态变了，就得写清楚。**

`[一句话]` **"零回归"是理想，不是每次都能拿到。拿不到时如实写出代价，
比假装没有更有说服力** —— 面试官看得出来你在掩饰。

### `[面试话术]` 加分版

「我做数据质量审计时，除了查『标记有没有删干净』，还专门加了一类
**『删过头』检测** —— 检查模板被删除后留下的空壳括号。扫全量发现空括号占 7.9%、
31 万块，是残留标记的 440 倍。

有意思的是，这个问题在前两轮修复中完全没被看见：我一直在优化『正则能不能匹配上』，
默认『删得越多越干净』。**实际上清洗有两个方向的风险，而我只防了一个。**
修的时候我没去补模板名单——那有几十种变体，补不全；而是加了一条兜底规则
直接清空壳，并且把检测口径和修复口径对齐后才上全量。」

**这段话值钱的地方**：它展示的不是"我会写正则"，而是**你建立了双向的质量视角，
并且知道"穷举补名单"和"兜底兜底"哪个方案更稳。**

## 5.5 这一步的产出（全量实测值，直接抄）

```
原始文档        raw_docs                : 1,507,969
清洗后(≥200字)  clean_docs              : 866,112     (剔掉 641,857，42.6%)
文档去重后      dedup_docs              : 866,103     (重复率 0.0%)
切块后          chunks_before_filter    : 3,709,162
质量过滤后                              : 3,351,587   (剔掉 9.6%)
chunk 去重后    chunks_final            : 3,316,395
平均块/文档                             : 3.83
耗时            elapsed_sec             : 371.6 秒（6.2 分钟）
吞吐            throughput              : 4,058 文档/秒
产物大小                                : 2.1 GB（snappy parquet，4 个 part）
```

`[注意]` **清洗器一共修了四轮。** 每轮的表现不一样，值得放在一起看：

| 轮次 | 修了什么 | clean_docs | chunks_final | 块数变化 |
|---|---|---|---|---|
| 第一轮 | 初版清洗器 | 1,018,077 | 3,946,101 | — |
| 第二轮 | 嵌套正则（缓释点号 / 语言变体跑两遍） | 1,017,253 | 3,934,758 | −11,343（−0.29%） |
| 第三轮 | 空壳 + 命名空间（Category / File 行） | 866,685 | 3,319,841 | −614,917（−15.6%） |
| **第四轮** | **空壳检测口径写窄（`（，）` 这类漏检）** | **866,112** | **3,316,395** | **−3,446（−0.10%）** |

**前两轮是"量级无关"的修复**（改的是标记残留形态，块数只微动）；
**第三轮是"量级级"的修复**（删掉的分类标签让大批短条目跌破门槛，掉 15.6%）；
**第四轮又是微调**（只多删了 573 篇的边缘条目）。

`[一句话]` **看块数变化就能反推修复的性质：**
**掉 0.1% 是"改了形态"，掉 15% 是"改了内容"。** 前者要盯回归，后者要盯误删。

第三轮掉了 **14.8%** 的文档，但空壳 + 命名空间只删掉了全语料 **1.27% 的字符**。
为什么不成比例？**因为被删的是分类标签和文件行，它们密集出现在短条目末尾** ——
一个原来 230 字的条目，末尾挂着 10 个 `[[Category:...]]`，删完只剩 150 字，
**整个条目就跌破 200 字门槛被剔掉了**。

`[我实测排查过]` 这不是误删。我抽查了「原文 ≥2000 字但清洗后 <200 字」的 114 篇
（占样本 0.57%），**全部是以模板 / 表格 / 图片说明为主的条目**，本身就没有成段正文。
另外验证了 `<gallery>` 标签在 20,000 篇样本里 **2,486 开 / 2,486 闭完全成对**，
排除了「未闭合导致非贪婪匹配一路吞到远处」的风险。

`[权衡]` 丢掉 15% 的条目，换来的是**知识库里不再有"整块只有分类标签"的零信息块**。
对 RAG 来说这是划算的：< 200 字的碎块检索价值本来就低，而纯噪声块会直接污染召回。

`[一句话]` **怎么判断两次跑出来的数字差是"抖动"还是"真变化"？看量级。**
差几十条（3,934,7xx 浮动）是规则在 50 字下限附近的翻转，属正常抖动；
差几千条（3,316,395）是内容真的变了。

产物尺寸对比（同一份数据）：

| 形态 | 大小 | 压缩比 |
|---|---|---|
| 原始 bz2 dump | 3.4 GB | 1× |
| 解析出的 JSONL | 9.2 GB | **膨胀 2.7×**（JSON 转义 + UTF-8 中文 3 字节） |
| 清洗后 Parquet | 2.3 GB | **比 JSONL 小 4 倍**（列存 + snappy + 去掉了 32% 文档） |

**这三个数字本身就是一个知识点**：JSONL 中间态是"必要的浪费"，
但一定要在下一步压成列存，否则向量化阶段光是读盘就吃不消。

`[验收标准]`

| 项 | 标准 | 实测 | 过？ |
|---|---|---|---|
| 读入条数 = JSONL 行数 | 完全一致 | 1,507,969 | ✅ |
| char_len 落在 [50, 800] | 无越界 | min 50 / p50 209 / max 800 | ✅ |
| cn_ratio 全部 ≥ 0.30 | 无越界 | 0 条违约 | ✅ |
| 唯一 doc_id = 唯一 title | 一致 | 833,615 = 833,615 | ✅ |
| 残留 wiki 标记占比 | < 0.1% | **0.042%**（1,406 / 3,316,395） | ✅ |
| **清洗副作用（空壳，7 项）** | **全 0** | **全 0** | ✅ |
| **命名空间残留** | **< 0.01%** | **0.004%**（CATEGORY 14 / FILEXT 129） | ✅ |
| 无空 title | 0 | 0 | ✅ |
| **`section` 字段可用** | 长条目要有小节名 | 78.5% 有值（713,278 块为空，多属短条目） | ✅ |

`[注意]` **验收标准表必须包含你修过的那一项。** 第一版这张表里既没有"清洗副作用"行、
也没有"命名空间残留"行 —— 因为那时候我根本没想到要检测它们。
修完之后把两类都加进标准表，意味着**下次再跑，任一项不达标都会立刻暴露**，
而不是等人工抽样撞见。

`[但更狠的教训在第五轮]` **光"加进表里"不够 —— 还得确认那行真的检测得出来。**
我把"空壳"加进了验收表，然后连收三轮 `0`，直到人工抽样撞见 `（，）`。
真相是 **89,143 块（2.685%）**：检测正则只认「括号里什么都没有」，
认不出「括号里还剩一个逗号」。

`[一句话]` **每次修完一个 bug，就把它变成验收表里的一行。**
**再加一句：这一行必须用一个"已知必定能命中"的样例验证过，否则它只是个装饰。**

`[section 字段这条怎么来的]` 人工审计第 1 条样本是这样：

```
section = '子长市'
chunk_text:
  *马家砭镇强家沟村（6）
  *杨家园则镇王家坪村（6）
```

**块内没有任何一句"这是子长市的村"**，光读正文根本不知道这些村属于哪儿。
但 `section` 字段记着 `子长市` —— 检索时拼 `title + section + chunk_text`
（`陕西省历史文化名村 / 子长市 / …`）上下文就补齐了。
这是 title-aware 切块的价值所在，**必须在第 7 步向量化时把 section 拼进待编码文本**，
否则白白浪费了这个字段。

后面推到集群跑一遍，再记一组数，两组一比就是**加速比**。

---

# 第 6 步 · 数据质量审计

**为什么单独做这一步**：面试官问"你的数据真实吗、质量怎么样"，说"我用的维基百科"没有说服力。**能拿出一张审计表才有。**

## 6.1 抽样人工核查（必须人眼看）

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" src\audit_sample.py --n 50 --seed 42
```

产物两个：
- `eval\audit_sample.md` —— **你要打开的就是这个**，逐条带打勾框
- `eval\audit_sample.seed42.json` —— 含 `chunk_id`，便于复查和将来对比

`[验收]` 最后看到：

```
AUDIT_SAMPLE_OK
  samples          : 50
  len_min          : 53
  len_max          : 800
  len_avg          : 291
  dirty_by_machine : 0  (0.0%)      ← 机器预检：无残留标记的条数
  markdown         : E:\AI-learning\projects\rag-kb\eval\audit_sample.md
  json             : E:\AI-learning\projects\rag-kb\eval\audit_sample.seed42.json
```

`[可复现]` 抽样用 `orderBy(F.rand(seed)).limit(n)`，seed 固定 → **同数据同 seed 必得同一批样本**。
实测验证过：改脚本重跑，50 个 `chunk_id` 与上一版**逐位一致**，所以人工判定结果不会因为重跑而失效。
这是能做"逐条复盘"的前提 —— 否则你判完，别人复现不出同一批数据，审计就没有公信力。

`[坑]` **别用 `python -c "..."` 写多行脚本**。手册初版就是那么写的，两个错：
1. cmd.exe 里 `-c "` 的外层双引号会被正文里的 `"` 提前闭合 → `SyntaxError`
2. `df.takeSample()` **不存在** —— 那是 RDD 的方法，DataFrame 上直接报
   `AttributeError: 'DataFrame' object has no attribute 'takeSample'`

正解是 `orderBy(F.rand(seed)).limit(n)`：`rand` 带 seed 保证可复现，而且 Catalyst 会把
`Sort+Limit` 优化成每个分区本地取 n 条候选再归并（`TakeOrderedAndProject`），
**不做全表 shuffle**，400 万行也就一两分钟。
➜ 结论：**多行 Python 一律写进文件再跑**，内联 `-c` 只用于真·一行命令。

打开 `E:\AI-learning\projects\rag-kb\eval\audit_sample.md`，**一条条看**，对照这个标准打分：

| 判据 | 通过 | 不通过 |
|---|---|---|
| 通顺 | 是完整句子，读得懂 | 断在半句、夹杂符号 |
| 自洽 | 前后说的是同一件事 | 明显拼接错误 |
| 有信息量 | 读完知道了一个事实 | 全是"参见""另见""外部链接" |
| 无残留标记 | 没有 `[[` `{{` `<ref` | 有 |

统计通过率，例如 `47/50 = 94%`。**这个数写进 README。**

`[实测]` 本项目的真实结果：**50 / 50 = 100%**。
第一轮人工判定是 48/50（4 号、8 号判不通过），复核后两条都撤回 ——
**问题出在审计材料，不在数据**，根因见下一节。这一段比通过率本身值钱。

## 6.1.1 🔥 人工审计也能假阳性：审计材料自身必须印全字段

这是第 6 步唯一一个真坑，而且**它不是数据的坑，是工具链的坑**。

### 症状

第一轮人工判 50 条，撞出两条「不通过」：

| # | 标题 | 判它的理由（按当时材料） |
|---|---|---|
| 4 | 明珠广场站 (绍兴市) | 标题带 `(绍兴市)`，正文里搜不到这个后缀 |
| 8 | 蓬镇 | 标题是地名，正文却在讲媒体 → 判「不自洽」 |

听起来都挺有道理。但**两条都是误判**。

### 根因：`section` 字段根本没印在审计材料上

8 号那条的真实数据是：

```
title   = 蓬镇
section = 媒体          ← 这一行，第一版材料没印
正文     = 法国主要的媒体均可在蓬镇接收……
```

**`section = 媒体`，而正文讲的就是媒体** —— 完全自洽。
它不是"标题讲 A、正文讲 B"，而是**一块标题为 `A` 的条目中「关于 A 的 B 小节」**，
这是 title-aware 切块的**设计意图**，不是拼接错误。

那个字段在 `audit_sample.py` 里**被读出来了、也写进了 JSON**，
唯独渲染 Markdown 时漏了 —— 于是判「自洽」的人手里缺了唯一能判它的信息。

> 换个说法：**判据 2「自洽」在缺 `section` 时，原理上无法判对。**
> 任何「标题 X + 正文讲 X 的某一节」都会被判成「标题讲 A、正文讲 B」。
> 材料没给你信息，你却把结论记在数据头上。

4 号是另一类：`section` 为空（短条目导语段），正文完整、信息量充足，
分歧点在**标题的消歧义后缀** `(绍兴市)` —— Wikipedia 用它区分同名条目，
正文本来就不会重现这个后缀，判据本身需要写清楚这一点。

### 修法（两条，都不碰数据）

1. `src/audit_sample.py` **v2**：详情区每条印出
   `**章节**：\`媒体\` · **检索串**：\`蓬镇 / 媒体 / 正文\``，
   并在文件开头讲清「**喂给向量模型的是检索串整条，不是单独正文**」；
   判据 2 补两条 ✅ 标准（消歧义后缀、正文讲的就是它那一节）。
2. `src/make_audit_sheet.py`：总览表加**「章节」列**，让人扫表时就能看见归属。

**改完重跑，50 个 `chunk_id` 与旧材料逐位一致，人工判定不作废。**

### 这一段的面试话术

> 「我抽 50 条人工审计，第一轮 96%。但我没直接把这个数写进 README，
> 而是逐条复核了那 2 条 —— 结果发现**是审计材料缺了一个字段**，
> 导致一条正常的分节切块被判成拼接错误。我把材料补上，通过率才是真实的 100%。
> 这件事之后我定了一条纪律：**审计工具的字段完整性，和被测数据的字段完整性，是同一件事**。」

`[纪律]` 判据的每一条，都要反问一句：**我手上的材料，够不够判这一条？**
不够就先修材料 —— 别急着在数据上记一笔。

`[注意]` 这一步不能省。你会发现某些类型的条目被切得很难看（比如列表型、表格型条目），**这本身就是你要在 README 里写出来的局限性**——面试官喜欢能说出自己系统缺陷的人。

## 6.2 完成六项审计表

在 README 里填这张表（数据从管道统计和上面抽样里来）：

| 审计项 | 本项实测值 | 怎么来的 |
|---|---|---|
| 可溯源 | chunk 带 `doc_id` / `title` / `section`；`section` 有值率 **78.5%** | Parquet schema |
| 抽样通过率 | **50/50 = 100%**（初判 48/50，复核撤回 2 条） | 6.1 + 6.1.1 |
| 文档重复率 | **0.001%**（866,112 → 866,103，剔 9 篇） | 管道统计 `doc_dedup_rate` |
| chunk 过滤率 | **10.59%**（3,709,162 → 3,316,395） | 管道统计 `chunk_filter_rate` |
| chunk 长度分布 | min 50 / **P50 209** / mean 285 / max 800 | `char_len` 直方图，见下 |
| 时效性 | **未实现**（当前 dump 不含 `last_modified`） | M2 阶段补 |

`[注意]` **"未实现"也是有效答案。** 六项里做不了的那一项，老实写「未实现 + 原因 + 计划」，
比硬编一个数有价值 —— 审计表的功能是**暴露边界**，不是把六行凑满。

`[数字要对得上]` `chunk 过滤率 10.59%` 拆开看是两段：
质量过滤 357,575 块（9.6%）+ chunk 级去重 35,192 块（0.95%）。
**报一个合计数之前先确认它由哪几项构成** —— 被问"这 10% 都是些什么"时答不上来就露馅了。

### 长度分布直方图（跨两个 Python，必须分工）

`[为什么必须跨]` 读 parquet 要 pyspark → 只能 3.11；画图要 matplotlib → 只装在 3.13。
两边用**一个 12 MB 纯文本文件**当桥。这不是绕路，是**让每个解释器只干它会干的事**。

**第一步：用 3.11 导出长度**

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python311\python.exe" src\export_char_len.py
```

**第二步：用 3.13 画图**

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\plot_len_dist.py
```

`[实测]` 两步的真实输出：

```
EXPORT_CHAR_LEN_OK
  rows        : 3,316,395
  files       : 1 个 -> eval\results\char_len_parts
  size        : 12.0 MB
  elapsed     : 6.6 s

PLOT_LEN_DIST_OK
  n / mean / p50 / max : 3,316,395 / 285.0 / 209 / 800
  <100 字占比          : 21.1%
  >=800 硬截断占比     : 2.5756%
  图 -> eval\results\chunk_len_dist.png
```

`[坑·三个]` **别把这两步写成内联 `python -c`**（手册初版就是那么写的，实测必炸）：

1. **不设 `HADOOP_HOME` 直接抛 `java.lang.UnsatisfiedLinkError: NativeIO$Windows.access0`。**
   Spark 能启动、session 能建，**一读 parquet 就崩** —— 它要加载 `winutils.exe` 里的原生方法。
   脚本里的 `ensure_hadoop_home()` 修的就是这个（易错点表 K 项）。
2. **`collect()` 把 331 万个 int 全拉回 driver**，Python 侧就是 100 MB+ 的对象，
   后面再加任何操作都容易顶爆。改成 `write.text()` 落盘，driver 峰值内存 ≈ 0。
3. cmd.exe 的引号地狱（同 6.1 的坑）。

`[验收]` 图能打开，并且**要读这三个数**才算验完：

| 判读点 | 本次实测 | 不健康说明什么 |
|---|---|---|
| 别全挤最左边 | `<100 字` 占 **21.1%** | 全挤左边 = 按句号硬切，语义被切碎 |
| 顶部别成一根针 | `≥800 字` 占 **2.58%** | 针状尖峰 = 大量块被 `max_len` 硬截断，长段落策略失效 |
| 长尾要有 | P50 **209** / max **800** | 没有长尾 = 长文档被过度切分 |

实测形态：主峰在 50–150（短条目导语），长尾拖到 800，**800 处是小台阶而非一根针** ——
符合"段落级切分、只有超长段才硬切"的设计。

产物：`eval\results\chunk_len_dist.png`（左直方图 + 右 CDF）、
`eval\results\chunk_len_stats.json`（机器可读统计量，README 直接引用）。

---

## 6.3 这一步的产出（已落地）

| 产物 | 位置 |
|---|---|
| **六项审计表** | `README.md` |
| 50 条人工审计 + 复核记录 | `eval\audit_sample.md` |
| 抽样原始数据（含 `chunk_id`，可逐条复查） | `eval\audit_sample.seed42.json` |
| 长度分布图 + 统计量 | `eval\results\chunk_len_dist.png` / `chunk_len_stats.json` |
| 产物验收日志（残留 / 副作用 / 命名空间） | `eval\verify_v6.log` |
| 管道统计（那三个数写进简历） | `eval\results\m1_stats_20260917_124346.json` |

`[一句话]` **第 6 步真正的产物不是那个通过率，是 README 里那张会被追问的表。**
每一项都要能回答三个问题：这个数怎么来的、当时的标准是什么、不达标会怎样。
答不出第三问的审计项，等于没做。

---

# 第 7 步 · 向量化

**为什么这一步的工具链和第 5 步完全不同**

| 步骤 | 主角 | 解释器 | 前置依赖 |
|---|---|---|---|
| 第 5 步 清洗切块 | Spark | **3.11** | winutils.exe / JAVA_HOME |
| 第 7 步 向量化 | torch | **3.13** | pyarrow（读 parquet） |

两步**唯一的交集是那个 parquet 文件**。而 3.13 里装了 pyarrow：
`pyarrow.dataset.dataset(目录, format="parquet")` 就能直接读 Spark 的输出目录
（注意：Spark 输出的是**目录**，`pq.ParquetFile(目录)` 会报 `PermissionError: WinError 5`，要用 dataset API）。

➜ **第 7 步不需要 3.11、不需要 Spark、不需要 winutils。** 别习惯性去调 3.11 ——
那边没装 torch，一 `import` 就 ImportError。

## 7.1 待编码文本怎么拼（最容易做错的一步）

```python
f"{title}｜{section}｜{chunk_text}"      # section 为空时 → f"{title}｜{chunk_text}"
```

三条理由：

1. **`section` 必须拼进去** —— 手册 §5.5 末尾已经论证过：`陕西省历史文化名村 / 子长市`
   那块正文里一个字都没提「子长市」，上下文全靠 `section` 补。
2. 分隔符用**全角竖线 `｜`**：中文维基正文里几乎不出现，不会被模型误认成内容。
3. `section` 为空时**不留空段** —— 21.5% 的块没有 section，否则拼出 `title｜｜text`。

## 7.2 跑

```cmd
cd /d E:\AI-learning\projects\rag-kb

REM ① 小样本验通（约 1 分钟）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\vectorize.py --limit 5000 --out eval\results\vec_smoke

REM ② 语义冒烟：真查一次，别只看形状
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\smoke_search.py --index eval\results\vec_smoke --topk 3

REM ③ 全量（可随时 Ctrl+C，之后 --resume 接着跑；约 8.3 小时）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\vectorize.py --resume --log-every 20000
```

## 7.3 实测数据（2026-09-17）

**小样本 5,000 条**：44.7 秒 / 112 chunk/s；自检范数 [0.9995, 1.0005]；NaN = 0。

**语义冒烟 —— 这一步才是真验收**：

| Query | Top-1 | 相似度 |
|---|---|---|
| 台灣東部開發於古時的人行道路 | 蘇花古道 | 0.6075 |
| 中國的化學工程學家 | 张克忠 | 0.6449 |
| **這個地方氣候怎麼樣** | **高邮市 · 气候** | 0.5767 |

🔴 **第三条才是重点。** query 里**没有任何地名**，命中的却是 `高邮市/气候`、`杭州市/气候` ——
**全是「气候」小节**。这直接证明 `title｜section｜正文` 的拼法让向量学到了**小节级语义**。
只喂 `chunk_text` 做不出这个效果：那两块正文里「气候」二字可能只出现一两次。

`[纪律]` **形状对、范数对、无 NaN 只能证明代码没写错，证明不了向量可用。**
检索里最贵的错误是「形状全对但语义全错」（拼串顺序反了、ids 与行号错位、关键内容被截断），
**这些都不报错**，只有真查一次才暴露。

**吞吐对比（同样 20,000 条真实 chunk，非伪样本）**：

| 模型 | 维度 | batch | 吞吐 | 全量 331.6 万块 | 索引大小 (fp16) |
|---|---|---|---|---|---|
| **bge-large-zh-v1.5** | 1024 | 64 | **110.7 c/s** | **≈ 8.3 小时** | 6.79 GB |
| bge-large-zh-v1.5 | 1024 | **128** | **99.8 c/s** | — | — |
| **bge-small-zh-v1.5** | 512 | 64 | **1102.7 c/s** | **≈ 50 分钟** | 3.39 GB |

`[坑]` **batch 不是越大越快。** 64 → 128 反而从 110.7 掉到 **99.8** chunk/s ——
显存换页 + padding 浪费抵消了并行收益。**该测就测，别凭直觉调参。**

`[数字]` large 与 small 吞吐**差 10 倍**，而参数量只差 3 倍。
这一步的耗时**完全由模型选型决定**，"等一下就好"是错的判断。
选型对比与决策记录见 `第7步-任务清单.md`。

## 7.4 这一步实测到的坑

**坑 A：`show_progress_bar=False` 会让 FlagEmbedding 直接报 TypeError。**

```
TypeError: PreTrainedTokenizerBase.pad() got an unexpected keyword argument 'show_progress_bar'
```

新版 FlagEmbedding（带 `utils/tokenizer_compat.py` 的那版）会把多余 kwargs **透传给 `tokenizer.pad()`**。
**别传这个参数** —— tqdm 进度条走 stderr 原地刷新，重定向到日志后不会淹掉正文。

**坑 B：`--limit` 小样本时，最后一批必须切齐到 N。**

scanner 按 `read-batch` 取数，最后一批必然超出 N；
直接 `emb[pos:pos+len] = vecs` 会 `could not broadcast`。
**写 memmap 前先切片对齐**（`texts, cids = texts[:k], cids[:k]`）。

**坑 C：`512 token` 是 BGE 的模型上限，不是可调参数。**

实测 **16.16%** 的块（20,000 条中 3,232 条）token 数超 512，会被**静默截断**
（token 长度 P50 = 201 / P90 = 609 / max = 827）。被截掉的是块尾内容。
`max_position_embeddings = 512` 改不动 —— 要更长上下文只能换模型（如 bge-m3）。
**这个数必须测出来**，否则检索效果差时，你分不清是模型问题还是截断问题。

`[注意]` **`smoke_search.py` 从 parquet 回查原文时，顺序必须按 `ids.txt` 重建。**

`pyarrow` 的 `filter=isin(...)` **不保证返回顺序**。必须用 `{chunk_id: row}` 建字典再按 `ids` 取，
否则会出现"相似度很高但内容对不上"的假故障 —— 那其实是**行号错位**，不是向量错了。
（本脚本已按此实现，见 `smoke_search.py` 里 `got = {...}` 那几行。）

## 7.5 中断与续跑（真事故，必看）

### 事故经过（同一晚断了两次）

| 次序 | 时间 | 中断点 | 剩余 |
|---|---|---|---|
| 第一次 | 9-17 16:14 | **20.8%**（689,687 行） | 6.4 小时 |
| 第二次 | 9-17 23:41 | **76.4%**（2,534,401 行） | 1.8 小时 |

两次表现完全一样：进程随关机结束，**日志里一个报错都没有** —— 不是崩溃，是被操作系统收走了。
表现是：日志停在某批 `Inference Embeddings: 9%` 之后，文件时间戳不再变化。

`[注意]` **怎么判断是"关机杀的"而不是"脚本崩了"**：崩溃必有 traceback；
没有 traceback 但进程没了 → 查系统日志：

```powershell
Get-WinEvent -FilterHashtable @{LogName='System'; StartTime=(Get-Date).AddHours(-12)} |
  Where-Object { $_.Id -in 1074,109,41,42,107 } |
  Select TimeCreated,Id,ProviderName,@{n='Msg';e={$_.Message.Substring(0,90)}}
(Get-CimInstance Win32_OperatingSystem).LastBootUpTime
```

| 事件 ID | 含义 |
|---|---|
| `1074 User32` | **发起关机/重启**。消息里带发起进程 —— 第二次正是 `StartMenuExperienceHost`，即用户从开始菜单点的关机 |
| `109 Kernel-Power` | 内核电源管理器已启动关闭转换 |
| `41 Kernel-Power` | 非正常掉电 |
| `42 / 107 Kernel-Power` | 进入睡眠 / 从睡眠恢复。**睡眠不杀进程**（挂起，唤醒后继续） |

`[结论]` 两次都是**主动关机**，不是意外。所以真正的教训不是"小心点"——
而是 **长任务必须假设它一定会被打断**（人要睡觉、要关机），
"能续跑"是硬需求，不是加分项。

### 根因：续跑起点查错了文件

原实现从 `progress.json` 读 `done`。问题在于 **`progress.json` 每 10 万行才落一次盘**：

| 文件 | 关机那一刻的值 |
|---|---|
| `ids.txt`（每批都写） | **689,687** ← 真值 |
| `progress.json`（每 10 万行写） | 601,827 ← 滞后 8.8 万 |

拿滞后的数当起点，会把 8.8 万行**再 append 一遍**。

### 连带的第二个 bug（更阴）

`done` 通常不是 `read-batch` 的整数倍，所以续跑时**跨起点的那一批**会整批写进 `ids.txt`，
把起点之前的行重复 append。而 `emb.npy` 对应那几行是**重复覆盖**（同样的文本算出同样的向量），
**内容看不出任何异常** —— 于是错位被完美伪装，只在检索时表现为
「相似度 0.7 的块，内容完全对不上」。

### 修法（已在 `vectorize.py` 落地）

1. **起点改数 `ids.txt` 的行数**，不读 `progress.json`。
   为什么它可靠：主循环是**先写 `emb.npy`、后写 `ids.txt`**，
   所以 `ids.txt` 的行数永远不会超过 emb 已写好的行数（最多落后一批）。
2. 若 `ids.txt` 末行不完整（断电写了一半）→ **丢弃该行**，绝不留残缺 id。
3. 跨起点的批次只 append `cids[skip:]`，`skip = max(0, done - pos)`。
4. 新增**接缝自检**：跑完立刻查 `done` 前后各 3 行的范数 + `ids.txt` 行数 == 已编码数。
   `[易错]` 只查开头 1000 行是查不出这个问题的 —— 那部分是上一轮写的，本来就没问题。
5. 行数不一致直接 `return 3` 并以非零码退出。

### 验证方法（改完必须做，别凭"看代码觉得对"）

跑一次**真实的中断→续跑对照实验**：干净跑 3,000 条存基线 → 把 `ids.txt` 砍到 1,559 行
（故意取非批次整数倍）+ 把 `progress.json` 伪造成滞后的 `done=1200` → `--resume` 续跑
→ 与基线 `cmp` 逐字节比对。

实测结果：`ids.txt` 与 `emb.npy` **字节完全一致**，续跑日志正确识别「起点 = 1,559 行」。

### 操作纪律（长任务通用）

- 这种几小时的任务**必须能续跑**，且**必须在真中断场景下验过**再放它跑。
- 关机 / 快速启动关机 会杀掉进程；**睡眠不会**（进程挂起，唤醒后继续）。
- 判断进度**只信 `ids.txt` 的行数**：`wc -l data\index\bge-large-zh-v1.5\ids.txt`。
- **每次续跑前先体检**，别默认上一轮是干净的。
- 🔌 **跑之前插上电源。** 实测第 3 次中断就是**电池耗尽触发的系统关机**，不属于任何人的手误 ——
  这类关机的事件 `1074` 发起者是 `winlogon.exe`（代表 `NT AUTHORITY\SYSTEM`），
  与用户点击关机的 `StartMenuExperienceHost` 可区分。**要跑 8 小时的任务，插电是前置条件。**

### 配套工具：`src\check_vectors.py`（续跑前 / 进检索前必跑）

查的是**跨文件的三个不变量** —— 单独看任何一个文件都"正常"，只有放一起才错：

| 不变量 | 发现什么 |
|---|---|
| `ids.txt` 行数 **≤** `emb.npy` 行数 | 若 ids **反超** → 真错位（说明 ids 写到了 emb 没写的地方） |
| id **无重复** | 重复 = 续跑把同一段 append 了两遍（§7.5 的 Bug A/B 特征） |
| 已写区间内抽样范数 ≈ 1.0 且无零向量 | 零向量 = 那块**根本没写过**，是预分配空洞被当成了数据 |

```
"...Python313\python.exe" src\check_vectors.py
```

**实测结果（76.4% 时）**：`2,534,401` 个 id **全部唯一，无一重复** → 证明第一次续跑没写歪。

`[易错]` **检测口径必须与事实对称** —— 这个脚本自己第一版就踩了：

初版把「`ids` 行数 ≠ `emb` 行数」一律判成错位，于是把**正常的"未跑完"**报成 🔴
（`emb.npy` 是 `open_memmap(w+)` **预分配**到 331.6 万行的，未跑完时尾部本来就是一片零）；
抽样也抽到了尾部空洞，又误报 2,000 个零向量。

修正后：`ids > emb` 才算错位；`ids < emb` 是正常的未完成状态；抽样只在 `[0, n_ids)` 内做。
**这条和 §5.4「检测口径 == 修复口径」是同一条纪律** —— 检测器自己也会犯。

---

# 第 8 步 · 建索引 + 混合检索

第 7 步产出的向量只是"一堆数字"。这一步才把它变成一个**能回答问题的检索器**。

## 8.1 三路检索的分工（先拿失败案例，再定分工）

| 路 | 强项 | 弱项 | 实测证据 |
|---|---|---|---|
| 向量（bge-large-zh） | **语义泛化**：问「這個地方氣候怎麼樣」（query 里没有任何地名）能命中气候小节 | 专名可能学不好 | Q1 把「東寧路 (臺南市)」排第 1 ❌ |
| BM25（倒排 + jieba） | **字面精确**：专名一次命中 | 关系与泛词：「中國」修饰不了「化學工程學家」 | Q3 把「韩东君 · 評價」排第 1 ❌ |
| 混合（RRF 融合） | 取长补短 | 不是免费的（见 8.4） | Q1 把正确答案从 `vec#42` 提到第 1 ✅ |

**这不是设计时拍脑袋定的分工，是先跑完两条单路、拿到各自的失败案例之后才定的。**

## 8.2 跑

```cmd
cd /d E:\AI-learning\projects\rag-kb

REM ① 建 BM25 倒排（全量约 20 分钟，16 进程）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\build_bm25.py

REM ② 建 faiss IVF 索引（约 8 分钟，落盘 13.6 GB）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\build_faiss_index.py --quantizer flat --metric ip --out "data\index\faiss_ivf_flat.index"

REM ③ 三模式对照（向量 / BM25 / 混合）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\search_hybrid.py --compare --vec-backend faiss --nprobe 512
```

`[验收]` 最后看到 `SEARCH_HYBRID_OK`，且**每个 query 都列出三路 top-5 和融合来源标记**：

```
  ── HYBRID ── [向量 0.17s · BM25 45ms · 融合 0.2ms]
  1. RRF 0.11014   (vec#42  bm25#1)   row=0
```

`(vec#42 bm25#1)` 这个标记是关键 —— 它告诉你**这条结果是谁拉上来的**。
没有它就只能看到"答案对了"，看不到"为什么对"。

## 8.3 实测数据

### 向量索引用哪种 faiss（这是本步最反直觉的一段）

| 方案 | nprobe | 延迟 | recall@10 | 索引体积 |
|---|---|---|---|---|
| IVF **SQ8**（标量量化） | 128 | 196 ms | 0.960 | 3.44 GB |
| IVF **SQ8** | 4096（全扫） | 5,511 ms | **0.980** | 3.44 GB |
| **IVF Flat（无量化）** | 128 | 60 ms | 0.970 | 13.6 GB |
| **IVF Flat** | **512** | **249 ms** | **1.000** | 13.6 GB |
| 暴力精确（对照） | — | 8,217 ms | 1.000 | — |

`[结论]` **SQ8 全扫也只有 0.98 —— 那 2% 是存储层的硬失真，调 nprobe 救不回来。**
Flat 只是"少扫几个簇"，扫到 12.5% 就拿到 1.000。

`[注意]` 我一开始只跑了默认的 `--quantizer sq8`，据此写下"faiss 在这个规模是负收益"，
补跑 `flat` 之后结论整个反转。**没测过的分支 ≠ 不存在的差异。**

### 三路延迟（faiss nprobe=512 + 全量倒排）

| 环节 | 耗时 |
|---|---|
| 向量路 | **170~270 ms** |
| BM25 路 | **41~52 ms**（首查 653 ms，含 jieba 冷启动） |
| RRF 融合（200 个候选，纯 Python） | **0.2~0.3 ms** |
| **合计** | **≈ 220~320 ms** ✅（阈值 2 秒） |

## 8.4 🔥 这一步最重要的坑：RRF 的 `k=60` 在我们的数据上是错的

**RRF 是什么**：把多路的「排名」合成一个分数，`score(d) = Σ 1/(k + rank(d))`。
它只用排名不用分数，天然免疫量纲差异 —— Elasticsearch 8.x 的官方融合就是它。

**但 k 不是无关紧要的常数**：

```
第 1 名 vs 第 20 名：
    k=60 → 1/61 = 0.0164  vs  1/80 = 0.0125    （差 31%）
    k=10 → 1/11 = 0.0909  vs  1/30 = 0.0333    （差 173%）
```

k 越大、排名越"平"，**「两路都在中游」就越容易反超「单路第一名」**。
实测（`src\probe_fusion.py`，3 题）：

| 策略 | Q1 | Q2 | Q3 | 命中 |
|---|---|---|---|---|
| `rrf k=60`（ES / 原论文默认） | ✅ | ❌ | ❌ | 1/3 |
| **`rrf k=10`** | ✅ | ✅ | ❌ | **2/3** |
| `cc`（min-max 归一化加权） | ✅ | ❌ | ❌ | 1/3 |
| `cc`（z-score 归一化加权） | ✅ | ❌ | ❌ | 1/3 |
| `--fuse gate`（领先幅度门控） | ✅ | ✅ | ✅ | **3/3** ⚠️ |

**Q2 的现场**（最能说明问题）：

```
朱汝瑾   vec#22 + bm25#18 → k=60: 1/82 + 1/78 = 0.0250   ← "两路都在中游"，赢了
张克忠   vec#1  + bm25#—  → k=60: 1/61        = 0.0164   ← 单路第一，输了
                             k=10: 1/11        = 0.0909   ← 换 k 之后赢回来
```

**Q3 为什么连 k=10 也救不了**：纯 RRF **完全丢弃分数强度**，区分不了

- BM25「**强**匹配」：蘇花古道 27.05 分，领先次名 **33.4%** → 该信
- BM25「**弱**匹配」：韩东君 19.90 分，领先次名 **2.8%** → 纯噪声

两者都是 `rank 1`，拿一样的权重。

**`--fuse gate` 用「领先幅度」当置信度信号**（33.4% / 6.9% / 2.8% 完美三分），
低于 10% 就把 BM25 权重降到 0.3，3/3 全过。

`[⚠️ 必须标注]` **那组阈值是在这 3 条 query 上拟合的，样本量严重不足。**
3/3 只能当**线索**，不能当**结论** —— 所以脚本默认 `--fuse rrf`，gate 只作实验入口。
**把它默认打开才是不负责任。**

**cc（分数加权）为什么三种做法全败**：两路的**分数分布形状根本不同** ——
向量余弦挤在 0.55~0.68 的窄带，BM25 是 0~27 的长尾。任何单调归一化都会扭曲"置信度"语义：
min-max 把两路 top1 都拉成 1.0（强弱抹平），
z-score 又因为向量分布太窄而**放大**了它的噪声（向量 top1 只领先 0.1% → z 值 1.52，
反被 BM25 的 2.10 压过）。

**所以最终默认：`--rrf-k 10`。** 只改一个数字，Q2 从错变对。

## 8.5 这一步踩到的坑

### 坑 A：`scan_topk` 返回顺序解包反了 —— 分数变成行号，零报错

`scan_topk` 返回 `(scores, rows)`，调用处写成 `v_rows, _ = scan_topk(...)`。
于是 **0.6803 这个分数被当成行号 `int()` 成 0**，100 个候选全塌成 `row=0`，
在 RRF 里同一个 key 被累加 100 次，**伪造出 0.992 的假高分**。

症状：VECTOR 那列 top-5 全是 `row=0`。**不报错、不崩溃、不警告。**

修法（两件事一起做）：
1. 把 `scan_topk` 的返回改成 `(rows, scores)`，**全项目统一「行号在前」**
2. 调用处加断言 —— 把静默错误变成显式崩溃：

```python
def _assert_rows(rows, n, tag):
    if len(rows) == 0:
        return
    if rows.dtype.kind != "i":
        raise AssertionError(f"{tag} 返回的 dtype={rows.dtype}，不是整数行号 —— 多半是解包顺序反了")
    if rows.max() >= n or rows.min() < 0:
        raise AssertionError(f"{tag} 行号越界")
```

`[面试话术]` 凡是「同 dtype / 同形状的两个数组」成对返回，调用点必须显式命名（`a, _ =` 就是隐患），
并且加一条断言 —— **这类 bug 靠 code review 和肉眼审不出来。**

### 坑 B：`faiss.IO_FLAG_MMAP` 不支持 IVF 索引，且报错信息完全看不出原因

```python
faiss.read_index(path, faiss.IO_FLAG_MMAP)
# RuntimeError: could not load ArrayInvertedLists as 646f6c69 ("ilod")
```

`6c6f6469` 是 `"ilod"`（InvertedLists On Disk）的十六进制。报错里没有一个字提到 mmap。

根因：**mmap 只能映射连续内存块**，而 IVF 的倒排列表是一堆分离的 `ArrayInvertedLists`，
没有连续布局可映射。所以 IVF 只能整体读入（13.6 GB 常驻）。

修法：try/except 回退到整体读入，并把这条写进注释 —— 下次不用再查一遍。

### 坑 C：查询分词口径必须 `import` 建索引那份，不能抄

`search_bm25.py` 直接 `from build_bm25 import STOP, _keep`。
如果为了"解耦"把 `_keep` 抄一份过来，哪天建索引那边改了规则、查询这边不跟，
表现就是「某些词永远搜不到」—— **不报错、不崩溃，纯粹静默失灵**。

`[⚠️]` 这和 §5.4「检测口径 == 修复口径」是同一条纪律的第三次出现：
**同一件事的两处实现，必须共享同一份定义。**

### 坑 D：`ids.txt` 是第 7 步的全局产物，别在 `data\index\bm25\` 下找

行号 → `chunk_id` 的映射是**全项目全局契约**
（parquet 行序 == `emb.npy` 行序 == `ids.txt` 行序 == BM25 postings 的 `doc_id`）。
它属于第 7 步的产物，BM25 检索器只是**借用**，默认路径指向 `data\index\bge-large-zh-v1.5\ids.txt`。

## 8.6 这一步的产出（已落地）

| 文件 | 说明 |
|---|---|
| `src\build_bm25.py` | 倒排索引构建（两趟分词 + CSR 直写，无需排序） |
| `src\search_bm25.py` | BM25 检索（字典序二分查找，无常驻 dict） |
| `src\search_hybrid.py` | 三模式检索 + RRF 融合（`--rrf-k 10` 默认，`--fuse gate` 实验性） |
| `src\probe_fusion.py` | 5 种融合策略对比实验 |
| `src\check_vectors.py` | 索引体检（续跑前 / 进检索前必跑） |
| `data\index\bm25\` | 3,092,039 词 / 1.92 亿 postings / 1219 MB |
| `data\index\faiss_ivf_flat.index` | 13.6 GB，nlist=4096，**nprobe=512 时 recall 1.000** |
| `eval\results\hybrid_compare.log` | 三模式对照实测 |
| `eval\results\fusion_probe.log` | 5 种融合策略对比实测 |

**遗留（下一步的方向，不是本步的疏漏）**：
- 3 条 query 的评测集**太小**，所有融合结论都需要扩到 30~50 条标注 query 才算数
- 真正的解法是加 **reranker**（cross-encoder 对 top-50 精排），
  它比任何"融合策略调参"都更根本 —— 但那是独立的一步
- Q2 的"正确"依赖 tie-break（同分时行号小的胜出），**换规则就翻**，不是稳健的正确

---

# 第 9 步 · 生成与引用

> M2 让系统「**找得到**」，这一步让它「**答得出**」。
> 交付物不是一堆原文片段，而是**带 `[n]` 引用的答案**，以及「不知道就拒答」的纪律。

## 9.1 Prompt 的三条设计（是设计，不是调参）

| 设计 | 为什么 |
|---|---|
| 上下文带 `[1] [2] …` 编号，要求答案回标 | 没编号模型只能说"根据资料"，你**无法验证它用了哪条**。编号把引用变成**离散符号选择**任务，错误率降一个数量级，也让"可验证"变成可自动打分的指标 |
| **拒答条款**：找不到依据时**逐字**回答「根据已有资料无法回答」 | LLM 默认"尽力回答"，不给这条库外问题会被自信地胡说，外面还包着一层"参考资料里说"的皮 —— **比纯幻觉更危险**。给**逐字话术**（而非"不知道就说不知道"）是为了能自动判定 |
| 先结论后依据 | 输出结构可预测，M4 自动评测才好抽取 |

⚠️ 还有一条经验规则：**system prompt 里要求"用中文回答"，但不要强制简体**。
语料是繁体维基，模型跟着语料用繁体更忠实（专名如「蘇花古道」不该被改成简体）。
代价是判定拒答时必须**繁简两种写法都匹配** —— 见 9.5 坑 AH。

## 9.2 跑

```cmd
cd /d E:\AI-learning\projects\rag-kb
set PY=C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe

REM ① 先看 prompt 长什么样（不调模型、零成本）—— 第一次务必先跑这个
"%PY%" -u src\generator.py --backend echo --limit 1

REM ② 云端一路：库内 10 条 + 库外 3 条
"%PY%" -u src\generator.py --backend dashscope --file eval\m3_in_kb.txt  --tag in_kb
"%PY%" -u src\generator.py --backend dashscope --file eval\m3_out_kb.txt --tag out_kb

REM ③ 本地一路（需先下 Qwen2.5-1.5B-Instruct）
"%PY%" -u src\generator.py --backend local --limit 2

REM ④ 演示界面 → 浏览器打开 http://127.0.0.1:7860
"%PY%" app\gradio_app.py
```

**为什么第一条必须先跑 `echo`**：prompt 里的问题（编号错位、上下文被截断、section 混进噪声）
在 echo 下一眼可见；而调 LLM 时你只会看到"答案好像不太对"，**无从定位是哪一环错了**。

**预期输出**：每条打印「检索 top-5（带两路排名）+ 答案 + 【核查】引用编号」，
末尾 `GENERATOR_OK` 汇总拒答数 / 非法编号数 / 耗时中位数。

## 9.3 实测数据（2026-09-19）

**库内 10 条**：繁简修复后 **10/10 全部正确、0 条非法编号**（修复前是 9/10，见 9.4）。
**库外 3 条**：**零幻觉**（2 条明确拒答 + 1 条"合理推断 + 明确免责"）。

| 环节 | 实测（复测 14 条一次跑完） |
|---|---|
| **检索总计** | **1534 ~ 3195 ms**（中位 ~2280；优化前 2378 ~ 4999） |
| └ 向量（faiss nprobe=512） | 776 ~ 3166 ms（**抖动大**） |
| └ BM25 | 40 ~ 752 ms |
| └ RRF 融合 | 0.2 ms |
| └ 回查原文 `fetch_texts` | 1250 ~ 2855 ms → 优化后约 **1.8x** |
| 云端生成 qwen-plus | 1.96 ~ 12.22 s |
| 本地生成 Qwen2.5-1.5B 4bit | ≈3 s / 显存 **1.07 GB** |

**回查原文的优化做对了但收益有限**：改成按行号定位分片后只快 **1.8x**（不是预期的 4x）——
top-5 的行号通常散落在约 3 个分片，只省掉 1/4 扫描。**别把"改动正确"当成"收益很大"。**

🔴 **检索侧没达到第 8 步那个 249 ms 的基准** —— 13.6 GB faiss 常驻挤掉 page cache 后，
向量检索退化到 776~3166 ms。第 8 步测 249 ms 时数据是热的。
**"基准数字"和"真实运行"的差距要如实记下来，别拿基线当承诺。**

**库外 3 条**：零幻觉（2 条明确拒答 + 1 条"合理推断 + 免责说明"）。
→ 拒答应该是**三档**：`拒答 / 推断+免责 / 编造`，M4 判定口径要按这个改。

## 9.4 🔥🔥 这一步最重要的坑：繁体 query 在进入检索前就归零了

**症状**：Q5「張克忠的籍貫是哪裡」误拒，但 Q2「中國的化學工程學家」里张克忠明明检索得到。

**对照实验**（只差 4 个字）：

| query | 向量 | BM25 | 检索 top1 | 结果 |
|---|---|---|---|---|
| 「张克忠的籍贯是哪里」简体 | vec#5 | bm25#1 | **张克忠** ✅ | 答"天津" |
| 「張克忠的籍貫是哪裡」繁体 | 未进 top100 | bm25#1（错条目） | 哈辛·耶巴達 ❌ | **误拒** |

**根因两层，第二层最阴**：

1. **字面层**：繁体「張克忠」在（未归一化的）语料里查不到，df=0，这个词等于不存在。
2. **分词层**：jieba 词典以简体为主，**繁体串会被切碎成单字**，
   而单字在第 8 步就被 `_keep` 过滤掉了（那是正确的决定：单字全是噪声）。
   → **query 里唯一有信息量的词，在进入检索之前就消失了。**
   注意：就算把「張」加进 jieba 词典也没用 —— 只要它被切成单字，照样被丢。

**修法**：`src\zh.py` 提供**唯一**的 `norm_text()`，**建索引与查询共用同一个函数**。

```python
# build_bm25.py（索引侧）
for w in jieba.lcut(norm_text(t)):        # ← 归一化必须在 lcut 之前

# search_bm25.py（查询侧）
return dict(Counter(w for w in jieba.lcut(norm_text(q)) if _keep(w)))
```

**验收（先验纯函数，比跑 20 分钟索引划算）**：

```
「張克忠的籍貫是哪裡」→ ['张克忠', '籍贯', '哪里']
「张克忠的籍贯是哪里」→ ['张克忠', '籍贯', '哪里']       对称 ✅
「台灣東部開發於古時的人行道路」→ ['台湾','东部','开发','古时','人行道']
                                    ↑ 归一化后 jieba 终于切出「人行道」这个正确词
```

重建索引：

```cmd
"%PY%" -u src\build_bm25.py          REM 全量约 20 分钟，16 进程
```

⚠️ **旧索引会被自动拒绝启动**：`meta.json` 里记了 `zh_norm` 字段，`load_bm25` 启动时校验。
状态不一致时报错并退出 —— 因为不一致的后果是"某些词永远搜不到"，**静默、无报错**。

## 9.5 这一步踩到的其他坑

**① `echo` 模式的核查结果是假的**
初版把 echo 回显的 prompt 也交给核查逻辑判定。而 prompt 里天然带 `[1]…[5]` 和那句拒答话术，
于是输出「引用了全部编号 + 判定拒答」。
**害处在于它看起来像核查逻辑坏了，实际是喂错了输入。**
修法：后端加 `is_model` 标记，echo 直接跳过核查。

**② `vectorize.find_model()` 是 bge 专用的，不能拿去查别的模型**
它的模式串硬编码成 `bge-{kind}-zh`。传 `"qwen-instruct"` 会拼成 `bge-qwen-instruct-zh`
→ 永远找不到，而报错信息会让你以为"模型没下载"。
修法：`generator.find_local_model(name_substr)` 按名字片段 glob，通用于任何模型。

**③ `app/` 下的脚本要把 `src/` 插进 `sys.path`，不是自己所在目录**
`sys.path.insert(0, HERE)` 插的是 `app/` → `import generator` 直接 `ModuleNotFoundError`。

**④ 下载脚本里配的是 base 版 Qwen，不是 Instruct 版**
`Qwen/Qwen2.5-1.5B`（base）**没有 chat template**，`apply_chat_template` 直接抛错，
而且它只会顺着上下文续写，**不会拒答、不会标引用** —— M3 要的两件事它一件都做不到。

## 9.6 这一步的产出（已落地）

| 文件 | 说明 |
|---|---|
| `src\generator.py` | 生成与引用（三后端：echo / dashscope / local）+ 引用核查 |
| `src\zh.py` | 繁简归一化（建索引与查询的唯一入口） |
| `app\gradio_app.py` | 演示界面（薄层：检索/prompt 全复用 `src/`） |
| `eval\m3_in_kb.txt` / `m3_out_kb.txt` | 验收问题集（10 库内 + 3 库外） |
| `eval\results\gen_dashscope_*.jsonl` | 每条问题完整记录（答案/引用/耗时/命中） |
| `data\index\bm25\` | 重建为**归一化**版本（`meta.zh_norm = "zh-cn"`） |

**遗留**：
- 回查原文（1.2~2.9 s）比向量检索还慢，**是端到端的大头**。可行优化：按行号区间定位分片
- 生成本身没有量化评测（无 Faithfulness / 引用准确率自动指标）—— M4 的核心任务
- 本地 1.5B 能答对、能拒答，但**标不稳引用编号**；云端稳定标注。这是 M4 消融要量化的差距

---

# 第 10 / 11 步 · 评测集构建与检索评测

> 方案里称 M4"全项目最值钱的一段"。这一步把"好不好"从印象变成数字。

## 11.1 怎么跑

```bat
REM ① 候选 → 正式评测集（有道过滤，秒级，不花钱）
python src\make_testset.py

REM ② 检索评测（三路 + 重排一次跑完，约 8 分钟）
python src\evaluate.py --testset qa_testset_v1.jsonl --tag v1_final --rerank

REM ③ 生成对比表（读 JSON 出 markdown，避免手抄错）
python src\report_ablation.py
```

**`--rerank` 关掉就不加载 cross-encoder**，其余指标照常 —— 池子里的三路排名照样有。

## 11.2 三个口径问题（不写清楚数字就会被误读）

| # | 事实 | 为什么重要 |
|---|---|---|
| ① | **Recall@K ≡ Hit Rate@K** | 每题只有 1 个 gold。两个都报是术语对齐，不是两个独立指标 |
| ② | **主指标用 strict（chunk 级）** | 标准答案从 gold chunk 提取，同条目的**另一条** chunk 未必含答案；relaxed 会虚高 |
| ③ | **三路只跑一遍** | `per_path` 一次返回两路完整排名 —— 省 3 倍时间，且三路用同一批查询向量 |

**relaxed 的正确用途是诊断**：
- strict 低 + relaxed 高 → 方向对了没精确定位 → **上 reranker**（实测正是这种情况）
- 两个都低 → 检索本身没找到 → 改召回

## 11.3 字形归一化消融怎么跑

思路：**用归一化前的旧索引**跑同一套题（旧索引当初特意留着没删）。

```bat
python src\evaluate.py --testset qa_testset_v1.jsonl --tag v1_nonorm ^
       --no-norm --bm25-index data\index\bm25_old_normoff --rerank
```

⚠️ **`--no-norm` 和 `--bm25-index` 必须成对**。脚本会在启动前校验
`meta.zh_norm` 与开关是否匹配 —— 只关一边是**静默漏词**（不报错，只是分数悄悄掉），
那会得出"归一化没用"的**反向结论**。这个校验宁可拒绝启动。

**实测结果**（跨字形子集 n=50，Recall@5 strict）：

| 链路 | 归一化开 | 归一化关 |
|---|---|---|
| 纯向量 | 0.660 | 0.660 ← **对照组，一分不差** |
| 纯 BM25 | 0.800 | 0.300 |
| 混合+重排 | 0.980 | 0.920 |

**「繁问简答」的 BM25：0.833 → 0.000（全灭）** —— 第 9 步那个 bug 的精确复现。

## 11.4 踩的坑

| # | 坑 | 症状 | 根因 | 修法 |
|---|---|---|---|---|
| A | **`t2s`/`s2t` 标签贴反** | "繁问简答"8 条问题全是简体 | 定义、prompt、plan 三处不一致；prompt 和 plan **互相自洽**所以跑得通、不报错，只有标签名和定义相反 | ① 改源码让三处一致 ② **导出层不信任 kind，用 `q_fanti`/`c_fanti`（生成后实测）重新推导** |
| B | 修正逻辑插在剔除判定**之后** | "两侧同字形"的题永远剔不掉 | 排序敏感的逻辑，位置就是正确性 | 整段前移到 `reason` 判定之前 |
| C | `qid` 用序号 | 一改排序全体漂移，人工黑名单全部失效 | 序号依赖排序结果 | 改用**问题文本的 md5 前 6 位** |
| D | reranker 的 tqdm 污染日志 | 按 `^N/104` 提取进度的 grep 失效，以为进程卡死 | `compute_score` 默认写 `\r` 进度条 | `show_progress_bar=False`（带 TypeError 兜底，参数名随版本变过） |

**坑 A 的额外收获**：用事实推导标签时，**顺带抓出 2 条"字形转换根本没生效"的题**
（标为跨字形，实际两侧同字形）。这类题测不出跨字形能力，属于必须剔的废题 ——
**如果只改标签名而不改判据来源，这 2 条会一直留在评测集里。**

## 11.5 🔴 匿名题型设计失败（本轮最重要的发现）

**预期**（写在第 10 步文档里）：匿名题里没有专有名词 → BM25 无从下手 → 只有向量能答。
**实测**：向量 0.375 / BM25 0.812，**方向完全相反**。

**根因**：匿名化只删了实体名，**描述词照样逐字来自原文**：

| 题型 | 问题实词与 gold 原文的重合率 |
|---|---|
| 基础事实 | 0.657 |
| **实体匿名** | **0.661** ← 和基础事实一样 |
| 释义改写 | 0.514 |

实例（真实数据）：
> 「一座**市立博物馆**，位于**海滨**百道开发区中央，西边是**福冈塔**，北边是**福冈巨蛋**，
> 还收藏着著名的**汉倭奴国王印**」→ 与 gold 原文重合率 **0.80**

"福冈塔""福冈巨蛋""倭奴国王印"确实**不在条目名里，但全在 chunk 正文里** ——
而我的约束是"禁止出现**原文里的**专有名词"，LLM 完全合规地绕了过去。

**由此得出的真结论**（比原假设有价值）：整体上混合 RRF 是正收益（R@5 0.788 → 0.875），
但**增益全部来自基础事实题**；在匿名子集上混合（0.781）**反而比纯 BM25（0.812）差** ——
向量路只有 0.375，它贡献的候选稀释了 BM25 的好结果。
⇒ **"混合检索值不值"没有全局答案**，得看子集；改进方向是加权 RRF 或查询路由。

**教训**：题型设计必须用数据校验，不能靠"看起来对"。
**如果没算那个重合率，这份报告会得出一个完全错误的结论。**

## 11.6 产出

| 文件 | 说明 |
|---|---|
| `src\make_testset.py` | 候选 → 正式评测集（含按实测字形推导标签） |
| `src\evaluate.py` | 检索评测（三路 + 重排一次跑完，含归一化配对校验） |
| `src\rerank.py` | bge-reranker-v2-m3 封装 + **已知答案自检**（`--smoke`） |
| `src\report_ablation.py` | 读 JSON 出 markdown 对比表 |
| `eval\qa_testset_v1.jsonl` | 定稿评测集（129 条） |
| `eval\results\ablation_tables.md` | 全部对比表 |

---

# 易错点速查表

| 编号 | 坑 | 症状 | 解法 |
|---|---|---|---|
| **A** | **PySpark 用 Python 3.13** | **能启动，一执行计算就崩** | **Spark 一律用 3.11**（见 0.1） |
| B | `pip --user` 假成功 | 报成功但 import 失败 | 用 `--target=` |
| C | `JAVA_HOME` 空 | Spark 起不来 | `setx JAVA_HOME ...` + **重开终端** |
| D | pyspark 装成 4.x | 要 Java 17 | 锁 `pyspark==3.5.8` |
| E | 用 huggingface | 下载 0 字节 | 一律走 ModelScope |
| F | bz2 截断 | `EOFError` | 正常现象，脚本已捕获 |
| G | XML 命名空间 | 解析出 0 条 | 用 `_local()` 剥命名空间 |
| H | iterparse 不 clear | 内存涨到崩 | 脚本已处理 |
| I | worker Python 不一致 | `Python in worker has different version` | 设 `PYSPARK_PYTHON` |
| J | 输出目录已存在 | `path already exists` | `mode("overwrite")` |
| K | winutils.exe 缺失 | Windows 上**写** Parquet 必失败（读不会）→ 前 6 步全绿、第 7 步炸 | **不是 WARN**。下 `winutils.exe` + `hadoop.dll` 到 `<项目>\hadoop\bin`，见 5.0 |
| L | 数据放 C 盘 | 磁盘爆 | 全放 E 盘 |
| M | 用裸 `python` | 跑到 3.14 上，一堆 import 失败 | 用完整路径，分清 3.13 / 3.11 |
| N | 中文用户名 | 路径写进配置文件变乱码 `鎼炴噦` | 配置里用 `~` 或纯 ASCII 路径 |
| O | 单线程下维基 | 只有 107 KB/s，3.2GB 要 8 小时 | 用 `download_wiki_mt.py` 多线程，18 分钟 |
| O2 | **并发开太大** | `HTTP 429 Too Many Requests`，203 片失败 107 片 | 降到 4 并发 + 读 `Retry-After` **全局**冷却；`--workers` 别超 4 |
| O3 | 短测外推长期行为 | 28 秒测出 3.2 MB/s，跑 15 分钟就被限流 | 压测要跑够时长，或直接按最保守参数用 |
| O4 | 只看"约 3.2 GB" | 少了 2 个分片也像"3.2GB" | **比精确字节数**，跟远端 `Content-Length` 对齐 |
| P | JAVA_HOME 是空字符串 | `os.environ.setdefault()` 不生效（键已存在），Spark 仍报找不到 Java | 用 `if not os.environ.get('JAVA_HOME'):` 判空后再赋值 |
| Q | 在 3.13 里装 pyspark | `import` 成功、`count()` 崩，自检还显示 OK，极难排查 | 已从 3.13 卸载，Spark 只用 3.11 |
| **R** | **Expat 在 Windows 上 2^31 字节溢出** | iterparse 解析 >2GiB 的 XML，稳定在累计输入 2^31 字节处报 `out of memory`，**但进程内存充足**（`XML_LARGE_SIZE` 默认关闭 → 32 位计数器） | **换逐行流式解析**（`wiki_extract.py` 默认 `--engine line`），彻底不用 expat |
| **S** | **把"真错误"当成"正常截断"** | 脚本报"完成"，实际丢了 90% 数据（只留 153,123 条 / 真实 1,507,969 条） | 只有 `EOFError` 算正常结束；其他异常必须 `[致命错误]` + 退出码 1 |
| T | 解析器没有自检 | 只能靠"没报错"判断正确性 | 用源数据自带字段核对（`<text bytes="N">` → 实测 100% 一致）；两套独立实现交叉验证 |
| U | **拿样本外推整体规模** | 432MB 样本推 79 万条，实测 150.8 万，**差 1.9 倍**；连带把"C 盘要 5GB"这类规划全算错 | 规模必须**全量实测**。真要外推，至少取 3 个不同位置的样本看方差 |
| **V** | **Windows 上用 `local[*]` 跑 PySpark UDF** | `Connection reset by peer: socket write error` + `Python worker exited unexpectedly`，**Python 侧只有 EOFError，看不到真死因**；第 1 次 UDF 好好的、第 2 次才崩 | 24 核 = 24 个 Python worker 一起抢内存。改用 `--master "local[4]"`（脚本已把 Windows 默认值改成这个） |
| **W** | **`spark.read.json` 自动推断 schema** | 试跑 2000 条和跑全量一样慢 | `samplingRatio` 默认 **1.0** → 先把整个 9.4GB 解析一遍猜类型。改用显式 `StructType` schema |
| **X** | **不清洗结果就连续 count()** | 每个 action 都重算整条 lineage，UDF 被反复执行 | Spark 惰性求值。清洗完立刻 `.cache()`，让 UDF 只跑 1 次（本机实测 20k 篇差 3 倍时间） |
| **Y** | **缓存原始数据而不是清洗结果** | `MemoryStore: Not enough space to cache ... Persisting block to disk`，白写白读 9.4GB | 要缓存的是**清洗后**的窄数据，不是原始 9.4GB 文本。用一次 `agg` 同时拿原始/清洗后条数 |
| **Z** | **检测规则写窄了 → 验收表报「0」的假绿** | `EMPTYPAREN = r"[（(]\s*[）)]"` 只认「括号里什么都没有」，抓不到 `（，）`（模板删了、逗号留下）。连报三轮 `0`，真实占比 **2.685%（89,143 块）** | 检测口径和修复口径**抽成同一个常量**（`SHELL_INNER`），从结构上消灭偏差。⚠️ 自动检查报的「0」比人工的「看起来没问题」更危险，因为它带数字、看起来权威 |
| **Z2** | **`rm -rf … 2>/dev/null` 吞掉删除失败** | 旧产物没删掉却以为删了，等待循环一看到旧的 `_SUCCESS` 就退出，读到上一轮数据（症状：**验收数字和上一轮一模一样**） | 删完必须 `ls` 确认；判完成要**三件事同时看**：日志收尾统计行 + 产物 UUID/时间戳 + 进程已退出 |
| **Z3** | **长轮询命令撞工具超时，连坐杀掉后台进程** | `sleep 200` + 40 次循环被终止时，**同进程组的 java 一起被带走**，日志停在半路、无 `hs_err_pid`（不是 JVM 崩） | 轮询命令保持短（`sleep 240` + `timeout 280`）；判"没输出"要先区分"在算"和"被打死" |
| **AA** | **同形状数组成对返回时解包反了** | `v_rows, _ = scan_topk(...)` 而它返回的是 `(scores, rows)` → **0.6803 这个分数被 `int()` 成 0**，100 个候选全塌成 `row=0`，RRF 里同一 key 被累加 100 次 → **伪造出 0.992 的假高分**。全程零报错 | 全项目统一「行号在前」；调用点**显式命名**（禁 `a, _ =`）；加 `_assert_rows` 断言把静默错误变成显式崩溃（`rows.dtype.kind != "i"` 即炸） |
| **AB** | **`faiss.IO_FLAG_MMAP` 不支持 IVF 索引** | `RuntimeError: could not load ArrayInvertedLists as 646f6c69 ("ilod")` —— 报错里**一个字都没提 mmap** | mmap 只能映射连续内存，IVF 的倒排列表是一堆分离的 `ArrayInvertedLists`。只能整体读入（13.6 GB 常驻），用 try/except 回退 |
| **AC** | **照搬 RRF 的 `k=60` 默认值** | 融合效果 **1/3**（`k=10` 是 3/3）：k=60 时第 1 名 vs 第 20 名只差 31%，**「两路都在中游」反超「单路第一」**（0.0250 > 0.0164） | k 决定头部差异放大倍数（k=10 时差 173%）。`k=60` 是给「两路质量相当」的场景准备的；BM25 质量波动大时必须调小 |
| **AD** | **只测一个分支就下"某方案更好"的结论** | 用 `--quantizer sq8`（默认值）跑完就写"faiss 是负收益"，补跑 `flat` 后**结论整个反转**（0.980/5511ms → **1.000/249ms**） | 有多个选项的参数就是**会改变结论的变量**，不是"等价写法"。**没测过的分支 ≠ 不存在的差异** |
| **AE** | **分数归一化融合（cc）对齐不了两路语义** | min-max 把两路 top1 都拉成 1.0（强弱抹平）；z-score 因向量分布窄（top1 仅领先 0.1%）反而放大噪声，被 BM25 的 2.10 压过 | 两路分数**分布形状不同**（向量 0.55~0.68 窄带 vs BM25 0~27 长尾），任何单调归一化都会扭曲「置信度」。要么用纯排名（RRF），要么上 reranker |
| **AF** | **把回显的 prompt 交给核查逻辑判定** | echo 模式报「引用了全部编号 + 判定拒答」—— prompt 里天然带 `[1]…[5]` 和拒答话术。**看起来像核查逻辑坏了，实际是喂错了输入** | 后端加 `is_model` 标记；非模型后端直接跳过核查 |
| **AG** | **拿 `vectorize.find_model()` 查非 bge 模型** | 传 `"qwen-instruct"` → 拼成 `bge-qwen-instruct-zh` → 永远找不到，报错还会让你以为"模型没下载" | 该函数的模式串是硬编码的（参数只支持 large/small）。改用 `generator.find_local_model(name_substr)` 按名字片段 glob |
| **AH** | **拒答判定只匹配简体写法** | 语料是繁体 → 模型答「**無法**回答」→ 判成「拒答 0 条」。**程序不报错、指标也"正常"，只是数字是错的** | 繁简两种写法全列。同类教训：任何"关键词匹配"都要先问一句"**语体会变吗**" |
| **AI** | **繁简混排语料下直接用原文 query 检索** | 繁体「張克忠」在简体索引里 df=0；jieba 还把繁体串切碎成单字、**被单字过滤吃掉** → 整个 query 的信息量在进入检索前就归零。实测同一个问题繁简两版：一个答对、一个误拒 | 建索引与查询**共用同一个** `zh.norm_text()`（归一化要在 jieba 分词之前）；归一化状态写进 `meta.json` 并在加载时校验 —— 不一致必须拒绝启动，否则是静默漏词 |
| **AJ** | **靠印象判断"语料的库外边界"** | 拿「SpaceX 星舰第五次试飞」「iPhone 17 Pro 芯片」当库外题 → **两条都命中了语料**（dump 比预想的新），"拒答成功"变成"答对了"，指标完全失效 | 用「**库内实体 + 库外属性**」（问「张克忠的血型」）而不是「未来事件」。后者模型本来就没知识，拒答是必然，**测不出拒答机制好使不好使** |
| **AK** | **"匿名化"了却仍能被关键词检索到** | 匿名题（去掉专名）上 BM25 0.812 **反超**向量 0.375，与"只有语义检索能答"的预期完全相反 | **删掉实体名 ≠ 降低词面重合**。描述词（"接受按立""教区""主教""单瓣""复瓣"）本来就逐字来自原文，而它们才是 BM25 的得分来源。实测词面重合率：匿名题 0.661 vs 基础事实 0.657 —— **一样高**。正确的匿名必须**同义改写**，不是删专名 |
| **AL** | **等权 RRF 在"两路能力差距大"时是负收益** | 匿名子集：纯 BM25 0.812、混合 RRF **0.781** —— 混合反而更差 | 弱路（向量 0.375）贡献的候选会**稀释**强路的好结果。RRF 只看排名、不看置信度，无法自适应。⇒ "混合值不值"**没有全局答案**，要按子集看；改进方向是加权 RRF 或查询路由 |
| **AM** | **标签名与实现相反，而代码"跑得通、不报错"** | `t2s`（定义=繁问简答）导出的问题**全是简体** | prompt 和 plan **互相自洽**（都按反语义配），只有标签名和定义相反 —— 所以无异常、无报错，只是报告方向全错。修法：① 改源码三处一致 ② **导出层不信任 kind，用生成后实测的 `q_fanti`/`c_fanti` 重新推导**。第 ② 层还顺带抓出 2 条"字形转换根本没生效"的废题 |
| **AN** | **对照实验"只关一边"= 静默漏词** | 想量"归一化值多少"，若关掉查询侧归一化却仍用归一化索引 → 分数悄悄掉 | 归一化必须**索引侧与查询侧成对**。`evaluate.py --no-norm` 会在启动前校验 `meta.zh_norm` 与开关是否匹配，**不匹配直接拒绝启动** —— 宁可跑不起来，也不要得出反向结论 |
| **AO** | **判据又一次骗了我：把 `leak_ratio==0` 当废题信号** | 抽检脚本把 7 条 easy 题报成"题面与原文无共同词"的废题。查定义才发现 `leak_ratio` 是「**问题 4-gram 命中率，只比 chunk_text**」，实测 104 题里 **18 题本来就是 0.000**、中位仅 0.25 —— **零重合不代表题目有问题**（「康托尔用什么字母表示序数？」是好题），只说明**这题对 BM25 不友好** | 报警判据必须**先用已知答案的样本校准**。同时把该判据从"报警"降级为"**提示**"（不是缺陷，但影响结果解读）。这是本项目**第二次**踩"判据比真相更响"（第一次是第 10 步的连续子串判据） |
| **AP** | **同一指标两个口径并存，绝对值差 2.5 倍** | 报告里"词面重合率"出现过两组数：11.5 的 easy 0.657 / anon 0.661（**临时算的，无脚本存档，无法复现**）vs 评测集 `leak_ratio` 字段的 0.260 / 0.244 | 两个口径的**相对关系完全一致**，所以核心结论（匿名化没降低词面重合）**对口径不敏感、是稳的**。但临时数字**不该写进正式结论**。纪律：**只引有定义、有脚本、可重算的指标**；写下的数字必须能一键复现 |
| **AQ** | **以为"融合效果不好"是权重没调好** | 加权 RRF 扫描四组：等权 0.875、1:2 0.817、1:3 0.798、2:1 0.769 —— **等权就是最优**。但分题型看：1:2 能把匿名子集从 0.781 救回 0.812（追平纯 BM25），代价是基础事实 0.956→0.844 | 两个子集需要**相反**的权重配比 ⇒ **任何全局权重都是妥协，最优妥协恰是等权**。这不是参数问题，是**架构问题**：正确解法是**查询级路由**。教训：**全局指标不动时，别急着调参 —— 先分子集看，可能是需求本身分裂了** |
| **AR** | **抄来的"上限类"默认值没人验证过**（`Reranker(max_length=512)`） | 它管的是 **(query, passage) 对**的总长，**不是 passage 单独的长度**。实测 **1 token ≈ 1.38 字符**（不是"中文 1 字 1 token"）⇒ 512 只够约 370 字，而 gold passage 均值 413 字 → **20.2% 的 gold 被静默截断** | 截断**伤 R@1（0.762 vs 0.916，差 15.4 点）却不伤 R@5（0.952 vs 0.964，差 1.1 点）** —— gold 仍在池里，只是压不过别的候选。调到 **1024**：R@1 +0.019 / MRR +0.013，端到端只 +1.7%。**纪律：任何"max/limit/上限"类参数，先花 20 秒量出"有多少样本越界"再决定要不要动它**（`src/diagnose_rerank_trunc.py` 只加载 tokenizer） |
| **AS** | **归因靠均值，不靠切分** | "整体 R@1 涨了 0.019"说明不了任何机制。按 ml=512 时 gold 是否被截分组后：**被截的 21 条 R@1 +0.095、R@5 直接做到 1.000；其余 83 条输出逐位不变** | 收益 **100% 集中在被截组** ⇒ 因果链闭合。⚠️ 但注意"完整组零变化"是**恒等式而非统计证据**（未截断时两次喂给模型的 token 序列完全相同，输出必然一样）—— 它的价值是证明**改动无全局副作用**，不是"效果显著"。**别把恒等式当发现** |
| **AT** | **拼接长度的常数照抄模型家族** | 按 BERT 的 `[CLS] q [SEP] d [SEP]` 记成 `+3`；实测反推是 **`+4`**：`qn=15, dn=488 → 估算 506，真实 507`。原因是 bge-reranker 用 **XLMRobertaTokenizer**，模板 `<s> q </s></s> d </s>` 有**双 `</s>`** | 差 1 个 token 会让"恰好卡边界"的样本被误判。**必须用 `tokenizer(q, d, truncation=False)` 反推一次，不能照抄。**（本项目 104 条恰无临界样本，是运气不是设计。）这条属于**验证工具本身** —— 检查工具写错时，它给的"通过"是假的 |
| **AU** | **召回池设 100，作为重排输入一半是浪费** | `pool` 100 → 50 → 30，重排延迟 1830 → 1437 → 1164 ms，R@5 是 0.9712 / **0.9808** / 0.9712 | 但**该降的不是 `--pool`**（它还管 RRF 融合输入，耦合着降要掉 R@5）。解耦后（池 100 / 候选 50）：**R@5 0.9904（104 题只差 1 条）、重排 1344 ms（−27%）—— 两个维度同时变好**。⚠️ 别被 `ms/对` 骗：它随池子缩小反而**升高**（17.8 → 37.0），因为 batch padding 开销不按比例减少 —— **看延迟看绝对 ms，不看单位成本** |
| **AW** | **一个参数绑住两件事，结论只能是"混合效果"** | `--pool` 同时控"各路召回池"和"喂给重排的候选数"。耦合着扫，只能说"降池子不亏"；**解开后才知道两者该各取最优**（池保持 100 保召回 + 候选降到 50 省时间），R@5 比耦合版再 **+2 条** | 加一个 `--rerank-pool`（默认 0 = 跟随 `--pool`，保证旧结论可复现）**只花 7 分钟**，收益超过前面三轮融合调参的总和。**看到"一个开关影响两个量"，先解开再下结论 —— 这是性价比最高的一类改动** |
| **AV** | **`show_progress_bar=False` 是个无效参数（静默被吞）** | 传了它，日志照样被 `Compute Scores` 的 `\r` 进度条刷屏、grep 失效 | 新版 FlagEmbedding 的签名是 `compute_score(..., **kwargs)` —— 多余参数**照单全收再丢弃**，不报错也不生效；真正的开关写死在源码里 `tqdm(..., disable=len(pairs) < batch_size)`。✅ 正解是**从外面静音 stderr**：`with contextlib.redirect_stderr(io.StringIO()):`。**推广：给第三方传"文档里看起来对"的参数前，先 `inspect.signature` 确认它真有这个名字** |
| **AX** | 🔴 **"最后一次性落盘"把 13 分钟的计算量押在一次 IO 上** | 本地路生成 129 条跑了 **13.6 分钟**，日志里每条都打印"成功"，产物文件 **0 字节**。根因：`LocalBackend.model` 是**模型对象不是字符串**，最后一次 `json.dumps(整批)` 时炸掉，**前面所有答案全丢** | 两条一起改：① 显式判类型（云端是模型名 str、本地是对象）再落盘；② **改成边跑边写**（开文件 append + 每写完一条 `flush()`），单条序列化失败只跳过这一条。**任何长跑任务都必须增量落盘 —— 崩溃时你至少还剩前 90%**。顺带又一印证：**日志说成功 ≠ 结果存在**，看进度要看产物文件 |
| **AY** | **指标脚本盲信"别的模块落盘的标记"** | 落盘的 `refused` 是 `generator.py` 用自己的标记表写的。它漏一个等价说法（"没有关于…的内容"），评测侧就把一条**真拒答报成幻觉硬答** —— 而它落在"最危险的一类"那张表里 | 指标脚本**必须能独立复算**：从答案原文用自己的判据重判一遍，并统计两边不一致的条数（实测确实有 1~5 条不一致）。否则改一行标记表就会让历史指标漂移、前后不可比 |
| **AZ** | **"取不到数据"被当成"得零分"** | `hits` 字段只存元数据没存正文 → faithfulness 全 0、数字违规虚报 **126/175 句**，看起来"像个指标"，实际是判据瞎了 | ① 结果里**落盘原文**（`contexts`）；② 拿不到时报表打 `—` 而不是 0。**"缺失"和"零分"必须在报表上分开** —— 混在一起会静默假低，还把人引向错误的优化方向 |
| **BA** | **判官答了中文，指标把它静默踢出分母** | 判官偶尔不按 JSON 里的 `yes/no` 回，改用"有依据/无依据"。`judge_rate` 只认 `yes/no` → **20 个子句被丢**，其中一条正是最需要看的库外硬答题 | 加 verdict 归一化（`yes/是/有依据/支持` → yes，其余同理）。**指标脚本不能"看不懂就丢"** —— 丢的方向若随结果好坏分布，指标就不可比；语义明确时，归一化比丢弃更诚实 |
| **BB** | **报"引用准确率 94%"时不报"冤枉率"** | 6 条"引用错误"里，**3 条是同一件事被切在多个块里**（引的块正文里确实写着答案，只是 gold chunk 是另一块）、**1 条是评测集 gold 答案太粗**（问"何时拍的"答"1942年"而 gold 写"大屠杀期间"）→ 真正归模型的只有 **2 条** | 引用要**双口径并列**：严格（含 gold chunk）与宽松（gold 答案出现在被引原文里）。**只报严格口径会让人高估模型的错**；报数时同时说明"假错"的构成 |
| **BC** | **拒答被合成一个"越高越好"的数** | 重排关的库外拒答 92%→**100%** 看着更好，同期**库内误拒 0%→9.6%** —— 它是**拿误拒换来的**（检索变差，模型找不到依据就拒答） | 拒答必须**拆成两个方向**分别报：库外拒答率（越高越好）+ 库内误拒率（越低越好），且误拒还要**按"gold 有没有进上下文"分层**（gold 在里面 = 真误拒，是生成侧的锅；gold 没召回 = 检索失败的传导，拒答其实是克制）。**跨配置比比率之前先看分母**，否则结论会读反 |
| **BD** | **词面判据只有 61~72% 一致率却差点当主口径** | 2-gram / 子句 / 实词覆盖率三套判据对人工标签的一致率只有 **61~72%**，且对"**用词全对、关系说反**"（"A 是 B 的父亲" vs "B 是 A 的父亲"）**一个字都抓不出来** —— 而这正是危害最大的一类幻觉 | 主口径换成 **LLM 判官，且"先校准后上场"**（18 条人工标签，含 4 条**故意写错的合成对照**，达不到 85% 一致率不许跑全量）。⚠️ 判官自己也会错（第一版 prompt 把"答非所问"混进"无依据"，校准立刻掉到 61%）—— **工具本身要被验证过，它给的"通过"才是真的**；换模型必须重过校准 |

### 第 12 步 · 取原文延迟优化（BE ~ BL）

| 编号 | 坑 | 症状 | 根因 / 正解 |
|---|---|---|---|
| **BE** | **"按行号定位分片"只解决了一半** | 以为第 9 步的 `RowTextFetcher` 已经把取原文优化过了，实测仍是 **1.9 s** | 它只解决了"去哪个分片找"，**进了分片还是要 `isin` 全扫 552 MB**。要真快必须换存储布局（偏移数组 + mmap），见 BG |
| **BF** | **parquet 取 5 行和取 100 行一样慢** | "取 100 条要 1.7 s" 直觉上该怪"取太多" | 取行要先**解压整个 row group**（20.2 万行 / 179 MB）。实测 300 行 1307 ms vs 100 行 1716 ms —— **与条数无关**。所以"少取几条"是无效优化 |
| **BG** | **`chunk_id.isin(...)` 的谓词下推剪枝率是 0** | 以为 parquet 有行组统计就能跳过无关数据 | 行组的 min/max 统计对**随机哈希**完全无效（全域覆盖）。**有序键过滤才剪得掉** —— 这也是"为什么分区键不能选 uuid" |
| **BH** | **Windows：mmap 打开的文件不能被改名/删除** | 重建侧车时 `PermissionError`，报错信息完全看不出原因 | mmap 会持有文件句柄。**重建前先关 Gradio**。脚本已捕获并给出中文提示（"最常见原因：Gradio 还开着"） |
| **BI** | **诊断脚本第一版的工作量构造是错的** | "真实命中"批次量出来比均匀随机快近一倍，**冷热差是负数** | 把行号排序后切**连续段** → 那 100 行落在**同一分片**里（少开 3 个分片）。改成 `rng.choice(..., replace=False)` 随机抽样。**benchmark 的工作量构造错了，结论就是错的** —— 造假最常见的两种方式就是"分布不真实"和"缓存状态不声明" |
| **BJ** | **本机的冷/热对比不可靠** | 同一份工作"冷盘 907 ms < 热盘 1822 ms"，反直觉 | 后台进程干扰大。**别用墙钟归因**，改用 **CPU 时间 + 磁盘读字节**（`psutil`）：实测 `CPU 5578 ms（墙钟 325%，即并行解压吃 3.3 核）+ 磁盘读 2060 MB` —— 与缓存状态无关，能直接定性 |
| **BK** | **侧车是"一对文件"，替换不原子** | 内容与偏移两次 `replace` 之间有空隙 | 先写 `.tmp` + **行数契约**（侧车行数 == ids.txt 行数）+ **每次取回错位断言**（`payload["id"] == ids[row]`）。**残留风险要"响"，不能静默错位** |
| **BL** | **`get_fetcher` 改签名会连累 `evaluate.py`** | 只改了 `generator.py`，评测脚本一处调用点直接崩 | 全仓库搜调用方（`grep -rn "get_fetcher"`）。**改公共函数签名前先搜调用点**；顺带发现 gradio 的耗时条用**减法**算取原文（`total−vector−bm25−fuse`），一开重排就把重排算进去且看不出错 → 改成直接读 `timing['fetch']` |

---

# 卡住了怎么办

**先做这三件事，再找我：**

1. **复制完整报错**（从 `Traceback` 第一行到最后一行）
2. **说清你在哪一步**（比如"第 5 步，跑 build_pipeline.py"）
3. **说清你改过什么**（比如"我把 max_chars 改成 500 了"）

**最常见的三种"假故障"：**

- 跑很久没动静 → Spark 第一次启动要 30–60 秒，等着
- 满屏 WARN → 只要没有 ERROR 就继续
- 进度条卡住 → 可能在 shuffle 阶段，看下磁盘灯有没有在闪

---

# 现在的进度

```
[x] 方案设计        —— E:\AI-learning\RAG项目-技术方案.md
[x] 目录结构        —— E:\AI-learning\projects\rag-kb\
[x] 代码骨架        —— src\ 下 6 个脚本
[x] 依赖安装        —— 全部完成（3.13 的 RAG 依赖 + 3.11 的 pyspark 都已核对通过）
[x] PySpark 环境    —— 已实测跑通（Python 3.11 + pyspark 3.5.8 + Java 8）
[x] 多线程下载器    —— 已实测（4 并发 + 429 退避，18 分钟下完 3.2GB）
[x] 环境自检脚本    —— 已重写，Spark 冒烟测试通过
[x] 第 0 步 环境准备  —— 你已做（截图确认 JAVA_HOME 已设）
[x] 第 3 步 下载维基数据 —— **完成**，3,400,882,048 字节，与远端 Content-Length 精确一致
[x] 第 4 步 解析 XML → JSONL —— **完成**，1,507,969 条 / 9,385 MB / 601 秒 / 自检 100% 一致
[x] 第 5 步 Spark 清洗管道 —— **完成**，3,316,395 块 / 2.1 GB / 371.6 秒（清洗器修了**五**轮）
[x] 第 6 步 质量审计      —— **完成**，人工抽样 **50/50 = 100%**，六项审计表已进 `README.md`（见 6.3）
[x] 第 7 步 向量化        —— **完成 ✅ 2026-09-18 17:54**。331.6 万块**全部编码**：
                          `emb.npy` 6.79GB(fp16) + `ids.txt` 3,316,395 行（与 parquet 逐位相等）+ `meta.json`
                          本轮 5,979.4 秒 / **113.0 chunk/s**；自检范数 [0.9995,1.0005]、NaN=0、接缝自检通过
                          ⚠️ 被中断 **3 次**（2 次手动关机 + 1 次电池耗尽），全靠 `--resume` 救回；
                          判别法与体检工具见 §7.5
[x] 第 8 步 建索引 + 混合检索 —— **完成 ✅ 2026-09-18**
                          ① BM25 倒排：3,092,039 词 / **1.92 亿 postings** / 1219 MB / 19.9 分钟
                          ② faiss IVFFlat：13.6 GB，**nprobe=512 → recall 1.000 / 249 ms**
                          ③ 混合检索：向量 170~270ms + BM25 41~52ms + 融合 0.3ms ≈ **220~320 ms**
                          ④ 三模式对照：**1 胜 2 负**（k=60）；改 `--rrf-k 10` 后 **2/3**；
                             `--fuse gate` 3/3 但**样本仅 3 条，属过拟合，默认不开**
                          ⚠️ 详见 §8.4 —— **RRF 的 k=60 默认值在我们的数据上是错的**
[x] 第 9 步 生成与引用      —— **完成 ✅ 2026-09-19**
                          ① `src\generator.py`：三后端（echo/dashscope/local）+ prompt 三要素
                             + 引用核查（非法编号 / 无引用 / 拒答）
                          ② 库内 10 条：繁简修复后 **10/10 全部正确、0 条非法编号**；库外 3 条：**零幻觉**
                          ③ 本地 Qwen2.5-1.5B-Instruct 4bit：加载 26.3 s，显存仅 **1.07 GB**
                             ⚠️ 能答对、能拒答，但**标不稳引用编号**（M4 要量化的差距）
                          ④ `app\gradio_app.py`：答案 + 可折叠引用原文 + 耗时分解 + 「←被引用」标记
                          ⑤ BM25 索引**重建为归一化版**（`meta.zh_norm = "zh-cn"`）
                          ⚠️ 详见 §9.4 —— **繁体 query 的信息量在进入检索前就归零了**
[x] 第 10 步 评测集构建    —— **完成 ✅ 2026-09-20**
                          ① `src\build_testset.py`：采样 + LLM 生成 + 三条本地预筛 + 库外两层校验
                          ② `src\make_testset.py`：候选 → 正式评测集（129 条）
                          ③ 采样 165.8 万 chunk → 可用条目 **402,713**；字形交叉题占 **48.1%**
                          ④ **废题率 2.9%**（方案预估三成）→ 人工校对从"逐条"降级为"抽检"
                          ⚠️ 坑：列表型内容占扫描量 **15.8%**（section 名过滤挡不住）
                             ／「生于」把植物判成了人（应为「出生于」）
[x] 第 11 步 检索评测与消融 —— **完成 ✅ 2026-09-20，§11**
                          ① 主表（104 条库内题，strict，池 100）：

                          | 链路 | Recall@1 | Recall@5 | MRR@10 |
                          |---|---|---|---|
                          | 纯向量 | 0.481 | 0.683 | 0.568 |
                          | 纯 BM25 | 0.577 | 0.788 | 0.656 |
                          | 混合 RRF | 0.606 | 0.875 | 0.726 |
                          | **混合+重排** | **0.885** | **0.962** | **0.924** |

                          ② **reranker 是收益最大的一环**（R@1 +46%、MRR +27%），远大于换检索策略
                          ③ 字形归一化消融：跨字形子集 BM25 **0.800 → 0.300**；
                             「繁问简答」**0.833 → 0.000（全灭）** —— 第 9 步 bug 的精确复现
                          ④ 🔴 **匿名题型设计失败**：实测 BM25 0.812 vs 向量 0.375，
                             与"只有语义检索能答"的预期完全相反（§11.5）
                          ⑤ 延迟新瓶颈：**回查原文 1.96 s**，比向量检索慢 9 倍
```

## 已经替你验证过的事（不用你重复试）

| 验证项 | 结论 |
|---|---|
| PySpark 3.5.8 + Python 3.11 + Java 8 | **跑通**。`count()` / 聚合 / UDF 全部正常返回 |
| PySpark 3.5.8 + Python 3.13 | **必崩**。见坑 A，已写进脚本的主动拦截 |
| 本机 JAVA_HOME | 原来是空的；**你已设好**（截图确认 `C:\Program Files\Java\jdk1.8.0_171`） |
| 本机 Java 版本 | 只有 JDK 1.8.0_171 → pyspark 必须锁 3.5.x |
| 单连接 vs 多连接下载 | 单连接被限速 107 KB/s；4 并发足够（见下条） |
| **盲目 12 并发** | **会被 429 反滥用限流，203 片失败 107 片**。必须降到 4 并发 + `Retry-After` 全局退避 |
| 三个海外维基镜像 | **全废**（32 B/s ~ 8 KB/s），别浪费时间 |
| 维基 dump 完整性 | 3,400,882,048 字节 == 远端 `Content-Length`，`Last-Modified: 2026-09-01` |
| 向量化吞吐（实测，非估算） | bge-large-zh **71 chunk/s**、bge-small-zh **813 chunk/s**，**差 11.4 倍** |
| RAG 依赖 10 个包 | 全部 import 成功（含 torch CUDA 可用、4060 被正确识别） |
| 3.13 里的 pyspark | **已卸载**（3.12+ 跑不了，留着只会误导；顺带还回 C 盘 345MB） |
| **全量解析规模（实测，非估算）** | **1,507,969 条** / 正文 66.629 亿字符 / 9,385 MB JSONL / 601 秒 |
| 解析器自检 | `<text bytes="N">` 属性核对 **100% 一致**；line vs xml 双引擎 20,000 条逐条相同（SHA256 `5253dde4…`） |
| **样本外推规模** | **不可靠**。432MB 样本推 79 万，实测 150.8 万 —— 差 1.9 倍 |
| 抽样可复现性 | 同数据同 seed **必得同一批 50 条**（改脚本重跑，`chunk_id` 逐位一致）→ 人工判定结果不会因重跑失效 |
| **审计材料字段完整性** | v1 漏印 `section` → 2 条正常块被误判成「不自洽」。**材料缺字段，判据在原理上无法判对**（6.1.1） |
| **`HADOOP_HOME` 缺失的后果** | 不是"完全跑不了"，而是**能启动、一读 parquet 才抛 `UnsatisfiedLinkError`** —— 最容易误诊成数据坏了 |
| **续跑的正确性（真中断对照实验）** | 干净跑 3,000 条做基线，把 `ids.txt` 砍到 1,559 行 + `progress.json` 伪造成滞后值，再 `--resume` → **`ids.txt` 与 `emb.npy` 逐字节完全一致**。修法见 §7.5 |
| **续跑起点必须数 `ids.txt` 行数** | `progress.json` 每 10 万行才落盘。实测关机时它记 601,827、真值 689,687 —— 用它续跑会**静默错位 8.8 万行** |
| **长任务中断的真实原因判别** | 关机（事件 `1074 User32`）会杀进程且**不留 traceback**；睡眠（`42`/`107`）不会。判别命令见 §7.5 |
| **续跑有没有把数据写歪** | 76.4% 时体检：`2,534,401` 个 id **全部唯一、无一重复** → 上一轮续跑干净。工具 `src\check_vectors.py` |
| **体检脚本自己的口径也会错** | 初版把「未跑完」（`ids < emb`）误判成错位、把 `emb.npy` **预分配空洞**误判成 2,000 个零向量 —— **检测口径必须与事实对称**，同 §5.4 那条纪律 |
| **`emb.npy` 是预分配文件** | `open_memmap(w+)` 一开始就占满 6.79 GB，**未跑完时尾部是一片零**，不是坏数据。别拿文件大小判断完成度 |

**下一步动作**：**第 10 / 11 步（M4 评测与消融）已完成**，见本手册 §10/§11。
核心结论：**reranker 是收益最大的一环**（Recall@1 0.606 → 0.885，+46%）。

**融合/权重这条线已于 2026-09-21 探到底**（§11.11 加权扫描 + §11.12 查询路由）：

- 全局权重**救不了** —— 基础事实要「两路都要」，实体匿名要「只要 BM25」，需求相反
- 查询路由能提 R@1（0.606→0.644）且**零损失**，但 n=104 下 1σ≈0.048，提升 0.038 **未达显著**
- 原版 `--fuse gate`（弱×0.3）**实证负收益**（匿名子集 0.781→0.719），源码已标注「勿用」
- 🔴 **oracle 上界只有 +0.019**（逐题都选对权重也仅此）⇒ **不再投入**

**为什么这个结论值钱**：**reranker 一层值 +0.28 R@5，融合方式折腾三轮只值 ±0.02。**
力气必须花在收益量级更大的环节。

接下来按性价比**重新排序**：

① ~~**reranker 的池子大小与截断长度**~~ ✅ **已做完（2026-09-21，见 §11.13 与速查表 AR/AS/AT/AU/AW）**
   结论：`max_length=512` **不够**（20.2% 的 gold 被静默截断，恰好伤在最关键的 R@1 上）；
   重排候选 **100 → 50** 用新增的 `--rerank-pool`（**别去动 `--pool`**）省 27% 延迟。
   ⇒ **推荐配置：`--pool 100 --rerank-pool 50 --rerank-max-length 1024`**
   （R@1 +0.019 / R@5 +0.029 / MRR +0.014，端到端**还快 7.5%** —— 两个维度同时变好）
   ⚠️ **再想削延迟，该动的是「回查原文」那 ~1900 ms（占端到端一半），不是重排。**
② **生成侧指标**（半天）—— **新的第一顺位**：Faithfulness / 引用准确率 / 拒答准确率 + 误拒率。
   25 条库外题测拒答、104 条库内题测误拒，数据已就绪。
   ✅ `v1-d27c79` 的答案**已补全（2026-09-21，见 §11.10）** —— 可以开工了。
③ **chunk 大小 / 切块策略**（方案里的消融 ①②）：需要重新切块 + 重新向量化，
   **每次 1h40m**，3 组 = 7 小时机器时间。是独立大动作，单独安排。
④ **评测集 v2**：按 §11.5 的教训重建 `hard_anon`（不能只删专名，要**真正改写**）。
   同时修 `leak_ratio` 口径 —— 它只比 `chunk_text`、**没算 title**；
   再叠加 §11.10 抽检出的 3 条瑕疵题 + 1 条答案不完整题。
