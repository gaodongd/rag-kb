# 第 11 步 · M4 评测与消融（上）

> **这一步交付**：一个可复现的检索评测框架 + 第一张自己跑出来的消融对比表。
> 方案里说 M4 是"全项目最值钱的一段"—— 学校不教、简历上几乎没人写。

---

## 11.0 这一步要回答什么

上一轮为止，项目一直在**造东西**（切块、向量化、建索引、接生成）。
但"造出来的好不好"始终没有量化答案 —— 只有第 9 步那 3 条样本得出的印象。

这一步把它变成数字：**每条链路各差多少、差在哪一类问题上、值不值得上**。

---

## 11.1 现实性检查：方案里的 5 组消融，成本差一个数量级

方案列了 5 组消融。动手前先算成本 —— 这是**决定先做哪组**的依据：

| 组 | 变量 | 跑一次要做什么 | 成本 |
|---|---|---|---|
| ③ 检索模式 | vector / bm25 / hybrid | 用现成索引 | **几分钟** ✅ 本步完成 |
| ④ reranker | 开 / 关 | 用现成索引 | **几分钟** ✅ 本步完成 |
| ＋ 字形归一化 | 开 / 关 | 已有归一化前的旧索引（1.2 GB，当初特意留着） | **几分钟** ✅ 本步完成 |
| ⑤ 生成模型 | 云端 / 本地 1.5B | 调 104 次 LLM（有 API 成本） | 半小时 |
| ① chunk 大小 | 256 / 512 / 1024 | **重新切块 + 重新向量化 3 遍** | **每次 1h40m** ❌ |
| ② 切块策略 | 定长 vs 标题感知 | 同上 | **每次 1h40m** ❌ |

**① ② 是真·大工程**：3 组 chunk 大小 = 3 次全量编码（331.6 万块）+ 3 次 BM25 重建（各 23 分钟）
≈ **7 小时纯机器时间**，还没算调试。这两个是**独立的一次大动作**，不该和本步混在一起做。

**③ ④ 和字形归一化几乎是免费的**，而且方案自己说 reranker "贡献最大"——
先做"最便宜且最可能出结论"的，是唯一合理的顺序。

---

## 11.2 评测集定稿：129 条

从 140 条候选过滤到 **129 条**（库内 104 + 库外 25），剔除 11 条：

| 剔除原因 | 条数 | 说明 |
|---|---|---|
| 库内题本地预筛不过 | 4 | 2 条答案疑似概括 / 1 条改写不足 / 1 条匿名化失败 |
| 库外题"其实在库内" | 4 | 检索验证发现能答出来，做库外题是废题 |
| 库外题"库外性未判定" | 1 | 证据不足，不硬凑 |
| **跨字形题转换未生效** | 2 | 标为跨字形，实际问题与 gold 同字形 —— 测不出跨字形能力 |

最后那 2 条是**新逻辑顺带抓出来的**（见 11.4 的 bug ①）。

**题型分布**：基础事实 45 · 实体匿名 32 · 释义改写 14 · 简约繁答 7 · 繁问简答 6 · 库外 25
**字形交叉 50 条**（占库内题 48.1%）。

> 一个反直觉的数据：**43 条是"简问繁答"、只有 7 条是"繁问简答"**。
> 因为语料里繁体占约 59%，"从繁体条目出题"自然就落到简问繁答这一侧。
> 归一化的两个方向**自动被数据加权了**，这是采样时的客观结果，不是设计出来的。

---

## 11.3 指标口径：三个必须先说清楚的地方

不说清楚，数字会被误读。

### ① Recall@K 和 Hit Rate@K 在这里是同一个数

每题只有 1 个 gold chunk，所以 `Recall@K = 命中题数/总题数 = HitRate@K`。
两个都报是**为了对齐术语**（面试官可能问任一个），不是两个独立指标。
只有多跳问答（多个 gold）时二者才分开。

### ② 主指标用 strict（chunk 级），relaxed（条目级）只做诊断

| 口径 | 判定 | 用途 |
|---|---|---|
| **strict** | 检索结果里出现 **gold 那一条 chunk** | **主指标** |
| relaxed | 出现**同一个条目**（title 匹配）就算命中 | 诊断 |

为什么主指标必须用 strict：**标准答案是从 gold chunk 里提取的**，
同条目的**另一条** chunk 未必含答案 —— 拿 relaxed 当主指标会虚高。

relaxed 的价值在于**定位问题**：

| 现象 | 含义 | 该修什么 |
|---|---|---|
| strict 低 + relaxed 高 | 方向对了，但没精确定位到那一块 | **上 reranker** |
| 两个都低 | 检索本身就没找到 | 改召回（切块 / 索引 / 查询扩写） |

实测正好落在第一种：纯向量 strict R@5 = 0.683，relaxed R@5 = 0.721 ——
**同条目基本都能召回，差的是"块级定位"**。这正是 reranker 在这份数据上收益巨大的原因。

### ③ 三种检索模式只跑一遍

`HybridSearcher.search(mode="hybrid")` 的返回值里带 `per_path`，
**同时含向量路和 BM25 路各自的完整排名** —— 所以 vector / bm25 / hybrid 不用跑三遍
（省 3 倍时间），而且**保证三路用的是同一批查询向量**，对比才是干净的。

⚠️ 前提：`per_path` 的深度要够，池子设 100 时 `topn` 也必须 100。

---

## 11.4 抓到的 bug（4 个，都是真问题）

### bug ①：`t2s` / `s2t` 标签系统性贴反（最严重）

**现象**：导出的"繁问简答"8 条问题**全是简体**。

**根因**：三处不一致 —— 定义、prompt 指令、plan 配池子的注释各说各的：

| 位置 | 内容 | 实际语义 |
|---|---|---|
| 文件头定义 | `t2s` = 繁体提问 → 简体 gold | 问题繁体 |
| prompt（代码） | `"t2s": "原文是繁体，请用简体提问"` | 问题简体 ← **反了** |
| plan（代码） | `("t2s", c) for c in fanti` | gold 繁体 ← 和 prompt 一致 |

prompt 和 plan **互相自洽**，所以代码"跑得通、没人报错"；只有标签名和定义相反。
代价是**按 kind 分组的报告会把两个方向的行首尾颠倒**。

**修法（两层，缺一不可）**：
1. **改源码**：交换 prompt 和 plan 的键名，让实现符合 `t2s` 的字面含义（Traditional→Simplified）
2. **改导出层**：不信任上游 kind，用 `q_fanti` / `c_fanti`（**生成后实测的**）重新推导标签

第 2 层是关键 —— 它让**已有数据不必重新生成**，而且顺带抓出 2 条"字形转换根本没生效"的题
（标为跨字形，实际两侧同字形）。**用事实推导，比修补错误标签更可靠。**

### bug ②：修正逻辑插在了"剔除判定"之后

导出脚本里，"按实测字形修正标签"的代码被我放到了 `if reason: continue` **后面** ——
于是"两侧同字形"这条剔除规则永远不生效（那时已经过了 continue）。
把整段前移才修好。**排序敏感的逻辑，位置就是正确性的一部分。**

### bug ③：`qid` 用序号，一改排序全体漂移

原来 `qid = f"{tag}-{i:03d}"`，而 `i` 来自排序结果。
标签一修，排序变了，**所有 qid 全漂移** —— 人工黑名单和实验结果的对照全部失效。
改成 **问题文本的 md5 前 6 位**，与排序解耦。

### bug ④：reranker 的 tqdm 进度条污染日志

`FlagReranker.compute_score` 默认往 stdout 写 `\r` 进度条，把调用方的 print 搅成一团 ——
**实测导致我按 `^N/104` 提取进度的 grep 完全失效**，一度以为进程卡死。
加 `show_progress_bar=False`（带 TypeError 兜底，因为该参数名随 FlagEmbedding 版本变过）。

### 顺便纠正一次我自己的误判

我曾判断"5 条库外题的 `pass_local=False` 但 `warn=None` 是判据 bug"。
**查下来不是**：库外题的合格判据走的是 `answerable_in_kb` 字段（`--recheck` 里已按 kind 分支），
`warn` 对它本来就没意义。这 5 条正是"其实在库内 / 未判定"的废题，剔除是对的。
→ 教训：**看到可疑现象先读代码，别急着下"这是 bug"的结论。**

---

## 11.5 🔴 最重要的发现：匿名题型设计失败

这是本步最有价值的产出，**推翻了我自己在前一步写下的预期**。

### 预期 vs 实测

第 10 步我写下：

> `hard_anon` 是最有价值的一类……BM25 的倒排索引此时**毫无办法**，只有向量检索能靠语义召回。

实测（R@5, strict）：

| 题型 | 纯向量 | 纯 BM25 | 差 |
|---|---|---|---|
| 基础事实 | 0.822 | 0.778 | 向量略优 |
| **实体匿名** | **0.375** | **0.812** | **BM25 反超一倍多** |
| 释义改写 | 0.714 | 0.786 | BM25 略优 |

**方向和预期完全相反。**

### 根因：匿名 ≠ 低词面重合

我算了一条关键指标 —— **问题里的实词有多少逐字出现在 gold 原文里**：

| 题型 | 词面重合率（均值） |
|---|---|
| 基础事实 | **0.657** |
| **实体匿名** | **0.661** ← 和基础事实一样 |
| 释义改写 | 0.514 |

**匿名化根本没降低词面重合度。** 看实例（每条都真实存在）：

| 问题（节选） | 重合率 | 命中的实词 |
|---|---|---|
| 一位 1907 年**接受按立**并被派往**中国传教**的**英国圣公会神职人员**，1936 年后担任哪个**教区**的**主教** | 0.73 | 接受 按立 中国 传教 英国圣公会 担任 教区 主教 |
| 一座**市立博物馆**，位于**海滨**百道开发区中央，西边是**福冈塔**，北边是**福冈巨蛋**，还收藏着著名的**汉倭奴国王印** | 0.80 | 市立 博物馆 位于 海滨 开发区 中央 福冈 巨蛋 倭奴 国王 |
| 一种**株高不到 30 厘米**、叶子厚而**肉质**、花有**单瓣**和**复瓣**、**颜色多样**还分**单色复色**的植物 | 0.67 | 厘米 肉质 单瓣 复瓣 颜色 多样 单色 复色 植物 |

**"删掉实体名"只删掉了人名/条目名，而描述性词汇本来就逐字来自原文。**
更糟的是我给的约束是"禁止出现**原文里的**专有名词"—— 第三个例子里
"福冈塔""福冈巨蛋""倭奴国王印"确实**不在 gold 条目名里，但全都在 chunk 正文里**，
LLM 完全合规地绕了过去。

### 这带来一个更重要的结论：混合检索会被弱路拖累

| 题型 | 纯 BM25 | 混合 RRF | 混合 vs 单路 |
|---|---|---|---|
| 基础事实 | 0.778 | **0.956** | +0.178 ✅ |
| **实体匿名** | **0.812** | **0.781** | **−0.031** ❌ |
| 释义改写 | 0.786 | 0.786 | 0.000 |

**在匿名子集上，等权 RRF 融合反而比纯 BM25 差。**
原因清楚：向量路在这个子集上只有 0.375，它贡献的候选会挤进融合结果，**稀释掉 BM25 的好结果**。

⇒ **"混合检索值不值"没有全局答案，得看子集。**
整体 R@5 从 0.788 提到 0.875（+0.087）是真的，但增益**全部来自基础事实题**；
在两路能力差距大的子集上，等权融合是负收益。

**可做的改进**（留给下一轮）：加权 RRF 降低向量路权重，或按查询特征路由。

### 教训

**题型设计必须用数据校验，不能靠"看起来对"。**
"问题里没有专有名词 → BM25 无从下手"这个推理，在"描述词照样重合"的事实面前直接崩掉。
**如果我没算那个重合率，这份报告会得出一个完全错误的结论：**
"向量检索在语义题上更强，所以混合有价值" —— 而实测是相反的。

---

## 11.6 实测数据

评测集：`eval/qa_testset_v1.jsonl`（129 条，库内 104 条参与检索指标）
召回池 100 · faiss nprobe=512 · RRF k=10 · reranker = bge-reranker-v2-m3 (fp16)

### 主表（strict = 命中 gold chunk 本身）

| 链路 | Recall@1 | Recall@5 | Recall@10 | MRR@10 |
|---|---|---|---|---|
| 纯向量（bge-large-zh） | 0.481 | 0.683 | 0.760 | 0.568 |
| 纯 BM25 | 0.577 | 0.788 | 0.846 | 0.656 |
| 混合 RRF | 0.606 | 0.875 | 0.923 | 0.726 |
| **混合 + 重排** | **0.885** | **0.962** | **0.981** | **0.924** |

### 宽松口径（relaxed = 命中同一条目即可）

| 链路 | Recall@1 | Recall@5 | Recall@10 | MRR@10 |
|---|---|---|---|---|
| 纯向量 | 0.587 | 0.721 | 0.779 | 0.650 |
| 纯 BM25 | 0.683 | 0.827 | 0.885 | 0.740 |
| 混合 RRF | 0.721 | 0.875 | 0.942 | 0.792 |
| **混合 + 重排** | **0.952** | **0.962** | **0.990** | **0.960** |

### 分题型 · Recall@5（strict）

| 题型 | n | 纯向量 | 纯 BM25 | 混合 RRF | 混合+重排 |
|---|---|---|---|---|---|
| 基础事实 | 45 | 0.822 | 0.778 | 0.956 | **0.978** |
| 实体匿名 | 32 | 0.375 | **0.812** | 0.781 | 0.938 |
| 释义改写 | 14 | 0.714 | 0.786 | 0.786 | 0.929 |
| 简问繁答 | 7 | 1.000 | 0.714 | 1.000 | 1.000 |
| 繁问简答 | 6 | 0.833 | 0.833 | 0.833 | 1.000 |

### 字形分组 · Recall@5（strict）

| 字形 | n | 纯向量 | 纯 BM25 | 混合 RRF | 混合+重排 |
|---|---|---|---|---|---|
| 同字形 | 54 | 0.704 | 0.778 | 0.870 | 0.944 |
| 跨字形 | 50 | 0.660 | **0.800** | 0.880 | **0.980** |

**跨字形的 BM25 反而比同字形高（0.800 vs 0.778）** —— 这是归一化生效后的结果，
说明 `norm_text` 把两个字形**真正合并成了同一套词**。
归一化值多少，见 11.6b（关闭归一化的对照）。

### 11.6b 字形归一化消融（用未归一化的旧索引重跑同一套题）

这是**量化第 9 步那个修复值多少**的实验。做法：把 `--no-norm` 打开、
BM25 指向归一化前的旧索引（309 万词，比新索引多 117 万词），其余全不变。

**整体 Recall@5（strict）**

| 链路 | 归一化开 | 归一化关 | 差 |
|---|---|---|---|
| 纯向量 | 0.683 | 0.683 | **±0.000** |
| 纯 BM25 | 0.788 | 0.558 | **−0.231** |
| 混合 RRF | 0.875 | 0.750 | −0.125 |
| 混合+重排 | 0.962 | 0.933 | −0.029 |

**跨字形子集（n=50）—— 归一化真正作用的地方**

| 链路 | 归一化开 | 归一化关 | 差 |
|---|---|---|---|
| 纯向量 | 0.660 | **0.660** | **±0.000** ← 对照组 |
| 纯 BM25 | **0.800** | **0.300** | **−0.500** |
| 混合 RRF | 0.880 | 0.620 | −0.260 |
| 混合+重排 | 0.980 | 0.920 | −0.060 |

**同字形子集（n=54）—— 本该不受影响的对照组**

| 链路 | 归一化开 | 归一化关 | 差 |
|---|---|---|---|
| 纯向量 | 0.704 | 0.704 | ±0.000 |
| 纯 BM25 | 0.778 | 0.796 | +0.018（噪声） |
| 混合 RRF | 0.870 | 0.870 | ±0.000 |
| 混合+重排 | 0.944 | 0.944 | ±0.000 |

**按题型看 BM25（归一化开 → 关）**

| 题型 | n | 开 | 关 | 差 |
|---|---|---|---|---|
| 基础事实 | 45 | 0.778 | 0.689 | −0.089 |
| 实体匿名 | 32 | 0.812 | 0.562 | −0.250 |
| 释义改写 | 14 | 0.786 | 0.500 | −0.286 |
| **繁问简答** | 6 | **0.833** | **0.000** | **−0.833 ← 全灭** |
| 简问繁答 | 7 | 0.714 | 0.286 | −0.429 |

### 三件事同时成立，这个对照才算数

1. **向量路纹丝不动**（0.683 → 0.683，跨字形 0.660 → 0.660）——
   归一化只加在 BM25 侧，向量路本来就该完全不受影响。
   **它一分不差地没动，说明对照是干净的**（没有别的变量混进来）。
2. **同字形子集几乎不动**（BM25 ±0.018 属噪声）——
   简体问题查简体语料，归一化本来就无事可做。
3. **跨字形子集腰斩**（0.800 → 0.300），而且**「繁问简答」直接归零**。

第 3 条不是"变差了"，是**第 9 步那个 bug 的精确复现**：
繁体 query 经过 jieba 被切碎成单字 → 单字被 `_keep` 过滤 → **query 里什么都不剩 → BM25 无从检索**。
归一化关掉就等于回到修复前，那 6 条繁体问题必然全灭 —— 实测正是 `0.000`。

**收益量化**：
- BM25 在跨字形题上 **+50 个百分点**（0.300 → 0.800）
- 繁问简答从 **0.000 → 0.833**
- 代价：重建索引 23 分钟，词表从 309 万降到 192 万（**−38%**，索引还更小了）

> **有意思的副产品**：reranker 把损失兜回了大半（跨字形 0.920 vs 0.980，只差 6 个点）。
> 因为向量路不受归一化影响、仍能给出正确候选，reranker 在重排阶段把它捞了回来。
> **但这不能替代修 bug** —— 0.920 仍低于 0.980，而且 reranker 要付 1.6 秒/题的代价。

---

## 11.7 延迟剖析：最大瓶颈不是检索

| 环节 | 均值 | 中位 |
|---|---|---|
| 查询编码（bge-large-zh） | 250.5 ms | 50.5 ms（首题为冷启动） |
| 向量检索（faiss nprobe=512） | 221.6 ms | 220.5 ms |
| BM25 | 57.9 ms | 46.5 ms |
| RRF 融合 | 0.2 ms | 0.2 ms |
| **回查原文（取 top-100 正文）** | **1960.8 ms** | **1912.3 ms** |
| 重排（100 对） | 1640.1 ms | 1560.8 ms |

**最讽刺的一条：回查原文（1.96 s）比向量检索（0.22 s）慢 9 倍，也比重排慢。**
第 9 步已经用"按行号定位分片"把它从 2.9 s 降到 2.2 s，但**池子从 5 条涨到 100 条**后，
它又成了新的大头。

**可行的优化**（下一轮）：
- 评测时只有 rerank 那一层需要正文；**strict 口径只需要 chunk_id**（不需要取正文）
- 缓存 `title` 列（3.3M 行 ≈ 66 MB）可以让 relaxed 口径零成本
- 真正要落地的服务，应该把 top-100 缩到 top-20~30（重排收益在 100 和 30 之间差异很小，见下）

reranker 吞吐：**15.8 ms/对**（batch=32, fp16, RTX 4060 Laptop）· 10400 对共 164 s。

---

## 11.8 下一步

**本步完成的**：消融组 ③ ④ + 字形归一化。
**后续补做（2026-09-21）**：评测集独立抽检（§11.10）、加权 RRF（§11.11）、**查询路由（§11.12）**。

**还没做的，按性价比排序**：

1. ~~加权 RRF / 查询路由~~ ✅ **已做完（§11.11 + §11.12）**
   结论：全局权重救不了（两个子集需要相反配比）；查询路由能提 R@1/MRR 且零损失，
   但 oracle 上界只有 +0.019 —— **这条路已探到底，不再投入**。
   ⚠️ 遗留动作：`search_hybrid` 里原版 `--fuse gate`（弱×0.3）**实证为负收益**
   （匿名子集 0.781→0.719），应加注释标明"勿用"或直接删掉该分支。
2. ~~reranker 的池子大小与截断长度~~ ✅ **已做完（§11.13）**
   结论：`max_length=512` **不够**（20.2% 的 gold 被截，恰好伤在 R@1 上）→
   调到 **1024**：R@1 +0.019 / MRR +0.013，端到端只 +1.7%，且**无副作用**
   （未被截的 83 条输出逐位不变）。池子 **100 → 50** 白省 21% 重排延迟、指标不降。
   ⇒ **两个"白捡"的参数，已并入默认配置。**
   ⚠️ 遗留动作：若要严格区分"检索池"与"重排池"，需另加 `--rerank-pool`（§11.13 末尾）。
3. **生成侧指标**（半天）：Faithfulness / 引用准确率 / 拒答准确率 + 误拒率 —— **新的第一顺位**
   用 25 条库外题测拒答，104 条库内题测误拒 —— 数据已就绪。
   ✅ 评测集那 1 条答案不完整的题（`v1-d27c79`）**已修（2026-09-21，见 §11.10）**，
   可以开工了。
4. **chunk 大小 / 切块策略**（7 小时机器时间）：方案里的 ①②，独立大动作。
5. **评测集 v2**：按 11.5 的教训重建 `hard_anon` ——
   不能只删专名，要**真正改写**（同义替换 + 语序重组）。
   同时修 `leak_ratio` 的口径：它只比 `chunk_text`，**没算 title**，
   导致 12 条 easy 题的泄漏率被低估（含 1~2 条"这本书的评价怎么样"这类**指代不明的废题**）。
   **再叠加 §11.10 抽检出的 3 条瑕疵题 + 1 条答案不完整题**，一并修掉。

---

## 11.9 简历话术（用本步实测数字）

> 构建 129 条中文 RAG 评测集（含 25 条库外拒答题、50 条繁简跨字形题），
> 设计检索模式 / 重排开关 / 字形归一化三组消融，
> 定位出 **reranker 是收益最大的一环：Recall@1 从 0.606 提升到 0.885（+46%）、MRR@10 从 0.726 提升到 0.924**，
> 显著高于更换检索策略的收益；
> 并通过分题型分析发现**等权 RRF 在向量路表现差的子集上反而拖累召回**，
> 给出加权融合的改进方向。

**面试验收点**：
- 为什么主指标用 chunk 级 strict 而不是条目级 relaxed（11.3 ②）
- 为什么 reranker 收益比换检索模式大（11.3 ② 的 relaxed-strict 差 → 块级定位问题）
- 匿名题为什么失败、怎么改进（11.5）—— **这是最能体现"真的做过实验"的一题**
- 为什么三路检索只跑一遍还能保证可比（11.3 ③）

---

## 11.10 评测集独立抽检（2026-09-21）

### 为什么要做

11.6 报的「废题率 2.9%」是**生成侧自己算的** —— 自己证明自己不算验证。
新增 `src/audit_testset.py`（**只读，不删不改任何数据**）：
分层随机抽 29 条（seed=42 可复现）+ 自动检测分两级
（**报警** = 疑似废题 / **提示** = 不是缺陷但影响结果解读）。
产出 `eval/results/testset_audit_v1.md`。

### 人工裁决（逐条看"问题自然 / 答案在原文 / gold 正确"）

| qid | 题型 | 问题 | 裁决 |
|---|---|---|---|
| `v1-09f297` | 基础事实 | 「这本书的评价怎么样？」——**无任何锚点**，读者不知道指哪本 | **删** |
| `v1-3dccab` | 基础事实 | 「刘烨是哪个城市的？」——语病（应为"哪个城市的人"） | **改或删** |
| `v1-0470cb` | 实体匿名 | 「…**被部队打死**的15岁男孩，他和哥哥遭遇了什么类型的射击后**受伤**」——前后自相矛盾 | **删** |
| `v1-d27c79` | 实体匿名 | 问"**宽度和厚度各是多少**毫米"，答案只给了 `2.6毫米` 一个值 | ✅ **已修（2026-09-21）**，见下 |
| `v1-56ff20` | 基础事实 | 答案只有单字 `铁`（材质） | 保留，但生成侧判分要容忍单字答案 |

**29 条里 gold 全对，一条错配都没有。** —— 这是本次抽检最让人放心的一条：
说明"gold 定位"这条链路（采样 → 切片 → 记录 chunk_id）是可靠的，
瑕疵全部集中在**问题表述**上。

### ✅ 修复 `v1-d27c79`：字符覆盖率的盲区（2026-09-21）

**改了什么**（答案字段，`喙宽度和厚度各是多少毫米`）：

| | |
|---|---|
| 原文 | `喙宽度约2.6毫米，喙厚度约2.6毫米` |
| 旧答案 | `2.6毫米` ← 只答了一个量 |
| 新答案 | `喙宽度约2.6毫米，喙厚度约2.6毫米` |

**两个值恰好相同**，所以出题的 LLM 把它们合并成了一个 —— 这不是幻觉，是**漏答**。

**为什么原来的校验拦不住**：`answer_coverage`（字符覆盖率）问的是
「**答案里的字有多少能在原文找到**」，旧答案是 **1.0**
（「2.6毫米」确实在原文里，一个字不差）。
它查的是"每个字有没有出处"，**不查"该答的量有没有答全"** ——
这是判据的**能力边界**，不是阈值调得不对。**换个阈值永远抓不到它。**

**修了三处**（顺序重要）：

1. **源头数据**：`eval/qa_testset_candidates_v1.jsonl` —— 答案是**从这里流下去的**，
   `make_testset.py` 只做转抄。只改定稿的话，重跑一次就丢了。
2. **定稿 + 校对清单**：`eval/qa_testset_v1.jsonl`、`qa_testset_review_v1.md`
3. **校验逻辑**：给 `build_testset.py` 的 `check_item` 加第 4 条
   `multi_ask_one_answer()` —— 命中即 `pass_local=False`，
   下一批生成时这类题会被**自动剔除**，不用再靠人事后翻。

**改数据的安全性**：`evaluate.py` **从不读取 `answer` 字段**
（只用 question / gold_chunk_id / gold_title / kind / script_cross / leak_ratio），
结果 JSON 里也不存它 —— 所以这次修改**不影响已跑完的 15 份评测结果**，无需重跑。

⚠️ 但**绝不能顺手改 `question`**：题干参与检索，
一改所有历史 R@K 全部作废（qid 对得上、名次对不上）。
**改答案安全、改题干致命** —— 这是一条要记牢的边界。

#### 新判据的设计与校准（判据必须先校准，这是本项目第 4 次了）

判据：**题干有强多问标记 + 答案是单个数值 + 原文含 ≥2 个数值**，三条同时成立才报。

刻意**不用**「和／与／、」做标记 —— 实测误报太多，反例：
「一种体细长侧扁、银色体色带深色纵带…的鱼，它最长能长到多少厘米」里
既有「、」也有「多少」，但它只问一个量。

校准做了**四个方向**（只测一端等于没测）：

| 方向 | 样本 | 期望 | 实测 |
|---|---|---|---|
| ① 已知坏样本 | 旧答案 `2.6毫米` | 报警 | ✅ 报警（原文含 7 个数值）|
| ② 修复后样本 | 新答案 | 不报 | ✅ 不报 |
| ③ 已知易误报样本 | 3 条（含「、」「和」但只问一个量 / 列表型答案）| 不报 | ✅ 三条全不报 |
| ④ 全量回归 | 140 条候选 | 0 条 | ✅ 0 条 |

### ⚠️ 抽检自身的教训：判据又一次骗了我

我一开始把「easy 题 `leak_ratio == 0`」当成废题信号写了自动报警 —— **错的**。
`leak_ratio` 的真实定义是「**问题 4-gram 命中率**，只比 `chunk_text`」，
实测分布是：

| 题型 | n | 均值 | 中位 | min | max |
|---|---|---|---|---|---|
| 基础事实 | 45 | 0.260 | 0.250 | 0.000 | 0.667 |
| 实体匿名 | 32 | 0.244 | 0.255 | 0.000 | 0.529 |
| 释义改写 | 14 | 0.123 | 0.084 | 0.000 | 0.371 |
| 全部 | 104 | **0.240** | **0.236** | 0.000 | 0.842 |

104 题里有 **18 题就是 0.000**。零重合**不代表题目有问题**
（「康托尔用什么字母表示序数？」是道好题），它只说明**这题对 BM25 不友好** ——
这是解读检索指标时该知道的信息，不是缺陷。已把该判据从"报警"降级为"提示"。

**同时发现源码里有一句错的经验值**：`build_testset.py` 生成校对清单时，
把泄漏率注释写成了「easy 通常 0.6~0.9，匿名题应 <0.3」——
那是**动手前拍的猜测**，定稿实测中位才 0.25。已改为实测值 + 更正说明。

### 🔴 更值钱的发现：报告里有**两个互相打架的"词面重合率"**

| 出处 | 口径 | 基础事实 | 实体匿名 | 释义改写 |
|---|---|---|---|---|
| 11.5（本报告） | 实词重合（临时算的，**无脚本存档**） | 0.657 | 0.661 | 0.514 |
| 评测集 `leak_ratio` 字段 | 4-gram 命中，只比 chunk_text | 0.260 | 0.244 | 0.123 |

**绝对值差 2.5 倍。** 但关键在于——**三个相对关系在两个口径下完全一致**：

1. 基础事实 ≈ 实体匿名（0.657/0.661 ↔ 0.260/0.244）→ **匿名化确实没降低词面重合**
2. 释义改写最低（0.514 ↔ 0.123）→ 改写真做了

⇒ **11.5 的核心结论（匿名题型设计失败）对口径不敏感，是稳的。**
但报告里两个数并排放会让人以为矛盾，所以在这里明确标注口径差异。

**教训**：临时算出来、没存脚本的数字**不要写进正式结论**。
11.5 那个 0.657 现在**无法复现**（没有脚本、没有中间产物），
只能靠 `leak_ratio` 这个有定义、有脚本、可重算的指标来支撑同一结论。

### 处置决定（不删）
3 条瑕疵题**不删**，原因：① 删掉要作废已出的 4 组加权扫描 + 3 组基线（7 份结果）；
② 3/104 对指标的影响 < 3 个百分点，远小于结论的差异量级。
统一留到 **评测集 v2** 一起修（v2 本来就要重做 `hard_anon`，见 11.8 第 5 条）。

---

## 11.11 加权 RRF 扫描：等权到底该不该改（2026-09-21）

### 动机

11.5 发现「实体匿名」子集上等权混合（0.781）**低于**纯 BM25（0.812）。
最直接的怀疑：**弱路（向量在该子集只有 0.375）的候选稀释了强路结果**。
那就把向量路权重降下来试试。

### 做法

`evaluate.py` 原来把两路权重硬编码成 `1.0, 1.0`（`search_hybrid` 早就支持加权了）。
本次把它暴露成 `--rrf-weights`，跑 4 个比值。关重排（要量的是融合，不是重排）。

**先看对照组合法性**：向量路 / BM25 路**不依赖融合权重**，
所以四组跑出来的单路指标必须**逐位相同** —— ✅ 实测完全相同。
（脚本里把这个校验写成断言，不一致直接退出，防止变量没隔离干净。）

### 结果：没有任何一个全局权重能同时满足两个子集

**整体（strict，n=104）**

| w_vec : w_bm25 | R@1 | R@5 | R@10 | MRR@10 |
|---|---|---|---|---|
| **1 : 1（等权）** | **0.606** | **0.875** | **0.923** | **0.726** |
| 1 : 2 | 0.606 | 0.817 | 0.904 | 0.709 |
| 1 : 3 | 0.587 | 0.798 | 0.875 | 0.689 |
| 2 : 1 | 0.587 | 0.769 | 0.798 | 0.662 |

→ **等权就是整体最优。** 动任何一个方向都掉分。

**分题型 R@5（strict）—— 真相在这里**

| 题型 | n | 1:1 | 1:2 | 1:3 | 2:1 | 纯向量 | 纯 BM25 |
|---|---|---|---|---|---|---|---|
| 基础事实 | 45 | **0.956** | 0.844 | 0.822 | 0.911 | 0.822 | 0.778 |
| **实体匿名** | 32 | 0.781 | **0.812** | 0.781 | 0.531 | 0.375 | 0.812 |
| 释义改写 | 14 | 0.786 | 0.786 | 0.786 | 0.714 | 0.714 | 0.786 |
| 简问繁答 | 7 | 1.000 | 0.714 | 0.714 | 1.000 | 1.000 | 0.714 |

**1:2 确实把匿名子集从 0.781 救回到 0.812（正好追平纯 BM25）** —— 假设成立，
弱路确实在稀释。**但代价是基础事实题从 0.956 掉到 0.844（−0.112）**，
一进一出，整体反而从 0.875 掉到 0.817。

| 子集 | 想要的方向 |
|---|---|
| 基础事实 | 两路**都**要（向量的语义召回有用，1:1 甚至好于任一单路） |
| 实体匿名 | **只要 BM25**（向量在这里是纯噪声） |

### ✅ 结论：**这不是"权重没调好"，是"全局权重本身不成立"**

两个子集需要**相反**的权重配比 ⇒ 任何单一全局权重都是妥协，
最优妥协就是等权（因为它至少不偏向任何一方）。

**所以 11.5 那个"混合负收益"的正确解法不是调权重，而是查询级路由。**
这条从数据里长出来的结论，比"调参调到 0.88"有价值得多 ——
它把问题从「参数问题」重新定义成了「架构问题」。

### 附带两个观察

**① 逐题看：混合几乎不会输给两路（胜在"兜底"）**

| 权重 | 融合比纯BM25差 | 融合比纯向量差 | 两路都赢 | **两路都输** |
|---|---|---|---|---|
| 1:1 | 16 | 11 | 11 | **0** |
| 1:2 | 9 | 18 | 9 | **0** |
| 1:3 | 7 | 22 | 7 | **0** |
| 2:1 | 22 | 5 | 7 | **0** |

「两路都输」**恒为 0** —— 任何权重下，融合结果都不会同时差于两个单路。
这就是 RRF 的价值：它主要不是"提上限"，而是**降低单路翻车的风险**。

**② 降权向量的收益是非单调的**：1:2 救回 0.812，1:3 又掉回 0.781。
说明 1:2 附近那个"正好把向量噪声压到不影响 BM25 头部"的位置很窄，
**靠调参撞上它不可靠** —— 又一个该做路由而不是调参的理由。

### 复现命令

```bat
REM 基线（等权）
"C:\...\Python313\python.exe" src\evaluate.py --tag wrrf_1_1 --rrf-weights 1.0 1.0
REM 降权向量
"C:\...\Python313\python.exe" src\evaluate.py --tag wrrf_1_2 --rrf-weights 1.0 2.0
REM 出表
"C:\...\Python313\python.exe" src\report_wrrf.py
```
四组共 ~18 分钟（每组 ~4.5 分钟，含索引加载）。

---

## 11.12 查询路由：探到底了，该换方向（2026-09-21）

### 动机（承接 11.11）

11.11 证明全局权重救不了 —— 两个子集需要**相反**的配比。
那就逐题判定：**这题该信 BM25 还是该信向量？**

### 选信号：BM25 top1 领先次名的幅度

`iter` 已有的 `gate` 机制本来就在算这个量（只是拿去降 BM25 权重了）。
本次把它**反向用**：领先幅度大 ⇒ 这题 BM25 捞到了强匹配 ⇒ **加权**。
改动很小：`search_hybrid` 加一个 `gate_strong_w`（默认 1.0 = 不加权，保持原行为），
并把这个信号记进评测的 per_query（`bm25_lead`）以便事后分析。

### 结果

**总览（strict，n=104）**

| 方案 | R@1 | R@5 | MRR@10 |
|---|---|---|---|
| 等权基线（1:1） | 0.606 | **0.875** | 0.726 |
| 全局 1:2 | 0.606 | 0.817 | 0.709 |
| 路由 · 门限 10%（强×2.0） | 0.635 | 0.856 | 0.740 |
| **路由 · 门限 30%（强×2.0）** | **0.644** | **0.875** | **0.753** |
| 原版 gate · 弱×0.3 | 0.577 | 0.846 | 0.689 |

**分题型**

| 方案 | 基础事实(45) | 实体匿名(32) | 释义改写(14) |
|---|---|---|---|
| 等权基线 | 0.956 | 0.781 | 0.786 |
| 纯 BM25（参考） | 0.778 | **0.812** | 0.786 |
| 全局 1:2 | 0.844 | **0.812** | 0.786 |
| **路由 · 门限 30%** | 0.956 | 0.781 | 0.786 |
| 原版 gate · 弱×0.3 | 0.933 | 0.719 ❌ | 0.714 ❌ |

### 🔴 结论一：**原版 gate 的方向本身就是错的**

`search_hybrid` 里那个 `--fuse gate`（BM25 领先不足就 **降**它权重）在这里是**负收益**：
匿名子集 0.781 → **0.719**，释义改写 0.786 → **0.714**，整体 0.875 → 0.846。

原因很清楚：**匿名子集恰恰是 BM25 最强的子集**（向量只有 0.375）。
你却在这个子集上给它降权 —— 越降越差。
**这个分支不该再用**（默认没开，但代码和文档里留着容易误用）。

### ✅ 结论二：反向路由是**唯一纯增益**的方案，但幅度未证实

门限 30% 那组：R@1 从 0.606 提到 0.644、MRR 从 0.726 提到 0.753，
**且 R@5 一点没掉（0.875 → 0.875）、基础事实一题没伤（0.956 → 0.956）**。

配对检验（McNemar，只看两边不一致的题）：

| 对比 | 名次 | 基线独中 | 新独中 | p | 判读 |
|---|---|---|---|---|---|
| 门限 30% vs 等权 | R@1 | **0** | **4** | 0.125 | **纯增益，零损失**，但未过 0.05 |
| 门限 30% vs 等权 | R@5 | 0 | 0 | 1.000 | 完全没动 |
| 门限 10% vs 等权 | R@1 | 2 | 5 | 0.453 | 有得有失 |
| 原版 gate vs 等权 | R@1 | **4** | 1 | 0.375 | **反向**（净损失） |

**n=104 时 R@1 的 1 个标准差 ≈ 0.048**，而实测提升只有 0.038 ——
⇒ **只能说"方向对、零损失"，不能宣称"显著有效"。**
（R@5 一模一样的 0:0 说明：它只重排了前 5 名**内部**，没改变 top-5 集合。）

### 🛑 结论三（最重要）：**oracle 上界只有 +0.019，这条路到头了**

给一个**完美路由**的上界：既然每题在两个权重下的 hybrid 排名都记在 JSON 里，
就能算「如果每题都选对权重，指标最高能到多少」= 逐题取更优排名。

| 口径 | 等权 | 全局 1:2 | **oracle** | 空间 |
|---|---|---|---|---|
| 全部 104 题 | 0.875 | 0.817 | **0.894** | **+0.019** |
| 仅基础事实 | 0.956 | 0.844 | 0.978 | +0.022 |
| 仅实体匿名 | 0.781 | 0.812 | 0.812 | +0.031 |

**即使每道题都能预知该用哪个权重（现实中做不到），整体 R@5 也只多 1.9 个百分点。**

原因在信号本身：它**有**区分度但不够用 ——

| 组 | n | 领先幅度中位 |
|---|---|---|
| BM25 更好 | 36 | 17.7% |
| 向量更好 | 27 | **5.5%** |
| 打平 | 41 | 26.3%（最高） |

（中位差 +12.2%，方向对；但"打平"那组的领先幅度反而最高，说明这个信号混了别的东西。）

### 📌 这一步的产出：**知道该停手了**

| 问题 | 答案 |
|---|---|
| 混合检索值不值？ | 分情况。整体正收益，匿名子集负收益（11.5） |
| 调权重能修好吗？ | **不能**。两个子集需要相反配比，等权已是最优妥协（11.11） |
| 查询路由能修好吗？ | 能提 R@1/MRR 且零损失，但**没救回匿名子集**，且幅度在噪声内（本节） |
| 那还值得继续投入吗？ | **不值得**。oracle 上界只有 +0.019 |

⇒ **融合/权重这条线已经探到底。** 真正的收益在别处（见 11.8 重新排序后的清单）：
**reranker 的池子与截断长度**、**生成侧指标**，以及代价最大的**重新切块**。
这正是 reranker 那组消融早就在说的事 —— 它一层就带来 +0.28 R@5，
而融合方式折腾半天只值 ±0.02。**该把力气花在收益量级更大的环节上。**

### 复现命令

```bat
REM 反向路由（门限 30%，唯一纯增益）
"C:\...\Python313\python.exe" src\evaluate.py --tag route_s2_m30 --fuse gate ^
    --gate-weak-w 1.0 --gate-strong-w 2.0 --gate-margin 0.30
REM 原版 gate 对照（弱分支，实证为负收益）
"C:\...\Python313\python.exe" src\evaluate.py --tag route_orig_gate --fuse gate ^
    --gate-weak-w 0.3 --gate-strong-w 1.0 --gate-margin 0.10
REM 出表（含 oracle 上界与 McNemar 配对检验）
"C:\...\Python313\python.exe" src\report_routing.py
```
三组共 ~18 分钟 · 报表见 `eval/results/routing_tables.md`

---

## 11.13 reranker 的截断长度与召回池：找到两个"白捡"的参数（2026-09-21）

承接 11.8 重新排序后的第 1 项。这一节证明：
**在收益量级最大的那个环节（重排）上，还有两个几乎零成本的改进被漏掉了。**

### 动机：一个从没验证过的默认值

`Reranker(max_length=512)` 是抄示例代码抄来的，从第 10 步到现在一直是 512。
而 `max_length` 管的是 **(query, passage) 对**的总长上限，不是 passage 单独的长度：

```
可用给 passage 的 token = max_length − len(query) − 4
```

### 第一步：先做**零成本静态诊断**，再决定值不值得跑实验

新增 `src/diagnose_rerank_trunc.py` —— 只加载 tokenizer，不加载模型、不加载索引，
**约 20 秒跑完**。先量清楚三件事再决定投入：

| 量 | 结果 |
|---|---|
| passage token 长度 | 均值 300 · 中位 243 · p90 544 · 最大 648 |
| query token 长度 | 均值 22 · 中位 17 · 最大 66 |
| **字符/token 比** | **1.379**（不是"中文 1 字 1 token"！） |
| 512 下截断 | **21 / 104 条 = 20.2%** |
| 1024 下截断 | **0 条** |

⇒ 512 token 只够约 370 字的 passage，而 gold passage 均值 413 字。

**诊断给出的关键判据**（交叉历史评测结果的 per_query 排名）：

| max_length=512 | n | R@1 | R@5 | MRR |
|---|---|---|---|---|
| gold **被截断** | 21 | **0.762** | 0.952 | 0.859 |
| gold 未截断 | 83 | **0.916** | 0.964 | 0.941 |

**R@1 差 15.4 个点，R@5 只差 1.1 个点** —— 截断伤的是"排第一"，不是"进前五"。
（容易理解：gold 通常仍在池子里，重排即使没看到尾巴，往往还能排进前 5；
但要在第一名上压过其他候选，缺了关键信息就不行。）
而本项目的主指标正是 R@1 / MRR ⇒ **值得跑**。

### 🔧 顺带修掉一个"差 1 个 token"的估算错误

第一版诊断用 `qn + dn + 3`（照 BERT 的 `[CLS] q [SEP] d [SEP]` 记的）。
实测反推发现**真实值是 +4**：

```
qn=15, dn=488 → 估算 506，实际 tokenizer(q, d, truncation=False) = 507
```

原因：bge-reranker-v2-m3 用的是 **XLMRobertaTokenizer**，模板是
`<s> q </s></s> passage </s>` —— **双 `</s>`**，特殊 token 是 4 个不是 3 个。

差 1 看着无所谓，但它会让"恰好卡在边界上"的那一条题被误判为未截断。
**教训：拼接长度的常数不能照抄模型家族，必须用
`tokenizer(q, d, truncation=False)` 反推一次。** 本项目 104 条里恰好没有落在
那个临界点上（修正前后都是 21 条），但这是运气，不是设计。

### 结果一：max_length 512 → 1024（pool 固定 100）

**先做复现校验**：`rerank_ml512` 重跑 vs 历史 `v1_final` ——
**104 题逐题名次 0 处不同，三个指标一位不差**。
说明新增的诊断代码是纯只读的，没碰任何链路，后续对比干净可信。

| 口径 | R@1 | R@5 | MRR@10 |
|---|---|---|---|
| ml=512 | 0.8846 | 0.9615 | 0.9242 |
| **ml=1024** | **0.9038** | **0.9712** | **0.9370** |
| Δ | **+0.0192** | **+0.0096** | **+0.0128** |

分题型 R@1：

| 题型 | n | ml512 | ml1024 | Δ |
|---|---|---|---|---|
| 基础事实 | 45 | 0.867 | 0.867 | +0.000 |
| 实体匿名 | 32 | 0.938 | **0.969** | **+0.031** |
| 释义改写 | 14 | 0.786 | **0.857** | **+0.071** |
| 繁问 | 6 | 0.833 | 0.833 | +0.000 |
| 简问 | 7 | 1.000 | 1.000 | +0.000 |

✔ **受益的是"需要读全文"的题**（释义改写、实体匿名）；
基础事实题靠词面匹配就够，重排没看全也能排对 —— 与诊断的预测一致。

### 🎯 结果二：机制归因 —— 收益 **100% 来自被截断的那 21 条**

这是本节最重要的一张表。按 **ml=512 时** gold 是否被截断分组（跨文件按 qid 对齐）：

| 分组 | n | ml512 R@1 | ml1024 R@1 | ΔR@1 | ml512 R@5 | ml1024 R@5 |
|---|---|---|---|---|---|---|
| gold **被截断** | 21 | 0.762 | 0.857 | **+0.095** | 0.952 | **1.000** |
| gold 完整 | 83 | 0.916 | 0.916 | **±0.000** | 0.964 | **0.964** |

被截组 R@5 做到 **1.000**（21/21 全部进前五），MRR +0.064。

> ⚠️ **别把"完整组零变化"读成统计证据 —— 它是恒等式，不是发现。**
> 对未被截断的题，1024 与 512 喂给模型的 token 序列**完全相同** ⇒ 输出必然逐位相同。
> 它的价值在于证明**改动的隔离性**：只影响被截的题，其他题的分数一位都没动，
> 不存在"换个超参碰巧整体好一点"的全局扰动。
> 换句话说：**这组实验的因果链是闭合的**，不是靠统计显著性撑的。

逐题迁移（ml512 → 1024，严格口径）：

| 方向 | 条数 | 明细 |
|---|---|---|
| 改善 | 2（**都是被截的题**） | `v1-c57ba9` 2→1（释义改写）· `v1-171770` 21→1（实体匿名） |
| 变差 | 1（也是被截的题） | `v1-f0d458` 2→3（基础事实，仍在 top-5 内） |

配对符号检验 2 vs 1，p=0.500 —— **样本量太小，判不了显著**。
但这里不需要显著性：机制已由"截断/完整"分组切开，效果方向明确、隔离干净。

**成本**：`ms/对` 15.85 → 17.79（+12%），重排均值 1642 → 1830 ms，
端到端 4100 → 4176 ms（**+1.7%**）。⇒ **这是纯赚的参数。**

### 结果三：召回池 100 → 50 → 30（max_length 固定 1024）

| pool | R@1 | R@5 | MRR@10 | 重排均值 | ms/对 | rerank 总耗时 |
|---|---|---|---|---|---|---|
| 100 | 0.9038 | 0.9712 | 0.9370 | 1830 ms | 17.79 | 185.0 s |
| **50** | 0.9038 | **0.9808** | **0.9378** | **1437 ms** | 27.64 | 143.7 s |
| 30 | 0.8942 | 0.9712 | 0.9266 | **1164 ms** | 36.97 | 115.3 s |

- **pool=50 是甜点**：指标持平（R@5 还 +1 条），重排延迟 **−21%**。
- **pool=30 开始掉**：R@1 −0.0096（1 条题），延迟再省 667 ms。
- 注意 `ms/对` **反而随池子缩小而升高**（17.79 → 36.97）—— 这不是变慢：
  它是"总时间 ÷ 对数"，池子小了对数少，但每个 batch 的 padding 开销
  （按 batch 内最长序列算）不按比例减少。**看延迟要看绝对 ms，别看单位成本。**

> ⚠️ **实验设计的耦合（必须写明）**：`--pool` 同时控两件事 ——
> ① 各路召回池大小、② 喂给重排的候选数。所以这里量到的不是"纯重排池缩小"。
> 影响有限：本表指标 K≤10，池子 ≥10 时召回指标不受池子大小影响；
> 但 RRF 融合结果的前 K 名在两路各取 30 vs 100 时会有微小差异
> （逐题比对：pool 100 vs 50 有 2 条名次不同）。
> **要严格解耦需另加 `--rerank-pool` 参数**（检索池固定、只缩小重排输入）。

### 🔭 顺带发现：一个**查询侧可算**的质量信号

池子里被截断的候选越多，重排质量越差 —— 这个信号**不需要 gold 就能算**：

| 池中被截候选数 | n | R@1 | MRR |
|---|---|---|---|
| 0 条 | 10 | **1.000** | 1.000 |
| 1–9 条 | 71 | 0.901 | 0.931 |
| 10–29 条 | 23 | **0.783** | 0.870 |

⇒ 生产环境可以统计每题的池内截断率，**持续偏高说明 chunk 切得太长** ——
这是一个能直接指向"切块策略"的监控指标，而不是只能干看着的日志。

### 结果四：解耦验证 —— 上面那个"耦合"值得花 7 分钟解开

上一节的池子实验有个设计缺陷：`--pool` 同时控"各路召回池"和"重排候选数"，
所以量到的不是纯重排效果。既然如此，就把它解开：
**新增 `--rerank-pool`**（默认 0 = 跟随 `--pool`，保持旧行为可复现），
然后固定检索池 100、只降重排候选，补跑一组验证。

| 检索池 | 重排候选 | R@1 | R@5 | MRR@10 | 重排均值 | rerank 总耗时 |
|---|---|---|---|---|---|---|
| 100 | 100（原默认） | 0.9038 | 0.9712 | 0.9370 | 1830 ms | 185.0 s |
| 50 | 50（**耦合**着降） | 0.9038 | 0.9808 | 0.9378 | 1437 ms | 143.7 s |
| **100** | **50（解耦）** | **0.9038** | **0.9904** | **0.9381** | **1344 ms** | **134.0 s** |

**解耦组两个维度同时变好**：R@5 **0.9904**（104 题只差 1 条）、
重排 **1344 ms**（比原默认快 27%）、端到端 396.0 s（比原默认**快 9.4%**）。

原因很清楚：**两个参数各管一件事，就该各自取最优** ——

- 检索池保持 100：RRF 的融合输入不缩水 ⇒ 召回质量不降
- 重排候选降到 50：只砍掉排在最后、本来也进不了 top-10 的候选 ⇒ 纯省时间

耦合着降（直接 `--pool 50`）会**连带缩小 RRF 的融合输入**，所以反而少 2 条 R@5。
**这是"两个变量被一个开关绑在一起"的典型代价** —— 而解开它只花了 7 分钟。

### 📌 结论（含推荐配置）

| 问题 | 答案 |
|---|---|
| `max_length=512` 够吗？ | **不够**。20.2% 的 gold 被截，恰好伤在最关键的 R@1 上 |
| 调大值得吗？ | **值得**。R@1 +0.019 / MRR +0.013，且**无副作用** |
| 池子 100 合理吗？ | **作为重排输入是浪费**。50 就够，白省 27% 重排延迟 |
| 那 `--pool` 也降到 50？ | 🚫 **不要**。`--pool` 还管 RRF 的融合输入，降了要掉 2 条 R@5 —— 该用 `--rerank-pool` 单独降 |
| 为什么现在才发现？ | 因为它不报错。截断是**静默**的 —— 反过来说，**任何"上限类"参数都该被如此检查** |

**推荐配置（本项目新默认）**：

```bat
--pool 100 --rerank-pool 50 --rerank-max-length 1024
REM 相对旧配置（pool=100, 候选=100, ml=512）：
REM   R@1  0.8846 → 0.9038 (+0.019)
REM   R@5  0.9615 → 0.9904 (+0.029)
REM   MRR  0.9242 → 0.9381 (+0.014)
REM   端到端 4100 ms → 3792 ms（快 7.5%）  ← 精度和延迟同时改善
```

**方法论产出（本节最可复用的部分）**：
1. **"上限类"参数先做零成本静态诊断** —— 用 tokenizer/长度分布量出"有多少样本越界"，
   成本 20 秒，却能决定要不要花 40 分钟跑实验。本节的判据（R@1 差 15 点 vs R@5 差 1 点）
   还顺带预测了收益会落在哪些题型上，事后完全吻合。
2. **归因要靠切分，不能靠均值** —— "整体涨了 0.019"不是结论，
   "涨的全部来自被截的 21 条、其余 83 条一位没动"才是结论。
3. **验证工具本身**：诊断的拼接常数差 1 个 token（+3 vs +4），
   靠 `tokenizer(q, d, truncation=False)` 反推才抓到。
   检查工具写错时，它给的"通过"是假的。
4. **一个开关绑住两件事时，先把它解开再下结论** ——
   `--pool` 既管召回池又管重排候选，耦合着调只能得到"两者的混合效果"；
   加个 `--rerank-pool` 花 7 分钟，结论就从"降池子会微升"变成
   **"两个参数各自取最优 ⇒ R@5 再 +2 条、延迟再 -27%"**。
   （成本极低、收益极高，属于**最该优先做的那类改动**。）

### 复现命令

```bat
REM ① 零成本诊断（20 秒，先跑这个再决定要不要做实验）
"C:\...\Python313\python.exe" src\diagnose_rerank_trunc.py ^
    --dump eval\results\trunc_gold_ml512.json

REM ② 四组对照（约 28 分钟，串行以免两份索引同时驻留内存）
"C:\...\Python313\python.exe" src\evaluate.py --tag rerank_ml512  --rerank --rerank-max-length 512  --pool 100
"C:\...\Python313\python.exe" src\evaluate.py --tag rerank_ml1024 --rerank --rerank-max-length 1024 --pool 100
"C:\...\Python313\python.exe" src\evaluate.py --tag rerank_p50_ml1024 --rerank --rerank-max-length 1024 --pool 50
"C:\...\Python313\python.exe" src\evaluate.py --tag rerank_p30_ml1024 --rerank --rerank-max-length 1024 --pool 30

REM ③ 解耦验证（检索池固定 100，只把重排候选降到 50）—— 推荐配置就是这一组，约 7 分钟
"C:\...\Python313\python.exe" src\evaluate.py --tag rerank_rp50_ml1024 --rerank --rerank-pool 50

REM ④ 出表（含复现校验、机制归因、解耦对比、配对符号检验）
"C:\...\Python313\python.exe" src\report_rerank.py
```

报表：`eval/results/rerank_tables.md` · 截断标记：`eval/results/trunc_gold_ml512.json`

⚠️ 跑之前**先关掉 Gradio**（另一个常驻进程也吃 13.6 GB 索引内存）。

