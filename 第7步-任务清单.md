# 第 7 步 · 向量化 任务清单

> 规格来源：`操作手册.md`（第 7 步）+ `E:\AI-learning\RAG项目-技术方案.md`（M2 阶段）
> 原则：**只做规格里写了的事**。混合检索、rerank、向量库选型属于第 8 步，本步不碰。

## 规格摘要（直接引用，不加工）

**关键约束（手册第 5 步末尾原话）**：

> "**必须在第 7 步向量化时把 section 拼进待编码文本**，否则白白浪费了这个字段。"

**技术栈**：Python 3.13 / torch 2.14.0+cu126 / FlagEmbedding / **pyarrow**（读 parquet）/ numpy
**模型**：`bge-large-zh-v1.5`（本地 ModelScope 缓存，1024 维，fp16）
**输入**：`data/processed/chunks.parquet` — **3,316,395 块**
**输出**：`data/index/bge-large-zh-v1.5/` — `emb.npy` + `ids.txt` + `meta.json`

## 任务清单

### [x] T1 · 环境与输入确认

**做什么**：确认这一步能用哪个解释器、能不能绕开 Spark、模型和显存是否就位。

**实测结论**：

| 项 | 结果 |
|---|---|
| pyarrow 读 Spark 输出的 parquet | ✅ `pyarrow.dataset` 直读目录，3,316,395 行 |
| 是否需要 Python 3.11 / Spark / winutils | ❌ **都不需要**。第 7 步全程只用 3.13 |
| 模型 | `ms-cache\models\...bge-large-zh-v1.5\snapshots\master`（完整） |
| 维度 / 归一化 | 1024 维，FlagModel 输出**已 L2 归一化**（内积=余弦） |
| CUDA | ✅ True（RTX 4060 Laptop 8GB） |

**验收**：四项全部确认，无一项需要安装。

---

### [x] T2 · 编写 `src/vectorize.py`

**做什么**：流式把 chunk 编码成向量，产物可回查、可续跑。

**必须实现的点**（少一个都算没做完）：

| # | 要求 | 理由 |
|---|---|---|
| 1 | 待编码文本 = `title｜section｜chunk_text` | 手册硬性要求；section 补父标题上下文 |
| 2 | `section` 空时不产生空段 | 21.5% 的块 section 为空，否则拼出 `title｜｜text` |
| 3 | memmap 预分配，不整表进内存 | 全量 fp16 = **6.8 GB**，进内存必炸 |
| 4 | `--limit` 小样本模式 | 先验通再全量，别一上来烧几小时 |
| 5 | `--resume` 断点续跑 | 数小时长任务，中断即全废没有第二个选项 |
| 6 | token 长度 / 截断率统计 | 见下方风险 R1 |
| 7 | 跑完自检范数 + NaN | 下游检索报错才发现就晚了 |

**验收**：小样本命令跑通并产出四件套。

---

### [x] T3 · 小样本验通（5,000 块）—— **通过**

**命令**：

```cmd
cd /d E:\AI-learning\projects\rag-kb
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\vectorize.py --limit 5000 --out eval\results\vec_smoke
```

**验收结果**：

| 验收项 | 标准 | 实测 | 过？ |
|---|---|---|---|
| 编码条数 | 5,000 | 5,000 | ✅ |
| emb 形状 / dtype | (5000, 1024) / float16 | (5000, 1024) / float16 | ✅ |
| 自检范数 | ≈ 1.0 | **[0.9995, 1.0005]** | ✅ |
| NaN | 0 | 0 | ✅ |
| `ids.txt` 行数 | 5,000 | 5,000 | ✅ |
| 从 parquet 回查原文 | 全部命中 | 5,000 / 5,000 | ✅ |
| 吞吐 | — | 112.0 chunk/s（含加载预热，见 T3.5） | — |
| **截断率** | 需量化 | **15.95%**（980 / 6,144 条超 512 token） | ⚠️ 见 R1 |

**语义冒烟 —— 这一步才是真验收**：

```cmd
"C:\...\Python313\python.exe" src\smoke_search.py --index eval\results\vec_smoke --topk 3
```

| Query | Top-1 | 相似度 | 判断 |
|---|---|---|---|
| 台灣東部開發於古時的人行道路 | 蘇花古道 | 0.6075 | ✅ 正文首句就是 query 原意 |
| 中國的化學工程學家 | 张克忠 | 0.6449 | ✅ 正文自称「中国化学工程学家」 |
| 這個地方氣候怎麼樣 | 高邮市 · **气候** | 0.5767 | ✅✅ query 里没有任何地名，命中的却全是「气候」小节 |

> 🔴 **第三条是 `section` 拼接策略的直接证据。**
> query 只给了"气候"这个抽象概念，top1/top2 却恰好是 `高邮市/气候`、`杭州市/气候` ——
> 说明 `title｜section｜正文` 的拼法让向量真的学到了**小节级语义**。
> 如果只喂 `chunk_text`，这类"无实体概念查询"会明显变差。
> **这条要写进 README 和面试话术。**

---

---

### [x] T3.5 · 稳态吞吐实测（决定全量预算）

**为什么必须单独测**：T4 是 6.8 GB / 若干小时的**不可逆**投入。
"大概几小时"不能靠估 —— 估错 10 倍，等于白等一整晚或白砍一半数据。

**命令**（同样 20,000 条，各跑一遍）：

```cmd
"C:\...\Python313\python.exe" src\vectorize.py --limit 20000 --out eval\results\vec_bench
"C:\...\Python313\python.exe" src\vectorize.py --limit 20000 --model-kind small --out eval\results\vec_bench_small
```

**实测对比（真实 chunk，不是伪样本）**：

| 模型 | 维度 | 稳态吞吐 | **全量 331.6 万块** | 索引大小 (fp16) | 截断率 |
|---|---|---|---|---|---|
| **bge-large-zh-v1.5** | 1024 | **110.7 chunk/s** | **≈ 8.3 小时** | 6.79 GB | 16.16% |
| **bge-small-zh-v1.5** | 512 | **1102.7 chunk/s** | **≈ 50 分钟** | 3.39 GB | 15.66% |

**差 10 倍。** 参数量只差 3 倍，吞吐却差 10 倍 —— small 的层数/宽度能让 4060 吃满，
large 则卡在访存上。**结论：这一步的耗时完全由模型选型决定，不是"等一下就好"。**

（顺带记录一个无害但吓人的警告：small 会打印
`Token indices sequence length is longer than the specified maximum sequence length (535 > 512)`。
那是 `--token-stats` 用 `truncation=False` 量长度导致的，**不影响编码**（编码时按 512 截断），
别当成错误去改。）

---

### [x] T4 · 全量编码 —— **完成 ✅ 2026-09-18 17:54**

**命令**：

```cmd
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\vectorize.py --resume
```

**验收标准**（附实测值）：

- [x] 编码条数 = **3,316,395** —— 与 parquet 行数**逐位相等**
- [x] `emb.npy` = 6,791,977,088 B（6.79 GB）；`ids.txt` 行数 = 3,316,395 **一致**
- [x] `meta.json` 字段齐全（`text_format` / `dim=1024` / `dtype=fp16` / `normalized=true` / `token_stats`）
- [x] 中断后 `--resume` 能接着跑（**已用真实中断场景验过 3 次**，见 T4.5）

**最终产出**：

| 项 | 值 |
|---|---|
| 编码条数 | **3,316,395 / 3,316,395 = 100%** |
| 本轮耗时 | **5,979.4 秒 = 1 小时 40 分**（113.0 chunk/s 稳态） |
| 自检范数 | [0.9995, 1.0005]，NaN = **0** |
| 接缝自检 | 第 2,640,894~2,640,900 行范数 [1.0000, 1.0003] ✅ |
| section 填充率 | **78.49%** |
| token 长度 | P50 **201** / P90 **609** / P99 775 / max 827 |
| ⚠️ 超 512 截断 | **16.16%**（3,232/20,000）—— BGE 硬上限，**已知局限，已接受** |
| 磁盘占用 | `emb.npy` 6.79 GB + `ids.txt` 113 MB |

**执行记录**：

| 时刻 | 事件 | 进度 |
|---|---|---|
| 9-17 14:32 | 启动全量（bge-large-zh-v1.5） | 0% |
| 9-17 16:14 | **电脑关机**（事件 `1074 User32`）→ 进程结束，日志**无任何报错** | **20.8%** |
| 9-17 16:14–18:25 | 停机 | |
| 9-17 19:22 | 修完 2 个续跑错位 bug → `--resume` 从 689,687 续跑 | 20.8% |
| 9-17 23:41 | **又是关机**（`1074` 发起者 = `StartMenuExperienceHost`，从开始菜单点的） | **76.4%** |
| 9-17 23:41–9-18 14:37 | 停机 | |
| 9-18 15:32 | 跑 `src\check_vectors.py` 体检：**2,534,401 个 id 全部唯一** → 前 76.4% 没写歪 | 76.4% |
| 9-18 15:36 | `--resume` 续跑 | 76.4% ↑ |
| 9-18 15:52 | 用户「只暂停我的推进」→ 我停手，进程继续跑 | 79.4% |
| 9-18 15:56 | **电池耗尽自动关机**（`1074` 由 `winlogon.exe` 代表 `NT AUTHORITY\SYSTEM` 发起 = **系统级关机**，非用户点击） | **79.63%** |
| 9-18 15:59 | 开机返工（`LastBootUpTime` = 15:59:37） | |
| 9-18 16:13 | 体检：**2,640,897 个 id 全部唯一** → 三次中断后数据仍可信 | 79.63% |
| 9-18 16:14 | `--resume` **第 3 次续跑**，起点正确识别 = `ids.txt` 行数 | 79.63% ↑ |
| 9-18 17:54 | **`VECTORIZE_OK` 全部跑完** ✅ 3,316,395 行逐位相等，接缝自检通过 | **100%** |

**实际耗时**：本轮 5,979.4 秒（1 小时 40 分，113.0 chunk/s）。
**总历时**：9-17 14:32 → 9-18 17:54（跨 27.4 小时，其中**有效计算约 8 小时**，
其余是 **3 次关机/断电导致的停机**）。→ 这就是为什么"能续跑 + 续跑前体检"值钱。
进度**只信 `wc -l data\index\bge-large-zh-v1.5\ids.txt`**，不信 `progress.json`（见 T4.5）。

🔴 **结论：三次中断全是主动关机/断电（用户点击 2 次 + 电池耗尽 1 次），一次崩溃都没有。** 长任务必须假设"一定会被打断"（人要睡觉、要断电、要关机），
所以"能续跑 + 每次续跑前体检"是硬需求，不是加分项。

---

### [x] T4.5 · 修复续跑错位 bug（事故衍生任务）—— **已修复并验证**

**为什么新增**：原 `--resume` 有两个**静默**错位 bug，会把 8.8 万行重复写进 `ids.txt` 并伪装成正常。

**Bug A —— 起点查错了文件**：`done` 原读 `progress.json`，但它**每 10 万行才落盘**。
关机那一刻真值 `ids.txt` = 689,687，`progress.json` 只记 601,827。

**Bug B —— 跨起点批次重复 append**：`done` 不是 `read-batch`(2048) 的整数倍，
整批写入会把起点之前的 id 也 append 一遍；而 `emb.npy` 那几行是**重复覆盖**（内容相同），
**肉眼与自检都发现不了**，只在检索时表现为「相似度 0.7 但内容对不上」。

**修法**（`src\vectorize.py`）：

1. 起点改为**数 `ids.txt` 的行数**（主循环先写 emb 后写 ids → ids 永远不超前，是可靠真值）
2. 末行不完整（断电截断）→ 丢弃该行，不留残缺 id
3. 跨起点批次只 append `cids[skip:]`，`skip = max(0, done - pos)`
4. 新增**接缝自检**：查 `done` 前后各 3 行范数 + `ids.txt` 行数 == 已编码数
5. 行数不一致 → `return 3` 非零退出

**验证方式（关键）**：干净跑 3,000 条存基线 → `ids.txt` 砍到 1,559 行（**故意取非批次整数倍**）
+ `progress.json` 伪造成滞后的 `done=1200` → `--resume` → 与基线 `cmp` 逐字节比对。

**实测结果**：`ids.txt` 与 `emb.npy` **逐字节完全一致**，日志正确识别「起点 = 1,559 行（52.0%）」，
接缝范数 `[0.9996, 1.0004]`。

---

### 选型决策记录（用户 2026-09-17 拍板）

**为什么必须由用户拍板**：这是本步唯一"不可逆地花时间"的动作（8.3 小时）。跑完才能进第 8 步建索引。

**选型对比（依据 T3.5 实测，不是估算）**：

| 方案 | 全量耗时 | 索引大小 | 适合什么情况 |
|---|---|---|---|
| **A. bge-large 全量** ← **已选** | **≈ 8.3 小时** | 6.79 GB | 作品集定位、检索质量优先；挂夜间/上班时间跑 |
| **B. bge-small 全量** | **≈ 50 分钟** | 3.39 GB | 时间或算力受限；先要一个能用的库 |
| **C. 先 small 全量 → 打通链路 → 择机重跑 large** | 50 分 + 8.3 时 | 两套 | 想今天就看到检索效果，但最终仍要 large |

**倾向 A**，三条理由：

1. 规格就是 `bge-large-zh-v1.5`（手册 + 技术方案）。换模型属于**改规格**，需要显式记录而不是顺手换掉。
2. 这个项目的用途是求职作品，**embedding 质量直接决定能拿出来讲的检索指标**。
3. 8.3 小时是**一次性**投入，`--resume` 保底；先 small 再 large 等于同一个全量跑两遍，总成本更高。

**C 唯一的不可替代之处**：第 8 步调检索策略（top-k、阈值、混合权重）要反复试，
小索引迭代快得多。如果预期要反复调参，先 small 打通更省时间。

⚠️ 无论选哪个，**同一个时刻只能跑一个**（只有一块 4060，8 GB 显存），不要在后台同时开两个。


---

### [ ] T5 · 建索引 + 冒烟检索

**属于第 8 步**。本步做完 T4 即停 —— 先确认向量本身没问题，再决定用 chromadb 还是 faiss。

## 已知风险（必须提前说清，不藏）

| # | 风险 | 量级 | 处置 |
|---|---|---|---|
| **R1** | **512 token 硬截断** | **实测 16.16%**（20,000 条样本中 3,232 条超 512 token；token 长度 P50=201 / P90=609 / max=827） | 已精确量化。选项：① **接受** —— 被截掉的是块尾约 30% 内容，而块首通常已含主体信息；② 调小切块上限重跑第 5 步（块数会从 331 万涨到约 450 万，向量化时间同比上升）；③ 换长上下文模型（如 bge-m3，8192 token，**超出本步规格**） |
| R2 | 磁盘占用 | 全量 fp16 = **6.79 GB** | E 盘余 150 GB，通过 |
| R3 | 长任务中断 | **已真实发生**：9-17 16:14 电脑关机，中断在 **20.8%**（689,687 行），日志无报错 | `--resume` 已修好 2 个静默错位 bug，并**用真实中断场景做了字节级对照验证**（T4.5）⚠️ 关机/快速启动关机**会**杀进程，**睡眠不会** |
| R4 | 8 GB 显存 | batch=64 / fp16 约需 1.5 GB | 有 `--batch` 可调；显存不足会自动报错而非静默降级 |
| R5 | 编码顺序稳定性 | 续跑依赖"扫过顺序 == 写入顺序" | 用 scanner 顺序迭代，不 shuffle；`ids.txt` 行号即向量行号 |

## 质量要求

- [x] **无后台常驻进程** —— 脚本跑完即退，不留 daemon
- [x] 不追加 `&`，不用 `nohup` 挂长期进程
- [x] 编码文本必须含 `section`（已写入 `meta.text_format` 留证）
- [x] 产物可溯源：`ids.txt` 第 n 行 ↔ `emb.npy` 第 n 行
- [x] 不提前建索引（避免 T3 发现问题后返工）

## 技术备注

1. **第 7 步与第 5 步的工具链不同**：第 5 步（Spark）→ 3.11 + winutils；第 7 步（torch）→ 3.13 + pyarrow。
   两者**没有重叠**，不要习惯性去调 3.11。
2. 分隔符选**全角竖线 `｜`**：中文维基正文里几乎不出现，不会被模型误当内容。
3. 向量已归一化 → 第 8 步检索直接用内积，不要再除范数。
4. `max_length=512` 是 BGE 的**模型上限**，调大无效（位置编码只有 512）。
