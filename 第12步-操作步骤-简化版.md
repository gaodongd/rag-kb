# 第 12 步 · 取原文延迟优化 —— 操作步骤（简化版）

> 这份是**能照着做**的版本：4 条命令 + 8 个注意点 + 6 个知识点。
> 想要完整过程（每条口径争议、每个 bug 的根因）看 [`第12步-任务清单.md`](第12步-任务清单.md)。

---

## 0. 一句话说清这一步

**把"为了取 100 行原文，去解压 2.2 GB"换成"用偏移数组 + mmap 直接点查"。**

| 层面 | 改前 | 改后 | 倍数 |
|---|---|---|---|
| 取原文（微基准，取 100 条） | **1716 ms** | **0.9 ~ 15 ms** | 114 ~ 1900× |
| 取原文（链路内实测，25 题中位） | **3069 ms** | **21 ms** | **146×** |
| 检索链路合计（同批 25 题） | 6096 ms | **3150 ms** | 1.9× |
| 端到端（云端生产配置，**外推**） | 4.78 s | **约 2.9 s** | — |

为什么这一步值钱：第 11 步量出来的延迟瓶颈排序是
**取原文 1.9 s > 重排 1.36 s ≈ 生成 1.23 s > 检索本身** ——
最大的一项既不是检索、也不是模型，而是"把原文捞出来"。

---

## 1. 五步操作（照抄即可）

```bat
REM ① 先关掉 Gradio（两个原因：它占 13.6 GB 索引内存；它会 mmap 锁住侧车文件）

REM ② 建侧车（48 秒，产出 2.87 GB 内容 + 26.5 MB 偏移数组）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\fetch_store.py --build

REM ③ 自检 —— **必做**，派生数据不校验就是埋雷（随机 300 行与 parquet 逐字段比对）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\fetch_store.py --verify 300

REM ④ 正常用就行：默认 --fetch-store auto，有侧车就自动用（会打印一行确认）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\generator.py ^
    --backend dashscope --testset eval\qa_testset_v1.jsonl --rerank

REM ⑤ 收尾自检：不加载索引、0.6 秒，验证"界面/命令行的参数表 + 检索链路"没被改坏
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\smoke_retrieve.py
```

第 ⑤ 步是**这次事故补上的**：界面原来自己手抄了一份检索参数表，
`generator` 新增 `--fetch-store` 时它没跟上 → 用户**加载完 13.6 GB 索引、点了"提问"**
才看到 `Namespace object has no attribute 'fetch_store'`。
现在参数只有一份来源（`generator.RETRIEVAL_DEFAULTS`），并且 0.6 秒就能把
"界面 / 命令行 / 空命名空间 / 旧版命名空间"四种情况全跑一遍。

想自己量前后对比（**不用加载索引，各 1 分钟**）：

```bat
"C:\...\python.exe" src\diagnose_fetch.py --impl parquet          REM 基线：全扫
"C:\...\python.exe" src\diagnose_fetch.py --impl blob --repeat 40  REM 侧车：点查
```

想临时退回旧实现（比如怀疑侧车脏了）：加 `--fetch-store parquet`。

---

## 2. 注意点（按"最容易踩"排序）

| # | 注意点 | 后果 / 怎么处理 |
|---|---|---|
| 1 | **侧车不入库**（2.87 GB） | 别人 clone 下来没有它。`auto` 会**显式打印一行提示**再退回基线 —— 不会静默退化。想用就先 `--build` |
| 2 | ⚠️ **Windows：mmap 会锁住文件** | 重建侧车时若 Gradio 还开着，替换会 `PermissionError`（脚本已给中文提示）。**先关进程再建** |
| 3 | **重建索引后必须重建侧车** | 侧车是派生数据。行数契约会直接报错（3,316,395 vs …），不会静默错位 |
| 4 | **别只看墙钟** | 本机实测出现过"冷盘比热盘还快"（后台进程干扰）。归因要用 **CPU 时间 + 磁盘读字节** |
| 5 | **别把热缓存当常态** | 0.9 ms 是"命中在少数页"的成绩；完全随机散布是 **15 ms**。两个数都要知道，别只报最好看的那个 |
| 6 | **代价与"取几条"无关** | 取 5 条和取 100 条一样慢（都得把分片解压一遍）。所以"少取几条"不是优化，"换存储布局"才是 |
| 7 | **派生物必须可校验** | 三件套：原子替换（先写 `.tmp`）、行数契约、每次取回的**错位断言**（chunk_id 与行号必须对得上） |
| 8 | **换实现会改延迟数字** | 侧车比全扫快约 150×。跟第 11 步的延迟结论对比时，必须两边实现一致 —— 这点已写进 `evaluate.py` 的参数注释 |
| 9 | **同一份配置别写两处** | 界面和命令行各写一份参数表 → 必然漂移。收成 `RETRIEVAL_DEFAULTS` 单一来源 + `ensure_retrieval_args()` 兜底，**且兜底要打印补了哪些**（不静默才看得见漂移） |
| 10 | **改完代码要重启界面** | 已经开着的界面跑的是**旧代码**（Python 进程里的模块不会自动重载）。改完必须关掉窗口重新双击，否则"改了没生效" |
| 11 | **参数改动跑一次 0.6 秒的自检** | 别赌"用户点了按钮才知道"。`src/smoke_retrieve.py` 桩掉需要索引/显卡/API 的三样东西，其余全真 |

---

## 3. 知识点教学（这 7 个能直接迁移到别的项目）

### K1 · 存储布局决定取数代价：列式 ≠ 点查友好

**一句话**：parquet 是**列式 + 行组（row group）+ 压缩**存的，它是为"扫一列做聚合"优化的，不是为"取几行"优化的。

- 取一行的动作是：定位 row group → **把整列解压出来** → 取那一行。
- 实测布局：4 个分片 × 552 MB，每个分片 5 个 row group（20.2 万行 / 179 MB 未压缩），压缩比只有 1.3×。
- 所以：**取 5 行和取 100 行一样慢**（都要先解压）。这是全表扫描的代价，不是点查的代价。

**可迁移**：数据库选型（OLAP vs OLTP）、数据湖查询、缓存设计。
面试会问："为什么查询只返回 10 行却要几秒？" —— 答案常常在这里。

### K2 · 谓词下推的前提是"过滤字段有序"

**一句话**：parquet 的行组有 min/max 统计，能跳过无关行组 —— **但前提是过滤字段有序或聚集**。

- 本项目用 `chunk_id.isin(...)` 过滤，而 chunk_id 是随机哈希（`b534134734df4a711535059fce4a87c4`）：
  每个行组的 min/max 都覆盖全域 ⇒ **剪枝率 0** ⇒ 4 个分片全部打开。
- 对比：如果按时间 / 自增 ID / 行号这类**有序键**过滤，能直接跳过 95% 的行组。
- 这就是为什么数据库里"主键/时间列"过滤秒回，而"uuid 列"过滤全表扫。

**可迁移**：分区键设计（Hive/Spark 按天分区，绝不按 uuid）、索引设计、ES 分片路由。

### K3 · 随机访问：读的字节数应该与"库多大"无关

**一句话**：偏移数组 + mmap 之后，取第 i 行的代价是 O(1)，**与 i 在哪无关，与文件多大无关**。

- 侧车 = 一个连续字节流（每行一条 JSON）+ 一个 int64 偏移数组；
  取第 i 行 = `mmap[off[i] : off[i+1]]`，两次内存访问 + 一次切片。
- 取 100 行 ≈ **87 KB**（100 × 870 字节），而全扫一次是 **2060 MB**。
  这个对比才是关键：**不是"缓存命中"，而是"根本不用碰那 2.87 GB 的其余部分"**。
- mmap 的两个附带好处：① 不用把 2.87 GB 读进进程堆（RSS 几乎不涨）；
  ② 偏移数组常驻 26.5 MB 就够导航。

**可迁移**：任何"大文件里随机取小块"的场景 —— 向量库的 ID→向量映射、日志按行号取、模型权重分片加载。

### K4 · 派生数据的一致性纪律（这是最容易翻车的地方）

**一句话**：派生物（侧车、缓存、物化视图）必须能**被校验**，否则你只是把 bug 藏得更深。

这次做了四件事，缺一件都会变成"输出看着正常但数据是旧的"：

1. **原子替换**：先写 `.tmp`，校验通过再改名 —— 中途失败不会留下"半截但看着能用"的侧车。
2. **行数契约**：开侧车时 `行数 == ids.txt 行数`，不等直接报错退出。
3. **抽样自检**：`--verify 300` 随机 300 行与 parquet **逐字段**（不只 chunk_id）比对 → 0 不一致。
   （只比 chunk_id 不够：错位一行也能取到一个合法的 chunk_id。）
4. **每次取回错位断言**：`payload["id"] == ids[row]`，成本几微秒。

**可迁移**：缓存层、特征存储、物化视图、CDN 回源、任何"为了快而复制一份数据"的设计。

### K5 · 测量方法论：墙钟不是证据（这一步最值钱的教训）

**一句话**：**优化前先量；量之前先审自己的工作量构造。**

- 归因别用墙钟：本机实测同一份工作"冷盘 907 ms、热盘 1822 ms" —— 冷盘反而更快，
  说明后台进程干扰大，冷/热这种分法**得不出可靠结论**。
  改用 **CPU 时间 + 磁盘读字节**：`CPU 5578 ms（墙钟 325%，即并行解压吃 3.3 个核）+ 磁盘读 2060 MB`。
  这两个数与缓存状态无关，能直接回答"瓶颈是 CPU 还是磁盘"。
- **我踩的坑（值得记住）**：第一版诊断把"真实命中"按行号排序后切连续的段 →
  那 100 行恰好落在**同一个分片**里 → 量出来比实际快一倍，冷热差甚至是负的。
  工作量构造错了，基准就是错的。改法：**随机抽样**，别用排序后的连续段。

**可迁移**：任何性能优化、压测、基准测试（benchmark 造假最常见的两种方式：
数据分布不真实、缓存状态不声明）。

### K6 · 优化排序：按"占比 × 可压缩倍数"，不是按"哪个听起来高级"

**一句话**：Amdahl 定律 —— **先干掉最大的那一项，而且要先确认它真的能被压缩。**

- 第 11 步已有的结论：检索侧已经探到底（融合权重 / 查询路由的 oracle 上界只有 +0.019）。
- 而下游躺着一个 1.9 s 的大头，占总延迟 40%。先动它。
- 结果也验证了判断：这一项被压缩了 **146×**，整条链路直接砍掉约一半（6.1 s → 3.2 s）。

**可迁移**：性能优化排序、容量规划、任何"资源该投哪"的决策。
面试官问"你怎么定优化优先级"，这就是答案模板。

### K7 · 验证的成本要压到"顺手就能跑"（这一步收尾时才学会的）

收尾时踩了个真坑：界面点"提问"直接报 `Namespace object has no attribute 'fetch_store'`。
根因不新鲜 —— **同一份配置在两个地方各写了一遍**（命令行 argparse 一份，界面手抄一份），
新增参数时漏了一处。真正难受的是**它的暴露时机**：

| | 代价 |
|---|---|
| 命令行版漏了 | 一条报错，改完就完事 |
| 界面版漏了 | 加载 13.6 GB 索引（1 分钟）→ 用户输入问题 → 点按钮 → 才报错 |

**同一个 bug，被"启动成本"放大成一次演示事故。** 所以正解不是"下次仔细点"，
而是把验证的成本压到很低的量级：

1. **参数只留一份来源**：`RETRIEVAL_DEFAULTS` 一个 dict，两边的 `default=` 都从它取；
2. **兜底要响**：`ensure_retrieval_args()` 缺啥补啥，**并把补了哪些打印出来** ——
   静默补齐等于把"漂移"这件事藏起来；
3. **把最贵的那一步换成桩**：验证里唯一需要 13.6 GB 索引的是 `HybridSearcher`，
   桩掉它、保留其余全部真实 → 检查从"1 分钟 + 一次点击"变成 **0.6 秒一次**。

⭐ **最值得带走的一条**：测试里要放**对照组**。
兜底上线之后，"旧版字段"那份样本自己就变绿了 —— 测试看起来什么都没测。
所以脚本里额外做了一件事：**临时把兜底拆掉再跑一次，期望它崩**。
这一步同时证明了两件事：**bug 真实存在过**，以及**测试确实抓得住它**。
否则将来谁把兜底删了，测试还是绿的 —— 那就是个**安慰剂**，
它给你的是"安全感"，不是"安全"。

**可迁移**：任何"要等很久才暴露"的 bug 都适用（配置缺失、启动期初始化、演示脚本）。
一句话版本：**别让验证路径比 bug 的暴露路径还贵。**

---

## 4. 招聘 / 简历视角

**这一步对应的岗位关键词**：RAG 工程师 / LLM 应用工程师 / 数据基础设施 / 检索工程 / ML 平台。

| 术语（面试会问） | 你的答案要点 |
|---|---|
| 列式存储 / row group | parquet 为全表扫设计；取行要解压整个 row group |
| 谓词下推 / 分区剪枝 | 前提是过滤字段有序；随机哈希 ⇒ 剪枝率 0 |
| 点查 vs 全表扫 | 100 行 87 KB vs 2060 MB —— 差在布局，不在硬件 |
| mmap / 零拷贝 | 进程 RSS 不涨；代价与文件大小无关 |
| 物化 / 派生数据一致性 | 原子替换 + 行数契约 + 逐字段自检 + 每次取回断言 |
| 冷热缓存 / p50 p90 | 两个数都要报；墙钟不可归因，用 CPU + read_bytes |
| 单一来源配置（SSOT） | 同一份配置写两处必然漂移；默认值从一个 dict 取 |
| 可测试性 / 桩与依赖注入 | 把"贵依赖"（索引/显卡/API）换成桩，验证从 1 分钟降到 0.6 秒 |
| 回归测试 / 对照组 | 断言"关掉修复应该失败"，否则测试会退化成安慰剂 |

**面试官最可能追问的三题**：

1. **"侧车脏了怎么办？"** → 四层防护：原子替换、行数契约、抽样逐字段自检、每次取回错位断言。
   而且**检测是响亮的**（报错退出 / 抛异常），不是静默错位。
2. **"怎么保证改了检索参数不会把界面搞坏？"** → 参数收成单一来源 + 兜底要打印 +
   0.6 秒的自检脚本；脚本里带**对照组**（临时关掉兜底，断言它必须崩），
   证明测试不是安慰剂。（这就是收尾那次事故的完整答案。）
3. **"为什么不用 SQLite / 数据库？"** → 都可以，但这里要的是"零解析开销的点查"：
   mmap + 偏移数组没有查询引擎、没有 SQL 解析、没有事务开销；26.5 MB 偏移就够导航。
   SQLite 是更通用的选择（要事务、要条件查询时选它），此处不需要。
4. **"这个优化有没有副作用？"** → 有两条，都量过：
   ① 磁盘多占 2.87 GB（用空间换确定性延迟）；
   ② 派生数据要和索引同版本（靠契约强制）。
   附带发现：**旧实现不只自己慢，还在抢 CPU** —— 换掉之后向量检索中位也从 542 ms 降到 232 ms。

**简历可以这么写**（数字都是实测）：

> 定位并消除 RAG 管线的最大延迟项：原文回查原为 parquet 全表扫（取 100 条 3.1 s，
> 占端到端 40%）。改为**偏移数组 + mmap 点查**的派生存储，取原文降至 **21 ms（146×）**，
> 检索链路 6.1 s → 3.2 s；设计原子替换 + 行数契约 + 逐字段抽样自检四层一致性防护，
> 并建立零成本诊断脚本（不加载索引，1 分钟量出分项延迟与 CPU/磁盘归因）。

---

## 5. 附：本次实测原始数据

**微基准（同一批 100 个行号，同一台机器）**

| impl | 中位 | CPU 时间 | 磁盘读 | 单分片代价 |
|---|---|---|---|---|
| parquet 全扫 | **1716 ms** | 5578 ms（墙钟 325%） | **2060 MB** | 429 ms |
| 侧车 mmap（均匀随机行号） | **14.6 ms** | 0 ms | 0 MB | — |
| 侧车 mmap（命中扎堆） | **0.9 ms** | 0 ms | 0 MB | — |

**链路内 A/B（25 题、同一评测集前缀、echo 后端、取第 2 题起算中位）**

| 分项 | parquet | 侧车 | 倍数 |
|---|---|---|---|
| 向量检索 | 542 ms | 232 ms | 2.3× ← 副作用：旧实现在抢 CPU |
| BM25 | 62 ms | 48 ms | 1.3× |
| **取原文** | **3069 ms** | **21 ms** | **146×** |
| 重排 | 2488 ms | 2828 ms | 0.9×（噪声） |
| **合计** | **6096 ms** | **3150 ms** | **1.9×** |

**侧车本身**：3,316,395 行 / 2.87 GB 内容 + 26.5 MB 偏移，构建 **48 秒**（70k 行/s）。
自检 300/300 字段一致；同一批两路耗时 1307 ms vs 4.9 ms（266×）。

> ⚠️ 端到端 4.78 s → 约 2.9 s 是**外推**（把已实测的 1.9 s 换成 0.02 s），
> 不是重新跑的一遍全量 129 题 —— 因为云 API 额度只剩 turbo，重跑全量的收益不值这个成本。
> 这条已在文档里标注，别当成实测。
