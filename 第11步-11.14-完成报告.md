# §11.14 生成侧指标 —— 完成报告

**项目**：`E:\AI-learning\projects\rag-kb`（中文 RAG 知识库问答）
**时间**：2026-09-21 19:00 ~ 21:30
**任务**：补上"生成侧"四类指标（拒答 / 误拒 / 引用 / Faithfulness）+ 本地路对照

---

## 一句话结论

**检索指标好 ≠ 能用。** 同一套检索链路（耗时 3471 vs 3455 ms，对照组纹丝不动），
只换生成模型，端到端通过率就从 **84.5% 掉到 51.9%**。
而重排开相对重排关最硬的收益是**引用到 gold 显著更好（97 vs 87，p=0.0063）**。

## 结果速览

| 配置 | 库外拒答 | 库内误拒 | 真误拒 | 引用准(严/宽) | 答案命中 | **端到端** | 判官FA | 检索中位 |
|---|---|---|---|---|---|---|---|---|
| 云端 · 重排开（生产） | 92.0% | **0.0%** | 0.0% | 94.2% / 92.3% | 82.7% | **84.5%** | 90.4% | 3471 ms |
| 云端 · 重排关 | 100.0% | 9.6% | 2.2% | 95.5% / 90.4% | 75.0% | 79.1% | 89.8% | 2094 ms |
| 本地 Qwen2.5-1.5B | 16.0% | 3.9% | 3.9% | 24.2% / 63.2% | 60.6% | **51.9%** | 72.1% | 3455 ms |

## 关键判断（三处最容易读错的）

1. **重排关的"拒答 100%"不是更好** —— 它是拿误拒换的（库内误拒 0% → 9.6%）。
   拒答必须拆成两个方向报，不能合成一个"越高越好"的数。
2. **误拒要按"gold 有没有进上下文"分层** —— 重排关那 9.6% 里八成是检索失败的传导，
   真误拒只有 2.2%。不拆开就会把检索的账算到生成模型头上。
3. **引用准确率 94.2% → 95.5% 不能直接比**（分母 103 vs 89）。跨配置比比率先看分母。

## 修掉的真问题（都是"指标自己错了"，不是模型的错）

| 问题 | 后果 |
|---|---|
| 拒答标记表漏了等价说法（"没有关于…的内容"） | 真拒答被误报成幻觉硬答，且落在**最危险的那张表**里 |
| 判官偶尔回中文 verdict（有依据/无依据） | **20 个子句被静默踢出分母**（含最该看的库外硬答） |
| `hits` 没存正文 → 上下文取不到 | faithfulness 全 0、数字违规虚报 126/175 —— **"缺失"被当成"零分"** |
| `search()` 第 4 参错传 `topk` | RRF 融合被截到 5 条，gold 进上下文 91 vs 103 对不上 |
| 本地路最后一次性落盘 | **13.6 分钟、129 条答案全丢**（已改边跑边写） |

## 判据可靠性（本节最花时间的一段）

- 三套词面判据（2-gram / 子句 / 实词覆盖率）对 18 条人工标签的一致率只有 **61~72%**，
  对"用词全对、关系说反"**完全漏检**。
- 主口径改用 **LLM 判官，先校准后上场**（16/16 通过才允许跑全量）。
- **判官模型敏感性**：同一批 104 条上 qwen-plus 93.6% vs qwen-turbo 93.9%，Δ **0.4 pp** ⇒ 结论不依赖判官选型。
- 🔴 本地路出现**判据反向分叉**：云端判官比词面**高 1 点**，本地判官比词面**低 8.6 点**。
  原因是错误类型变了 —— 1.5B 大量吐表格残片（`｜生平`）被词面判据当成断言，
  还有"字面全对、关系说反"（判官理由就是"与参考资料矛盾"）。
  **⇒ 只报一个词面 FA 无法做跨模型比较，它的偏倚方向随模型而变。**

## 交付物

**新增源码**
- `src/evaluate_gen.py` —— 四类指标主脚本（离线，只读 jsonl，秒级，不碰索引）
- `src/judge_faith.py` —— Faithfulness LLM 判官（含校准与缓存）
- `src/calibrate_faith.py` + `eval/faith_labels.jsonl` —— 18 条人工校准标签（含 4 条合成对照）

**结果产物**（`eval/results/`）
- `gen_tables.md` —— 三方对照表 + 配对检验
- `gen_metrics.json` / `gen_metrics_run.log` —— 全量指标与逐条错误案例
- `gen_dashscope_testset_{rr,norr}.jsonl`、`gen_local_testset_local_rr.jsonl` —— 三组生成结果（含原文上下文）
- `gen_faith_judge_{rr,norr,local_rr}_turbo.jsonl` —— 判官明细

**文档**
- `第11步-任务清单.md` §11.14（11 个小节，含方法论与复现命令）
- `README.md` —— 新增"生成侧指标"小节；**改写"双路 LLM 对照"**（旧的"本地路拒答稳定"已被实测推翻）
- `操作手册.md` —— 坑清单新增 **AX~BD** 共 8 条

## 后续可做（按性价比）

1. **压延迟先压"取原文"**（1896 ms，占端到端 40%，比整个重排还多 40%）——
   批量取块、减少回查次数。
2. **拒答门槛针对"主观评价题"**：库外硬答 2 条都是"问性格、资料里只有客观事实"，
   要检测的是"query 要评价 + 上下文只有事实"这个组合，不是调阈值。
3. **本地路若要用**：先解决指令遵守率（66.9% 不标引用），再谈拒答能力（库外仅 16%）。
4. 评测集 v2：修 3 条表述瑕疵 + 1 条 gold 答案过粗（v1-f4e8ca 的"大屠杀期间"）。
