# 第 12 步 · 取原文延迟优化

> **这一步交付**：把 RAG 管线里最大的那一项延迟（原文回查）压掉 146×，
> 并留下一个**不加载索引、1 分钟出结果**的诊断脚本。
>
> 起点是第 11 步给出的硬结论（§11.14.9）：
> **取原文 1.9 s > 重排 1.36 s ≈ 生成 1.23 s > 检索本身**，
> 取原文单项占端到端 40% —— 比整个重排还多 40%。

---

## 12.0 为什么是这一步（而不是继续调检索参数）

| 方向 | 预期收益 | 依据 |
|---|---|---|
| 继续调检索参数（融合权重 / 路由） | **≤ +0.019** | 第 11 步 oracle 上界，已探到底 |
| 调重排参数 | 已榨干 | §11.13：`rerank-pool 50` + `max-length 1024` 是白捡的 |
| **压取原文延迟** | **端到端 −36%** | 1.9 s / 4.78 s，且它是"纯工程开销"，与模型质量无关 |
| 换更快的 LLM | 便宜不了多少，还掉质量 | 生成 1.23 s 已接近 API 往返下限 |

**判据**：优化顺序按「占比 × 可压缩倍数」排。
取原文既占比最大（40%），又几乎 100% 可压缩（本质是"为了 100 行解压 2.2 GB"），
所以它是唯一合理的第一顺位。

---

## 12.1 先诊断：写一个"零成本"诊断脚本

**方法**：取原文的耗时只取决于 ① 取哪些行号 ② 存储的物理布局 ——
这两件事**都不需要加载索引**。行号可以从历史评测结果里回收（`hit_chunk_ids`），
布局可以从 parquet 元数据里读（只读元数据，不解压）。

→ 于是有了 `src/diagnose_fetch.py`：**不加载 faiss / BM25 / 模型，1 分钟出结果**。
（同一条方法在第 11 步用过：`diagnose_rerank_trunc.py` 只加载 tokenizer 就量出截断率。）

### 发现 ①：布局

| | 数值 |
|---|---|
| 分片 | 4 个 × 552 MB |
| 每分片 row group | 5 个（202,614 行 / 179 MB 未压缩） |
| 压缩比 | 1.3×（磁盘 2.21 GB → 解压 2.92 GB） |

**要取 100 行，至少要打开 1 个 179 MB 的 row group。**

### 发现 ②：谓词下推在这里完全失效

`fetch_texts` 用 `chunk_id.isin(...)` 过滤。chunk_id 是**随机哈希**
（`b534134734df4a711535059fce4a87c4`），任何 row group 的 min/max 统计都覆盖全域
⇒ **剪枝率 0** ⇒ 4 个分片全部打开、全部解压。

### 发现 ③：代价与"取几条"无关（反直觉，但决定性）

| 请求条数 | 耗时 |
|---|---|
| 100 行 | 1716 ms |
| 300 行（自检时） | 1307 ms |

**取 3 倍的行数，耗时不变甚至更低。** 这说明时间不花在"取数"上，
而花在"为了取数而解压整片"上 —— 所以任何"少取几条"的思路都是无效优化。

---

## 12.2 归因：别用墙钟（我自己先踩了一次）

### 坑：工作量构造错了，基准就是错的

第一版诊断把"真实命中"排成有序的行号列表，然后**切连续的段**当批次 ——
结果那 100 行恰好落在**同一个分片**里，量出来比均匀随机快近一倍，
甚至算出"冷启动 301 ms < 热态 1076 ms"这种荒谬结论（冷热差为**负数**）。

**改法**：批次必须**随机抽样**（`rng.choice(hit_rows, size=pool, replace=False)`），
不能切排序后的连续段。修完之后两批工作量的"均摊分片数"都是 4.0，数字才可比。

> 教训值得单独记：**benchmark 造假最常见的两种方式就是
> "数据分布不真实"和"缓存状态不声明"。**

### 归因改用 CPU 时间 + 磁盘读字节（与缓存状态无关）

| impl | 中位墙钟 | CPU 时间 | 磁盘读 | 说明 |
|---|---|---|---|---|
| parquet 全扫 | 1716 ms | **5578 ms（墙钟 325%）** | **2060 MB** | 并行解压，吃掉约 3.3 个核 |
| 侧车 mmap | 0.9 ~ 14.6 ms | 0 ms | 0 MB | 页缓存命中，连盘都不碰 |

- **CPU 时间 > 墙钟** ⇒ 它在多线程并行解压。这同时解释了"为什么它会拖慢别的环节"。
- 冷/热这种分法在本机**得不出可靠结论**（后台进程干扰，实测出现过冷盘更快）。
  所以诊断脚本里直接把这条写进结论，避免下次又有人去纠结"是磁盘还是 CPU"。

---

## 12.3 方案对比：为什么选"偏移数组 + mmap"

| 方案 | 可行性 | 结论 |
|---|---|---|
| parquet row group 级裁剪 | 100 个随机行号几乎会覆盖全部 5 个 row group，每个仍 179 MB | ❌ 收益有限 |
| 只取 top-5 而不是 pool-100 | 重排需要整个池子的正文，截了就掉指标（§11.13 已证明池子有价值） | ❌ 会伤质量 |
| 全量读进内存 dict | 2.9 GB 侧车 + 21.6 GB 索引 > 31.7 GB 内存上限 | ❌ 直接 OOM |
| SQLite | 可行，但多一层查询引擎/事务开销；且同样要写 2.9 GB | ⚠️ 需要事务/条件查询时再选它 |
| Arrow IPC + mmap | 可行，但列式拿"单行三字段"要按列拼接，逻辑绕 | ⚠️ 次选 |
| **偏移数组 + mmap（选中）** | 两次内存访问 + 一次切片，O(1)；26.5 MB 偏移即可导航 | ✅ 且能自检 |

**关键判断句**：
**体积没有变小（2.87 GB，和 parquet 的 2.21 GB 同量级），变的是布局。**
同样的字节数，"按行放"和"按列+行组放"的取数代价差 100 倍以上。

---

## 12.4 实现：`src/fetch_store.py`

**抽成独立模块**（不只是搬家）：它只依赖 pyarrow / numpy，**不依赖 faiss / torch** ——
这是"诊断脚本能零成本"的前提。原先它埋在 `generator.py` 里，
而 `generator.py` 一 import 就会拖起 `search_hybrid` → faiss/encoder。

| 类 | 作用 |
|---|---|
| `RowTextFetcher` | 基线：按行号定位分片 + isin 过滤（保留做 A/B 对照与兜底） |
| `BlobFetcher` | 侧车 + mmap，O(1) 点查 |
| `open_fetcher(mode)` | `auto`：有侧车就用；**没有就显式打印提示**再退回基线 |

侧车格式（刻意做得可读、可 `sed` 查看）：

```
data/index/fetch_store/chunks_text.jsonl      331.6 万行，每行一条 JSON（id/title/section/text）
data/index/fetch_store/chunks_text.off.npy    int64 偏移数组（N+1，末尾哨兵）
```

### 一致性四层防护（这是本步第二个值得记的部分）

1. **原子替换**：先写 `chunks_text.jsonl.tmp`，行数契约通过才 `replace` 改名 ——
   中途失败不会留下"半截但看着能用"的侧车。
2. **行数契约**：开侧车时 `侧车行数 == ids.txt 行数`，不等直接退出。
   （`build` 时也校验，不等就删掉 `.tmp` 拒绝交付。）
3. **抽样逐字段自检**：`--verify 300` 随机 300 行与 parquet 比
   `chunk_id / title / section / chunk_text` **四个字段**。
   只比 chunk_id 是不够的 —— 错位一行也能取到一个"合法"的 chunk_id。
4. **每次取回错位断言**：`payload["id"] == id_list[row]`，成本几微秒，
   但保证"契约破了会响"。

> 残留窗口已如实标注：内容与偏移是**两个文件**，两次 `replace` 之间仍有极小间隙，
> 没有引入构建号配对。兜底是上面第 2、4 条 —— **残留风险会被响亮地检测到**，
> 而不是静默错位。这才是底线。

---

## 12.5 验收

| 项 | 结果 |
|---|---|
| 构建 | 3,316,395 行 / 2.87 GB + 26.5 MB，**48 秒**（70k 行/s） |
| 行数契约 | ✅ 侧车 3,316,395 == ids.txt 3,316,395 |
| 抽样自检 | ✅ 300 行 **0 不一致**；同批两路 1307 ms vs **4.9 ms**（266×） |
| 微基准 | 1716 ms → **0.9 ms**（扎堆）/ **14.6 ms**（均匀随机） |
| 链路 A/B（25 题） | 取原文 **3069 → 21 ms（146×）**；合计 **6096 → 3150 ms** |

### 链路 A/B 明细（25 题、同一批问题、echo 后端、取第 2 题起算中位）

| 分项 | parquet | 侧车 | 倍数 |
|---|---|---|---|
| 向量检索 | 542 ms | 232 ms | **2.3×** |
| BM25 | 62 ms | 48 ms | 1.3× |
| **取原文** | **3069 ms** | **21 ms** | **146×** |
| 重排 | 2488 ms | 2828 ms | 0.9×（噪声） |
| **合计** | **6096 ms** | **3150 ms** | **1.9×** |

**顺带发现（标注为观察项，不当作结论）**：换掉全扫之后，**向量检索也快了 2.3×**。
推测是旧实现并行解压时抢 CPU、并把 2.2 GB 分片数据塞进页缓存，挤压了编码/检索。
两次运行有先后顺序，存在混杂因素，所以只作为**待验证的观察项**记录。

> ⚠️ 端到端 **4.78 s → 约 2.9 s 是外推**（把实测的 1.9 s 换成 0.02 s），
> 不是重跑全量 129 题的实测 —— 云 API 额度只剩 turbo，重跑的价值不抵成本。
> **外推必须标注，不然它就会变成"我测过"。**

---

## 12.6 坑清单（本步新增）

| 编号 | 坑 | 根因 / 处理 |
|---|---|---|
| 1 | **诊断第一版工作量构造错了** | "真实命中"按行号排序切连续段 → 落在同一分片 → 量出来快一倍，冷热差为负。改随机抽样 |
| 2 | **冷/热分法在本机不可靠** | 后台进程干扰，实测冷盘比热盘还快。改用 CPU 时间 + 磁盘读字节归因 |
| 3 | **Windows：mmap 会锁文件** | 重建侧车时若 Gradio 开着，`replace` 抛 `PermissionError`（附中文提示，指路"先关进程"） |
| 4 | **派生数据是"一对文件"** | 内容与偏移两次替换之间有空隙。先用行数契约 + 每次取回断言兜住，并如实标注 |
| 5 | **`evaluate.py` 的调用点会一起坏** | `get_fetcher` 签名改了（多了 parquet 与 mode），全仓库只有 1 处调用 —— 已同步 |
| 6 | **gradio 的耗时条用减法算取原文** | `retrieve_total − vector − bm25 − fuse` —— 一旦开重排就把重排算进取原文，且看不出错。改用 `timing['fetch']` 直接取 |
| 7 | **诊断脚本要能指定侧车路径** | 否则无法量"冷读"（把侧车复制一份换 inode）。→ 加 `--blob/--offs` |
| 8 | **侧车不入库** | 2.87 GB，`.gitignore` 已覆盖 `data/`。所以 `auto` 必须**显式提示**再退回基线 |

---

## 12.7 结论（本步）

| 问题 | 答案 |
|---|---|
| 最大延迟项是什么？ | **取原文**（1.9 s，端到端 40%）—— 不是检索，也不是模型 |
| 为什么这么慢？ | parquet 为全表扫设计：取 100 行要解压 2.2 GB；且 chunk_id 是随机哈希，**剪枝率为 0** |
| 压到什么程度？ | 微基准 1716 ms → 0.9~14.6 ms；链路内 3069 ms → **21 ms（146×）** |
| 整条链路省多少？ | 检索链路 6096 → **3150 ms**；端到端外推 4.78 s → **约 2.9 s** |
| 代价是什么？ | 磁盘多 2.87 GB + 派生数据要与索引同版本（靠契约强制） |
| 最值钱的可迁移经验？ | ① **体积不变，换布局就能快两个数量级**；② **派生物必须可校验**；③ **benchmark 的工作量构造错了，结论就是错的** |

---

## 12.8 复现命令

```bat
REM ① 关掉 Gradio（内存 + 文件锁）

REM ② 建侧车（48 秒）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\fetch_store.py --build

REM ③ 自检（必做）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\fetch_store.py --verify 300
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\fetch_store.py --info

REM ④ 诊断（零成本，1 分钟，不需要索引）
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\diagnose_fetch.py --impl parquet
"C:\Users\搞懂\AppData\Local\Programs\Python\Python313\python.exe" src\diagnose_fetch.py --impl blob --repeat 40

REM ⑤ 链路 A/B（25 题，echo 后端零 API 成本）
"C:\...\python.exe" src\generator.py --backend echo --testset eval\qa_testset_v1.jsonl ^
    --limit 25 --rerank --fetch-store parquet --tag _e2e_ab_parquet
"C:\...\python.exe" src\generator.py --backend echo --testset eval\qa_testset_v1.jsonl ^
    --limit 25 --rerank --fetch-store blob    --tag _e2e_ab_blob

REM ⑥ 正常使用（默认 auto 自动用侧车）
"C:\...\python.exe" src\generator.py --backend dashscope --testset eval\qa_testset_v1.jsonl --rerank
```

**产物**：`eval/results/fetch_store_build.log`、`fetch_diag_{parquet,blob}.log`、
`e2e_ab_{parquet,blob}.log`、`e2e_ab_summary.json`。

---

## 12.9 下一步候选（按性价比）

1. **重排的 2.8 s 现在是最大项** —— 但它"有效"（R@1 0.613 → 0.885），
   要压只能压"候选数/批大小/精度"（`rerank-pool 50` 已是最优），空间有限。
2. **生成 1.23 s** —— API 往返为主，本地路换模型可以省掉，但要先解决指令遵守率（66.9% 不标引用）。
3. **侧车再加一层"rerank 专用裁剪"** —— 重排只需 title|section|正文，可以考虑存一份更小的
   重排专用侧车（当前 870 B/行中 `id` 占 33 B，正文是大头，收益有限，优先级低）。
4. 评测集 v2：修 3 条表述瑕疵 + 1 条 gold 答案过粗（v1-f4e8ca）。
