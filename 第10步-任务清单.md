# 第 10 步 · 评测与消融（M4）

> 方案原话：**「学校不教、培训班不教、简历上几乎没人写。你写了，面试官会眼前一亮。」**
> 这一步的产出是**一张自己跑出来的消融对比表** —— M1~M5 里唯一能直接变成简历数字的东西。

---

## 10.0 本步要回答的问题（先明确，否则会做成"跑一堆指标"）

| 问题 | 用什么回答 |
|---|---|
| 检索到底行不行？行到什么程度？ | Recall@5 / MRR@10 / Hit Rate@K |
| 混合检索（向量+BM25）值不值？ | 检索模式消融：vector / bm25 / hybrid |
| reranker 值不值那 200ms？ | 重排开关消融 |
| 繁简归一化（第 9 步修的）值多少？ | 字形交叉题子集上的 Recall 对比 |
| 模型会不会胡说？ | Faithfulness + 拒答率 / 误拒率 |
| 云端 vs 本地 1.5B 差在哪？ | 生成模型消融（是准确率差，还是指令遵守差？） |

**没有第一行 gold 标注，以上全部无法回答。** 所以本步的第一块砖是评测集。

---

## 10.1 为什么评测集必须先做（而不是先加 reranker）

组内常见做法是"先把能优化的都加上，最后再评测"。**顺序反了**。

对 RAG 来说：
- 加 reranker 是**手段**，Recall@5 提升才是**结果**
- 没有 gold 标注，你只能说"感觉 reranker 好一点"—— 面试官追问"好多少"，答不上来
- 更致命的是：**如果评测集没有区分度，5 组消融会全部打平**（都 1.000），
  你花了三天时间做实验，得到一张全是等号的表

所以本步顺序：**评测集 → 评测脚本 → 5 组消融**。

---

## 10.2 采样设计（本步真正的技术含量）

### 题型配比 —— 人为构造难度梯度

| 题型 | 数量 | 构造方式 | 预期谁能答对 | 用来回答什么 |
|---|---|---|---|---|
| `easy` | 45 | 同字形提问，含实体名 | BM25 / 向量都行 | 基线：能不能搜到 |
| `hard_anon` | 35 | **实体匿名化**：禁止出现任何专有名词 | 只有语义检索能 | **混合检索到底值不值** |
| `hard_para` | 15 | 同义改写，换掉关键词 | 语义检索占优 | 词面匹配的脆弱性 |
| `t2s` / `s2t` | 15 | 繁体问→简体 gold，及反向 | 依赖归一化 | 繁简归一化值多少 |
| `oob` | 30 | 库内实体 + 库外属性 | 谁都不该答对 | 拒答能力（防幻觉） |

**如果全是 easy 题，5 组消融会全部打平 —— 这是评测集设计最常见的死法。**

`hard_anon` 想构造的是这一类：把「张克忠」换成
「有位天津出生、创办了南开大学化工系的化学工程学家」，问题里一个专有名词都没有 ——
指望 BM25 的倒排索引无从下手，**只有向量检索能靠语义召回**。
它被设计来回答「混合检索值不值」。

> 🔴 **这个预期在第 11 步被实测推翻，见【第11步-任务清单.md 的 11.5】。**
> 实测：匿名题的**词面重合率和 easy 题一样高**（0.661 vs 0.657），
> 结果是 **BM25 在匿名题上 0.812、向量只有 0.375**，方向和预期完全相反。
>
> 根因：**"删掉实体名" ≠ "降低词面重合"**。匿名化只删了人名/条目名，
> 而"创办、化学工程、籍贯、单瓣、复瓣、肉质"这类**描述性词汇本来就逐字来自原文**，
> 它们才是 BM25 的主要得分来源。问题反而因此变成了"60 字的多重约束"，
> 向量把它压成一个 1024 维向量时信息被稀释，比逐词精确匹配更吃亏。
>
> 教训：**题型设计必须用数据校验，不能靠"看起来对"**。此处保留了原始预期，
> 因为"预判错了"本身就是这份文档最该记的东西。

反过来也要防：不能全是难题，否则基线全丢、差距被夸大。

### 三条硬约束

| 约束 | 数据依据 | 不做的后果 |
|---|---|---|
| **过滤噪声段** | `外部連結` 5052 条 + `参见` 4436 条 | 生成"这篇条目有哪些外部链接"这种废题 |
| **一个条目只出一题** | 441,362 条目 / 829,100 chunk，平均每条目仅 1.9 个 | 检索命中一个 chunk 就算全中，指标虚高 |
| **库外题必须自动校验** | 第 9 步踩过：用「SpaceX 星舰」当库外题，语料里真有 | 测的不是防幻觉，是"运气" |

### 字形维度：实测，而不是预测

采样时会按比例预测 chunk 字形，但**长文本容易误判**（前 300 字繁体、后面全是数字表格 → 判成简体）。
所以生成完成后**用实际文本重新测**，标记 `script_cross`（问题字形 ≠ gold 字形）。

这样"繁简归一化值多少"就是一个可以单独统计的子集，而不是混在总体里的一笔糊涂账。

---

## 10.3 踩到的坑（三条，都值得记）

### 坑 A：`section` 名过滤挡不住「列表型内容」

**现象**：过滤了 `外部連結`/`参见` 等噪声段名后，采样出来的 chunk 里仍有
```
* 東京巨蛋城
** 東京巨蛋
** 東京巨蛋城遊樂園
```
和 64 个字全是 `* 慈眼堂 / * 御野島 / * 中之島 …` 的条目。

**根因**：问题出在**内容格式**，不在 section 名上 —— 那条的 section 叫「車站構造」，看着完全正常。

**修法**：加 `is_listy()`，两条判据命中任一即剔除：
① 以 `*` / `:` / `|` 开头的行占比 ≥ 35%
② 行数 ≥ 5 且「12 字以内的短行」占比 ≥ 60%

### 坑 B：`生于` 两个字，把植物全变成了人

**现象**：库外题生成了「**中华淡竹叶**有血型吗？」「**曹州**是在哪家医院出生的呀？」「北宋的**置將法**它有血型吗？」

**根因**：人物判据里用了关键词 `生于` —— 而**植物志的固定句式正是「生于山坡草地/海拔 800 米」**。
一个字的宽泛匹配，把整个植物界划进了人物池。

**修法**（两层）：
1. `出生于` 比 `生于` 精确得多 —— 去掉后者
2. 启发式**天然不可靠**（「置將法」是法规、「曹州」是地名，改字表就是打地鼠）
   → 加一层 **LLM 确认**：一次给 45 个条目名让模型挑真人，成本比逐条判低两个数量级

**教训**：跨领域的关键词匹配必然误伤。启发式用来粗筛可以，**最终判定要交给模型或人**。

### 坑 C：FlagModel 首次加载不是线程安全的

**现象**：库外题校验用 4 线程并发，4 条里 3 条报
`AttributeError: 'list' object has no attribute 'keys'`。
日志里 `[模型加载] FlagModel bge-large-zh` 出现了 **3 次**。
**串行跑同样的查询 100% 正常**。

**根因**：多个线程同时触发**首次** encode，模型加载过程中的内部状态被并发修改 ——
典型的一次性竞态（加载完后就安全了，所以不容易复现）。

**修法**：进并发前先单线程"预热"一次 `searcher._encode(["预热"])`。

**教训**：这类 bug 的特点是**只在新进程的第一次并发时出现**，
所以"我本地跑没问题"往往是"我已经跑过一次了"。

---

## 10.4 库外题的两层校验

库外题的设计是「**库内实体 + 库外属性**」（问「张克忠的血型」）——
这样测的才是"模型明知这个实体，也不许编"。

但它有两个失败模式，各需一层校验：

| 层 | 校验什么 | 手段 | 实测效果 |
|---|---|---|---|
| 1 | 实体是不是**真人** | LLM 批量挑（`confirm_persons`） | 挡住「飓风的血型」「法规的血型」这类荒谬题 |
| 2 | 这个属性是不是**真的不在库里** | 检索 top-5 → 交给裁判模型判"能否回答" | 试跑 6 条中挡下 **1 条**（约 17%，与方案预估的三成同量级） |

⚠️ **属性选择纪律**：不用隐私/违法类（银行卡号、身份证号）。
因为模型拒答可能出于"这是隐私不能说"，而不是"资料里没有" ——
两种拒答混在一起，**拒答率的指标就脏了**。

---

## 10.5 实测数据

### 采样（扫 2 个分片，1,658,199 条 chunk）

| 环节 | 数量 | 占扫描量 |
|---|---|---|
| 剔除 噪声段（section 名） | 35,994 | 2.2% |
| 剔除 长度不合规（<150 或 >800 字） | 572,262 | 34.5% |
| 剔除 **列表型内容**（坑 A） | **262,300** | **15.8%** |
| 条目去重后可用 | **402,713** | — |
| 其中 简体条目 / 繁体条目 | 163,394 / 239,319 | 繁体 59.4%（口径见下） |
| 疑似人物条目（库外题来源） | 29,048 | — |

> ⚠️ **口径陷阱**：这里 59.4% 与前面「47.3% 的 chunk 含繁体特征字」**不是同一个指标**，不能直接比。
> 47.3% 用的是粗筛（只查 30 个繁体特征字、要求命中 ≥3 个）；这里用 zhconv 全字表 + 2% 字符阈值，
> 检测更全、阈值更低，必然更高。**我第一次就是把两者写成"两条一致，说明采样没偏"—— 那是错的。**
>
> 列表型内容占 **15.8%**，说明坑 A 不是边缘情况：**每 6 条 chunk 就有 1 条是列表/导航**，
> 不做这一步过滤，七分之一的题会变成废题。

### 生成（140 条候选，耗时 80 秒）

| 项 | 结果 |
|---|---|
| 库内题 | 110 条（easy 45 / anon 35 / para 15 / cross 15） |
| 库外题 | 30 条 |
| **字形交叉题** | **52 条（47%）** —— 与语料繁体占比同量级，说明繁简是**普遍问题**，不是边缘情况 |
| 平均泄漏率 | easy 0.26 / anon 0.24 / para 0.15（都很低 → 问题不是抄原文） |

### 预筛效果（判据改进前后）

| 判据版本 | 通过 | 说明 |
|---|---|---|
| 初版（答案须为原文**连续子串**） | 99/140 | 误报 9 条 —— LLM 提取答案是**重组**的，子串判据太严 |
| 改后（答案**字符覆盖率** ≥ 0.85） | **131/140** | +32 条；剩下的 4 条是**真问题** |

**最终废题率 4/140 = 2.9%**（方案预估「自动生成的 100 条里约三成是废的」，
本脚本的预筛把它压到了三十分之一）。

剩余 4 条真警告（人工删除即可）：

| 类型 | 问题 | 警告 |
|---|---|---|
| hard_anon | 某电视剧首播时间 | 泄漏率 0.55（改写不够） |
| hard_anon | 某声优的家庭排行 | 答案覆盖率 0.50（概括过度） |
| hard_anon | 某作品剧情 | 覆盖率 0.00 **＋ 匿名化失败**（标题词「炎之刻印」出现） |
| hard_para | 浙大学生宣言 | 泄漏率 0.57（改写不够） |

### 库外题校验

| 结果 | 条数 |
|---|---|
| ✅ 确认库外（可用） | 25 |
| ❌ 其实在库内（剔除） | 4 |
| ? 未判定（需人工看） | 1 |

另有 LLM 人物确认环节：启发式筛出 60 个候选 → LLM 判为真人 **50 个**（83%），
即**启发式的误判率约 17%**（把法规、地名当成了人）。

---

## 10.6 下一步

1. **人工校对** —— 但可以**从"逐条"降级为"抽检"**：
   方案原话是"人工逐条校对（自动生成的三成是废的）"，
   而本脚本的预筛已把废题率压到 2.9%，所以合理做法是
   **全量看那 4 条警告 + 从 131 条合格里随机抽 30 条复核**。
   ⚠️ 这个改动要在文档里写明，不能悄悄降标准。
2. 写 `src/evaluate.py`：Recall@5 / MRR@10 / Hit Rate@K
3. 跑 5 组消融（含 reranker 开关、字形交叉子集）

---

## 10.7 简历话术（方案给定，等数字填进去）

> 构建 100 条人工校对的中文 RAG 评测集，设计 5 组消融实验（切块粒度、检索策略、重排开关、生成模型），
> 定位出 reranker 对 Recall@5 贡献最大（+X%）、chunk=512 优于 1024；
> 通过元数据过滤 + 拒答机制将库外问题误答率降至 X%。

**这一句里的每个数字，都来自下面这张还没跑出来的表。**
