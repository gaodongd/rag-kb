# 第 9 步 · 生成与引用（M3）

> M2 让系统"**找得到**"，这一步让它"**答得出**"。
> 检索出 top-5 只是中间产物 —— RAG 的交付物是**带依据的回答**，不是一堆原文片段。

## 规格（来自 `RAG项目-技术方案.md` · M3）

**任务**
1. `src/generator.py`，双路 LLM：
   - A 路：云端 API（`DASHSCOPE_API_KEY`），质量基线
   - B 路：本地 `Qwen2.5-1.5B` 4bit，成本为零的对照
2. Prompt 要点（这几条是拉开质量的，不是调参）：
   - 上下文带编号 `[1] [2]`，要求答案里标注引用来源
   - **明确拒答条款**：上下文中找不到依据时必须回答"根据已有资料无法回答"，不许编
   - 要求先给结论再给依据
3. `app/gradio_app.py`：输入框 + 答案区 + 可折叠的引用原文 + 检索/生成耗时显示

**验收**：问 10 个知识库内问题能答且带引用；问 3 个知识库外问题能拒答。

---

## 三个设计点（这一步真正值钱的地方）

### 1. 上下文编号 `[1] [2] …`

没有编号，模型只能说"根据资料"—— 你**无法验证它到底用了哪条**。
而"每个事实可追溯到原文"恰恰是 RAG 相对纯 LLM 唯一的硬优势。

编号把引用从"模糊的语言描述"变成了**离散符号选择**任务，模型错误率立刻降一个数量级；
同时它让"可验证"从一句口号变成了可以自动打分的东西（M4 的引用准确率指标就靠它）。

### 2. 拒答条款 —— 幻觉率的最大单点改进

LLM 的默认行为是**尽力回答**。不给拒答指令，库外问题会被自信地胡说，
而且胡说的内容外面还包着一层"参考资料里说"的皮 —— **比纯幻觉更危险**，
因为它借用了系统的可信度。

成本一行 prompt，收益贯穿整个 M4。

### 3. 先结论后依据

让输出结构可预测，M4 做自动评测时才好抽取（否则每条答案格式都不一样，
连"哪句是结论"都要靠 LLM 再判一次，评测本身就引入了噪声）。

---

## 开发任务

### [x] T1 · 环境预检（动手前先量，不猜）

| 检查项 | 结果 |
|---|---|
| `DASHSCOPE_API_KEY` | ✅ 已配置 |
| `bge-large-zh-v1.5` / BM25 / faiss 索引 | ✅ 齐备（第 8 步产物） |
| `app/` 目录 | ❌ 空的 —— 界面一个字没写 |
| 本地 `Qwen2.5-1.5B` | ❌ **未下载**（ms-cache 里只有 Math 版） |
| `gradio` | ✅ 6.27.0（注意 `allow_flagging` 已被删除） |

> 📌 **预检抓到的第一件事**：`download_models.py` 里配的本地模型 ID 是
> `Qwen/Qwen2.5-1.5B`（**base 版**）。必须换成 `Qwen2.5-1.5B-Instruct` ——
> 理由见 T7。

### [x] T2 · `src/generator.py` + prompt 三要素

**结构**：三个后端（`echo` / `dashscope` / `local`），共用同一套 prompt 与检索逻辑。

`echo` 后端是本步的"意外实用工具"：**不调任何模型，只把组装好的 prompt 打出来**。
别小看它 —— prompt 里的问题（编号错位、上下文被截断、section 混进一坨噪声）
在 echo 下一眼就能看出来；而调 LLM 时你只会看到"答案好像不太对"，很难定位是哪一环错了。

**云端后端用 openai SDK 而非 dashscope SDK**：兼容端点是标准协议，
以后换任何一家（DeepSeek / 智谱 / 本地 vLLM）都只改 `base_url`，业务代码一行不动。

**踩的坑（自己挖的）**：`echo` 模式第一版把回显的 prompt 也交给核查逻辑判定，
结果 prompt 里天然带着 `[1]…[5]` 和那句拒答话术 → 输出「引用了全部编号 + 判定拒答」。
**这个假结果的害处是它看起来像核查逻辑坏了，实际是喂错了输入。**
修法：给后端加 `is_model` 标记，echo 直接跳过核查。

### [x] T3 · 引用核查 —— 三种能自动发现的失败模式

| 检查 | 判据 | 为什么必须有 |
|---|---|---|
| **非法编号** | 答案里出现 `[7]`，而上下文只有 5 条 | **最难肉眼发现的幻觉**：答案读起来完全合理、编号看着也很专业，实际在编 |
| **无引用标注** | 有答案、没拒答、也没有任何 `[n]` | 说明模型在自由发挥，没有基于上下文 |
| **拒答** | 命中拒答话术 | 库内问题被拒答 = 误拒，是 M4 要量化的核心指标 |

> 🔴 **抓到的真 bug**：拒答话术只匹配了简体（"无法回答"），
> 而**语料是繁体**，模型跟着用繁体回答「**無法**回答」→ Q3 明明拒答了，判定却是「拒答 0 条」。
> 这类错误最阴的地方是：程序不报错、指标也"正常"，**只是数字是错的**。
> 修法：繁简两种写法全列上。

### [x] T4 · 冒烟验收：库内 10 + 库外 3

问题集落在 `eval/m3_in_kb.txt` 和 `eval/m3_out_kb.txt`。

**库内 10 条**（覆盖专名 / 人名 / 机构 / 科学概念 / 地理）：

| # | 问题 | 检索 top1 | 引用 | 结果 |
|---|---|---|---|---|
| 1 | 台灣東部開發於古時的人行道路 | 蘇花古道（vec#42 bm25#1） | [1] | ✅ 教科书级案例，见下 |
| 2 | 中國的化學工程學家 | 张克忠（vec#1） | [1][3] | ✅ |
| 3 | 蘇花古道的起點和終點分別在哪裡 | 蘇花古道 | [1] | ✅「起自蘇澳、終於花蓮」 |
| 4 | 東寧路是哪個城市的道路 | 東寧路 (臺南市) | [1] | ✅ |
| 5 | 張克忠的籍貫是哪裡 | ~~哈辛·耶巴達 ❌~~ → **张克忠** ✅（修复后） | [2] | ~~误拒~~ → **答「天津」** ✅ |
| 6 | 八田數是以誰的名字命名的 | 卡羅琳·赫歇爾（但 #2 命中） | [2] | ✅ |
| 7 | 余国琮曾任哪些職務 | 余国琮 | [1][2][4] | ✅ 职务列得很全 |
| 8 | 大別山屬於什麼氣候類型 | 大别山·气候 | [1] | ✅ |
| 9 | 溫帶海洋性氣候有什麼特徵 | ~~美国地理~~ → **温带海洋性气候** ✅（修复后） | [1][4] | ✅ |
| 10 | 青梅市的地形有什麼特點 | 青梅市·地理 | [1] | ✅ |

**汇总：繁简修复并重建索引后，10 条全部正确、0 条非法编号。**

> 🎯 修复带来的不只是"错的那条变对了"，还有一条**原本没被发现的问题**：
> 第 9 题「溫帶海洋性氣候有什麼特徵」原来检索 top1 是「美国地理」（靠 top-5 里的正确条目勉强答对），
> 归一化后 top1 直接是「温带海洋性气候」——「溫帶」→「温带」让 BM25 的字面匹配生效了。
> **这说明繁简问题的影响面比"那一条误拒"大得多**，只是其他题被 top-5 兜住了没暴露。

🎯 **Q1 是第 8 步那个案例的完整闭环**：
向量把「蘇花古道」排到 **#42**、把错误的「東寧路」排 #1；BM25 把「蘇花古道」排 **#1**。
RRF 融合后蘇花古道回到 **#1** → 模型答出「是蘇花古道[1]」。
**第 8 步用排名证明混合有价值，第 9 步用最终答案把它兑现了。**

**库外 3 条**（方法见下）：

| 问题 | 结果 |
|---|---|
| 张克忠的血型是什么 | ✅ 拒答（且列出 [1][2] 说明"资料未提及血型"） |
| 蘇花古道的門票價格是多少 | ⚠️ 未硬拒答，但也没编：「蘇花古道不收取門票費用…並無任何關於門票收費的記載」 |
| 余国琮的配偶叫什麼名字 | ✅ 拒答，且主动区分「资料[4]提到的是余程萬的妻妾子女，与余国琮无关」 |

**零幻觉**。第 2 条严格说没拒答，但它给了**合理推断 + 明确的免责说明**，
比硬拒答更友好 —— 这说明"拒答"应该是**三档**而不是二值：
`拒答 / 推断+免责 / 编造`。M4 的判定口径要按这个改。

> 🔴 **库外问题集第一版设计错了，值得记下来**：
> 当时用的是「SpaceX星舰第五次试飞」「iPhone 17 Pro 芯片」——
> 逻辑是"这些是 dump 之后的新事，肯定查不到"。
> **结果两条都命中了语料**（连「Apple A19」条目都有）—— dump 的实际时间边界比印象中新。
> → 教训：**设计库外问题前必须先确认语料的真实时间边界**，不能靠印象。
>
> **正确做法**：选语料里**真实存在的实体**，问一个语料里**必然没有的属性**。
> 问「2026 年诺贝尔奖得主」测的是"这东西不存在"（模型本来就没知识，拒答是必然）；
> 问「张克忠的血型」测的才是"**模型明知也不许用**"—— 后者才真正验证了拒答条款。

### [x] T5 · 繁简归一化（本步抓到的最大 bug）

**症状**：Q5「張克忠的籍貫是哪裡」误拒，但 Q2 里张克忠明明检索得到。

**对照实验**（只差 4 个字）：

| query | 向量排名 | BM25 排名 | 检索 top1 | 结果 |
|---|---|---|---|---|
| 「**张**克忠的籍**贯**是**哪里**」（简体） | vec#5 | bm25#1 | **张克忠** ✅ | 正确答"天津" |
| 「**張**克忠的籍**貫**是**哪裡**」（繁体） | 未进 top100 | bm25#1（错条目） | 哈辛·耶巴達 ❌ | 误拒 |

**根因有两层，第二层更隐蔽**：

1. **字面层**：繁体「張克忠」在语料里（简体「张克忠」）**查不到**，df=0，这个词等于不存在。
2. **分词层**：jieba 词典以简体为主，繁体串会被切碎成单字，
   而单字在第 8 步就被 `_keep` 过滤掉了（那是正确的决定：单字全是噪声）。
   → **query 里唯一有信息量的词，在进入检索之前就消失了。**
   就算把「張」加进 jieba 词典，只要它被切成单字，照样被丢。

**修法**：`src/zh.py` 提供唯一的 `norm_text()`，**建索引和查询共用同一个函数**。

- ⚠️ 只转一边 = 换了一种字形的字面不匹配，**比不转更糟**（而且静默查不到、不报错）。
  这就是第 8 步 §5.4「检测口径必须 == 修复口径」同一条纪律。
- ⚠️ 归一化状态写进 `meta.json`，`load_bm25` 启动时校验 ——
  否则「新代码 + 旧索引」也能跑起来，表现只是"某些词永远搜不到"，一行报错都没有。

**验证**（先验纯函数，比跑 20 分钟索引划算）：

```
「張克忠的籍貫是哪裡」→ ['张克忠', '籍贯', '哪里']
「张克忠的籍贯是哪里」→ ['张克忠', '籍贯', '哪里']   对称 ✅

「台灣東部開發於古時的人行道路」→ ['台湾','东部','开发','古时','人行道']
（归一化后 jieba 甚至切出了「人行道」这个正确词）
```

**复测**（BM25 全量重建：23.0 分钟 / 1,919,820 词 / 190,919,844 postings / 1192 MB / 自检通过）：

| query | 修复前 | 修复后 |
|---|---|---|
| 「張克忠的籍貫是哪裡」（繁体） | top1 = 哈辛·耶巴達 ❌ 误拒 | **top1 = 张克忠 ✅ 答「天津」** |
| 「溫帶海洋性氣候有什麼特徵」 | top1 = 美国地理（靠 top-5 兜住才答对） | **top1 = 温带海洋性气候 ✅** |

→ 库内 10 条从 **9/10 → 10/10**，非法编号仍为 0。

🎯 **修复的价值比"那一条误拒"大**：第二行说明繁简问题其余题也受影响，
只是被 top-5 兜住了没暴露 —— 检索质量提升但指标看不出来，
这种"沉默的改善"只有做对照实验才看得见。

### [x] T6 · `app/gradio_app.py`

**界面只做四件事，每一件都有用途，不是装饰**：
1. 答案（带 `[n]` 标注）
2. **引用原文（可折叠）** ← 让答案**可验证**，这是 RAG 的硬优势
3. 耗时分解（检索 / 向量 / BM25 / 融合 / 取原文 / 生成）
4. 「← 被引用」标记，一眼看出答案真正用了哪几条

**设计约束：界面必须是薄薄一层。** 检索、prompt、核查全部 import 自 `src/`。
演示界面最常见的腐化方式是"为了快点出效果先抄一份检索代码"，
然后两份代码各自演化，某天发现 demo 的表现和评测脚本对不上，却查不出哪边是对的。

**踩的坑**：`sys.path.insert(0, HERE)` 插的是文件所在的 `app/`，
而 `generator` 在 `src/` → `ModuleNotFoundError`。要插 `PROJECT / "src"`。

### [x] T7 · 本地 Qwen2.5-1.5B 一路

**必须用 Instruct 版，不是 base 版**：M3 要求模型「按 [n] 标注引用」「找不到依据就拒答」——
这些是**指令跟随**能力。base 版只会顺着上下文往下续写，不会拒绝，也不会标引用，
而且它没有 chat template，`apply_chat_template` 会直接抛错。

**实测**（`_m3_local_probe.py`，不经过检索链路，单独钉死这个变量）：

| 项 | 值 |
|---|---|
| 加载耗时 | 26.3 秒（+ 分词器共 59.1 秒） |
| 显存占用 | **1.07 GB**（4bit nf4 + double quant） |
| 库内题 | 「蘇花古道的起點在蘇澳，終點在花蓮。」✅ 内容对，**但没标 [1]** |
| 库外题 | 「根据已有资料无法回答。」✅ 完美拒答 |

🎯 **本步最有价值的实测发现之一**：本地 1.5B **能答对、能拒答，但标不稳引用编号**。
云端 qwen-plus 则是稳定标注。这正是 M4 要量化的东西 ——
「生成模型」这个消融维度上，差的不是准确率，而是**引用遵守率**。

---

## 关键决策记录

| 决策 | 选择 | 理由 |
|---|---|---|
| 生成后端抽象 | openai 兼容协议 | 换供应商只改 `base_url`，业务代码不动 |
| 温度 | **0（贪心）** | 评测要可复现；RAG 不需要创意 |
| 上下文条数 | top-5 | 与第 8 步延迟结论对齐；更多条会稀释注意力 |
| 拒答话术 | 逐字规定「根据已有资料无法回答」 | 给模型自由裁量空间 = 它会把"知道一点点"也算作"知道"；**逐字话术才能自动判定** |
| 繁简归一化方向 | 统一到**简体** | jieba 词典与本机工具链都以简体为主，分词质量更稳 |
| 本地模型 | Instruct 而非 base | 见 T7 |

---

## 遗留（诚实清单）

| 项 | 说明 |
|---|---|
| **检索延迟受内存压力影响大** | 13.6 GB faiss 常驻后 page cache 被挤占，实测向量检索 776~3166 ms 抖动（空载基准 249 ms）。取原文（`fetch_texts` 扫 2.17 GB 分片）另需 1.2~2.9 s。端到端因此落在 3~10 s 而非预期的 2 s 内 |
| ~~**`fetch_texts` 是隐形大头**~~ → **已优化** | 原来 1.2~2.9 s（比向量检索还慢）。根因是**真扫 4 个分片做字符串 `isin`**（不是 dataset 重建 —— 实测重建只要 1 ms）。已改为 `RowTextFetcher` **按行号定位分片**，实测 **1.8x**。⚠️ **不是预期的 4x** —— top-5 的行号通常散落在多个分片（5 个随机点覆盖 4 片的期望是 **3.05 片**），所以只省掉约 1/4 的扫描。这算"改对了但没改多少"，如实记录 |
| **生成本身没有量化评测** | 靠人工读 13 条判对错，没有 Faithfulness / 引用准确率的自动指标，也没有 gold 标注 —— M4 的核心任务 |
| **评测集仍是 3 条 query** | 第 9 步的 13 条是验收问题，不是带 gold 的评测集。融合策略的结论依旧不能外推 |
| **未加 reranker** | 第 8 步就列为遗留。加 cross-encoder 精排 top-50 比继续调融合参数更根本 |
| ~~**废弃索引占 6.88 GB**~~ → **已清理** | `data/index/` 下 `faiss_ivf_sq8.index`(3.44 GB) 和 `faiss_ivf_sq8_l2.index`(3.44 GB) 已于 **2026-09-19** 删除，释放 6.88 GB。删前确认 Flat 索引完好、`probe_two_stage.py` 默认索引改指 Flat；删后冷启动复验通过（详见第 8 步清单"删除后复验"） |
| **`bm25_old_normoff/` 1.2 GB 仍保留** | 归一化前的旧索引（词表 309 万 vs 新 192 万）。复测已稳定，可随时删；保留是为方便对比"归一化到底改了什么" |

---

## 实测数据汇总（2026-09-19 复测，14 条问题一次跑完）

### 验收

| 组 | 条数 | 结果 |
|---|---|---|
| 库内（含 1 条繁体对照） | 11 | **11 条全部正确**、0 条非法编号 |
| └ 其中 `eval/m3_in_kb.txt` 原 10 条 | 10 | **10/10**（修复繁简前是 9/10） |
| 库外 | 3 | **零幻觉**：2 条明确拒答 + 1 条「合理推断 + 明确免责」 |
| **合计** | **14** | 拒答 2 · 非法编号 **0** · 无引用且无拒答 **0** |

### 延迟

| 环节 | 复测区间 | 复测中位 | 优化前 |
|---|---|---|---|
| **检索总计** | 1534 ~ 3195 ms | **~2280 ms** | 2378 ~ 4999 ms（中位 ~3220） |
| └ 向量（faiss nprobe=512） | — | — | 776 ~ 3166 ms |
| └ BM25 | — | — | 40 ~ 752 ms |
| └ RRF 融合 | — | — | 0.2 ms |
| └ 回查原文 | — | — | 1250 ~ 2855 ms |
| 生成（qwen-plus） | 1.96 ~ 12.22 s | ~3.9 s | — |
| 端到端 | 4.45 ~ 28.20 s | ~6.3 s | — |

→ 检索中位从 ~3220 ms 降到 ~2280 ms，**主要来自 `RowTextFetcher` 的 1.8x**。
（首条 28.20 s 含 faiss 索引加载；其余已剔除。）

### 索引重建（繁简归一化）

| 项 | 初版 | 归一化重跑 |
|---|---|---|
| 词表 | 3,092,039 | **1,919,820**（**−38%**，繁简合并） |
| postings | 191,647,819 | 190,919,844 |
| 索引体积 | 1219 MB | 1192 MB |
| 耗时 | 19.9 分钟 | 23.0 分钟 |
| 自检 | 通过 | **通过**（每词 postings 数 == df） |

**词表掉 38% 而 postings 几乎不动** —— 说明合并掉的是**低频繁体异形词**，
高频词本来就以简体为主。这个对比本身就是"语料繁简混排"的量化证据。

### 关键文件

- `eval/results/gen_dashscope_recheck.jsonl` / `.log` —— 本次复测完整记录
- `eval/results/gen_dashscope_in_kb.jsonl` —— 修复前的库内 10 条（留作对照）
- `eval/results/gen_dashscope_out_kb_v1_newer_than_expected.jsonl` —— 第一版错误的库外问题集（留档）
- `eval/results/bm25_zh_norm.log` —— 索引重建日志
- `eval/results/dl_m3.log` —— 模型下载日志
