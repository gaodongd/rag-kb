# 第 8 步 · 检索器（向量 / BM25 / 混合）

> M2 阶段第二段。第 7 步已经把 331.6 万块编成向量，这一步把它变成**能查的东西**：
> 一句话问进去 → 返回最相关的几块原文。

## 规格（来自操作手册）

第 8 步  检索器（向量 / BM25 / 混合） ← M2

技术选型（M2 已定）：**向量 / BM25 / 混合三模式**，云端 API 与本地 Qwen2.5-1.5B 双路对照留到 M3。

## 三路检索的分工

| 模式 | 原理 | 强在哪 | 弱在哪 |
|---|---|---|---|
| **向量** | 双塔编码 → 内积（向量已 L2 归一化，内积==余弦） | 语义近似：问「这个地方气候怎么样」能命中「气候」小节 | 精确词面：人名/编号/生僻专名容易漏 |
| **BM25** | 词频 × IDF 的倒排索引 | 精确词面：专有名词、术语、编号一把就中 | 不会同义扩展：「气候」问不到「气温」 |
| **混合** | RRF 融合两路排名 | 互补兜底，工业界默认姿势 | 需要调融合参数，且两路都得先跑通 |

**为什么要做混合而不是只做向量**：这两路的失败模式**不重叠**。
向量漏的是「没见过这个词」，BM25 漏的是「说法不一样」。融合后召回率才有明显提升 ——
这个结论要用第 8 步的冒烟数据说话，不是拍脑袋。

---

## 开发任务

### [x] T5 · 向量检索器 `src\search_vector.py` —— **完成，但延迟炸了（这是本步最有价值的部分）**

**做什么**：读 `emb.npy`(memmap) + `ids.txt` → 编码 query → 分块内积 → top-k → 回查原文

**关键约束**（已实现）：
- **必须分块**。`np.asarray(emb, dtype=np.float32)` 会把 6.79GB fp16 撑成 **13.6GB** fp32，
  加上 memmap 本身，20GB 起步 —— 分块 20 万行，峰值临时内存 ~820MB。
- **回查原文只查命中的那几条**（不要像 `smoke_search.py` 那样 `isin(全部 331.6 万个 id)`，会卡死）。
- 顺序纪律：**按 `ids.txt` 重建**（`pyarrow` 的 `isin` 不保证返回顺序，见 §7 坑 D）。

**验收标准（实测）**：

- [x] 3 个语义 query 语义相关 —— 3/3
- [ ] **单次查询延迟 < 2 秒** → 🔴 **实测 8.2~15.9 秒，超标 8 倍**
- [x] 延迟集中在「扫全量」而非「排序」（topk 从 1 到 50 几乎不改延迟）
- [x] 输出带 `score / chunk_id / title / section / 正文片段`

**实测**：全量 331.6 万，分块 20 万，平均 **10.83 s** / 最大 15.92 s。

⚠️ **Q1 出现真实失败案例**（正是后面证明混合检索价值的证据）：
「台灣東部開發於古時的人行道路」top1 = 「**東寧路 (臺南市)**」（0.6803，讲台南市区道路，**不相关**），
top2/top3 才是「台灣古道」。**小样本时 top1 是「蘇花古道」—— 全量数据把它盖掉了。**

**产出**：`eval\results\search_vector.log`

---

### [x] T5.5 · 定位瓶颈 + faiss IVF —— **完成，结论反直觉**

**第一步：先测，别猜**（`src\bench_search_io.py`）

| 环节 | 耗时 | 结论 |
|---|---|---|
| 纯内存 fp32 内积（全量） | **263 ms** | 数据在内存 + dtype 对 → 本来可以很快 |
| mmap + fp16→fp32 分块扫 | **8.2 s** | **31 倍差距** |
| 冷扫 vs 热扫 | 差 5% | **不是** page cache / 磁盘 IO |
| 磁盘顺序读 | 587 MB/s | 盘也不算慢 |

→ **瓶颈 = 每次查询都要做 3.4e9 次 fp16→fp32 转换**（numpy 约 4e8 元素/秒）。
  "IO 慢"是最容易脱口而出的错误答案，实测把它排除了。

**第二步：建 faiss IVF**（`src\build_faiss_index.py`）

`data\index\faiss_ivf_sq8.index` —— 3.44 GB，SQ8 量化，nlist=4096，训练 300k 随机样本。

| nprobe | 延迟 | recall@10 | 扫描比例 |
|---|---|---|---|
| 1 | 2.6 ms | 0.48 | 0.0% |
| 32 | 51 ms | 0.87 | 0.8% |
| 128 | 196 ms | 0.96 | 3.1% |
| **4096（全扫）** | 5.5 s | **0.98** | 100% |
| 暴力精确（对照） | 8,051 ms | **1.000** | 100% |

🔴 **全扫也只有 0.98 → SQ8 量化本身吃掉 2%，这是硬上限**，不是调 nprobe 能救的。

**第三步：我的假设被数据否定（如实记录）**

我以为「向量已归一化时 L2 与 IP 排序等价，但 L2 的 k-means 更稳 → recall 更高」，
专门建了 `faiss_ivf_sq8_l2.index` 做 A/B：

| nprobe | IP | L2 |
|---|---|---|
| 1 | 0.480 | **0.600** |
| 4 | 0.760 | 0.750 |
| 32 | 0.870 | 0.850 |
| 64 | 0.930 | 0.870 |
| 128 | 0.950 | **0.960** |
| 4096 | 0.980 | 0.980 |

**没有系统性优势。假设不成立。**

**第四步：两阶段检索**（`src\probe_two_stage.py`）

IVF 召回 1000 候选 → 用 **fp16 原始向量**精确重排 top-10：

| nprobe | 召回 | 重排 | 合计 | 候选覆盖率 = 最终 recall |
|---|---|---|---|---|
| 16 | 4.2 ms | 3.6 ms | **7.8 ms** | 0.83 |
| 32 | 7.3 ms | 3.7 ms | **11.0 ms** | 0.89 |
| 64 | 13.9 ms | 3.7 ms | **17.6 ms** | 0.95 |

重排只要 3.6 ms，且**候选覆盖率 == 最终 recall**（重排是精确的，真答案只要进池就一定找得到）
→ **瓶颈 100% 在 IVF 召回，不在重排。**

**第五步：结论（数据支撑）**

既然 IVF-SQ8 在 200 ms 档位只有 0.96，而精确暴力只要 263 ms 就能拿 1.000 ——
**对 RAG 场景（recall 远比延迟重要），引入 faiss 在这个规模上是负收益。**

| 方案 | 延迟 | recall | 内存 | 定位 |
|---|---|---|---|---|
| **精确暴力（fp32 常驻）** | 263 ms | **1.000** | 13.6 GB | ✅ **RAG 主力** |
| faiss IVF-SQ8 两阶段 | 17.6 ms | 0.950 | 3.44 GB | 高 QPS / 内存紧张时 |
| faiss 纯 IVF | 2.6 ms | 0.480 | 3.44 GB | 只适合粗筛 |

> 331.6 万这个规模，暴力检索完全跑得动 —— **"向量库是必需品"是错觉**。
> 只有到了千万级以上、或要扛高并发，IVF/HNSW 的收益才盖过它的召回损失。

**第六步：换掉量化方式 —— 上面「负收益」的结论被推翻**

上面那个"faiss 是负收益"的判断，其实隐含了一个**我没验证的前提**：
`--quantizer` 我只跑了默认的 `sq8`，`flat` 那个选项写在代码里却从没执行过。

补跑 `faiss_ivf_flat.index`（**IndexIVFFlat，不做标量量化**，13.6 GB）后：

| nprobe | 延迟 | recall@10 | 扫描比例 |
|---|---|---|---|
| 64 | 34.6 ms | 0.950 | 1.6% |
| 128 | 60.5 ms | 0.970 | 3.1% |
| **512** | **249 ms** | **1.000** | 12.5% |
| 1024 | 463 ms | 1.000 | 25% |
| 4096（全扫） | 1676 ms | 1.000 | 100% |
| 暴力（对照） | 8217 ms | 1.000 | 100% |

🔴 **同一个库、同一个 nlist、同一个 nprobe 语义，只因为量化方式不同，结论完全反转：**

| | SQ8 | Flat |
|---|---|---|
| recall 上限 | **0.980**（全扫也只有这么多） | **1.000** |
| 拿 1.000 的代价 | **拿不到** | nprobe=512 → 249 ms |
| 索引体积 | 3.44 GB | 13.6 GB |

→ **SQ8 的 2% 损失是硬失真，调 nprobe 救不回来**（全扫也没用，因为失真发生在存储层）；
  Flat 只是"少扫几个簇"，只要真答案落在扫到的簇里就是**精确**的 —— 而扫 12.5% 的簇就够了。

**修正后的结论**：对 RAG（recall 优先），**IndexIVFFlat + nprobe=512 是正解**
（249 ms / recall 1.000），暴力只在"内存装得下且不想引入依赖"时才用。

**教训（比结论重要）**：`--quantizer` 有三个选项不是"三个等价写法"，而是**决定结论走向的变量**。
只跑其中一个就下"某个方案更好"的判断，是把**没测过的分支**当成了**不存在的差异**。
这和第 5 步「检测口径必须 == 修复口径」是同一类错误：**没被覆盖到的那条路径，会安静地失效。**

---

### [x] T6 · BM25 倒排索引 `src\build_bm25.py` + `src\search_bm25.py` —— **完成**

**实测结果（全量 331.6 万块）**：

| 项 | 值 |
|---|---|
| 词表 | **3,092,039** 词（min_df=2，砍掉 df=1 的 4,576,423 个） |
| postings | **191,647,819**（1.92 亿） |
| 索引体积 | **1219 MB**（doc_ids 767 / tf_norm 383 / vocab 31 / indptr 25 / doc_len 13） |
| 平均块长 | **77.66 词**（原始 167.7 token → 过滤后砍掉 53.7%） |
| 建索引耗时 | **19.9 分钟**（第一趟统计 df 9.5 分 + 第二趟写 postings 10.4 分，16 进程） |
| 查询延迟 | **26 ms** 稳态（首查 392 ms 含 jieba 冷启动 1.4 秒摊入） |
| 自检 | ✅ 每个词的 postings 条数 == df —— 写入完整 |

**四个关键设计**（都要写进手册）：

1. **两趟分词**：第一趟只为统计 df（每个词出现在多少块），据此算 indptr；
   第二趟按 `cursor[term]` 直接写入 → **完全不需要给 1.92 亿条 postings 排序**。
   否则排序就是主要成本。
2. **tf_norm 预计算**：BM25 的词频分量 `tf×(k1+1)/(tf+k1×(1-b+b×dl/avgdl))` 只依赖
   `(tf, dl, avgdl)`，**与查询无关** → 离线算好（1.92 亿次），查询时只剩「查表 + 乘 IDF + 累加」。
3. **词表按字典序落盘**：查询用 `np.searchsorted` 二分查找，
   **不必常驻 309 万项的 Python dict**（那个 dict 光对象头就要 400MB+）。
4. **查询分词必须 `import` 建索引那份 `_keep`，不能抄一遍**。
   口径一旦漂移，表现是「某些词永远搜不到」—— 不报错、不崩溃，纯粹静默失灵。
   这和第 5 步「检测口径必须 == 修复口径」是同一条纪律的第二次出现。

**Q1 的铁证（这就是混合检索存在的理由）**：

| 检索方式 | top1 | 分数 | 判断 |
|---|---|---|---|
| 向量 | 東寧路 (臺南市) | — | ❌ 讲台南市区道路，语义无关 |
| **BM25** | **蘇花古道** | **27.05** | ✅ 正文首句就是「蘇花古道是一條開發於古時的人行道路」 |

BM25 27.05 分领先第二名 20.28（**1.33 倍**）。这一条 331.6 万块里的 row=0，被字面匹配精确捞了出来。

**但 BM25 也有输得很惨的地方（如实记录，不藏）**：

- Q「中國的化學工程學家」→ top1 =「八田四郎次」（**日本**化學家）。
  词袋模型处理不了「中國」修饰「化學工程學家」这种**关系**，只能各自匹配词面。
- Q「這個地方氣候怎麼樣」→ top1 =「韩东君 · 評價」。
  被「這個 / 地方」这类高 df 泛词带偏（df 分别 73,717 / 108,970，IDF 只有 3.4~3.8）。

→ 这两条正好交给向量路来补。**互补关系是实测出来的，不是设计时拍脑袋定的。**

---

### [x] T7 · 混合检索 `src\search_hybrid.py` —— **完成（结论：RRF 原论文默认值在我们的数据上是错的）**

**延迟（faiss IVFFlat nprobe=512 + BM25）**：

| 环节 | 耗时 |
|---|---|
| 向量路（faiss，含 13.6GB 索引常驻） | **170~270 ms** |
| BM25 路（倒排扫描 + 二分查找） | **41~52 ms**（首查 653 ms 含 jieba 冷启动） |
| RRF 融合（纯 Python，200 个候选） | **0.2~0.3 ms** |
| **合计** | **≈ 220~320 ms** ✅ 远优于 2 秒阈值 |

**三模式对照（同一组 query）**：

| Q | 向量 top1 | BM25 top1 | 混合 top1（k=60） | 判定 |
|---|---|---|---|---|
| 台灣東部開發於古時的人行道路 | 東寧路 (臺南市) ❌ | 蘇花古道 ✅ | **蘇花古道** ✅ | **混合大胜** |
| 中國的化學工程學家 | 张克忠 ✅ | 八田四郎次(日本) ❌ | 朱汝瑾 ⚠️ | **混合变差** |
| 這個地方氣候怎麼樣 | 温带海洋性气候 ✅ | 韩东君·評價 ❌ | 韩东君 ❌ | **混合变差** |

✅ **满足验收标准：既有「混合 > 单路」的实例，也有「混合反而变差」的实例，都如实记录。**

**Q1 为什么是混合的胜利（机制拆开看）**：

```
蘇花古道   vec#42 + bm25#1   → 1/102 + 1/61 = 0.0262   ← 两路都认可，提到第 1
東寧路     vec#1  + bm25#—   → 1/61        = 0.0164   ← 只有向量认可，掉到第 5
```

向量把「東寧路」排第一（0.6803，但只领先次名 **5.0%** —— 它自己也不确定）；
BM25 把「蘇花古道」排第一（27.05，领先 **33.4%** —— 非常确定），而東寧路连 BM25 的 top-100 都没进。
**融合后错误 top1 被压到 #5，正确答案升到 #1** —— 这就是 RRF 该干的事。

**Q2/Q3 为什么变差（RRF 的固有弱点，实测暴露）**：

RRF 只用排名、**完全丢弃分数强度**，于是无法区分：

- BM25「**强**匹配」：蘇花古道 27.05 分，领先 **33%** → 该信
- BM25「**弱**匹配」：韩东君 19.90 分，领先 **2.8%** → 纯噪声

两者在 RRF 眼里都是 `rank 1`，拿一样的 `1/(k+1)`。后果：

| Q | 现象 | 数字 |
|---|---|---|
| Q2 | 「两路都在中游」反超「单路第一」 | 朱汝瑾 vec#22+bm25#18 = **0.0250** > 张克忠 vec#1 = **0.0164** |
| Q3 | BM25 的垃圾 top1 与向量的正确 top1 同分，靠行号小胜出 | 韩东君 = 温带海洋性气候 = 0.0164 |

**T7.5 · 融合策略对比**（`src\probe_fusion.py`，5 种策略）

| 策略 | Q1 | Q2 | Q3 | 命中 | 说明 |
|---|---|---|---|---|---|
| `rrf k=60`（ES/原论文默认） | ✅ | ❌ | ❌ | 1/3 | 排名太平，多路中游共识容易反超强信号 |
| **`rrf k=10`（新默认）** | ✅ | ✅ | ❌ | **2/3** | 放大头部差异：1/11 vs 1/30 差 173%（k=60 时只差 31%） |
| `cc` min-max 归一化 | ✅ | ❌ | ❌ | 1/3 | 两路 top1 都被拉成 1.0，**强弱彻底抹平** |
| `cc` z-score 归一化 | ✅ | ❌ | ❌ | 1/3 | 向量分数分布极窄（top1 只领先 0.1%）→ z 值 1.52，反被 BM25 的 2.10 压过 |
| `--fuse gate`（领先幅度门控） | ✅ | ✅ | ✅ | **3/3** ⚠️ | 见下方警告 |

**关键结论**：**k 不是无关紧要的超参。** 只把 60 改成 10，Q2 就从错变对。
`k=60` 那个默认值是给「两路质量相当」的场景准备的；这里 BM25 质量波动极大
（专名题能压过向量，泛词题全是噪声），必须用更小的 k 让强信号说话。

**cc（分数加权）三种做法全败** —— 根因：两路的**分数分布形状根本不同**，
向量的余弦集中在 0.55~0.68 的窄带里，BM25 是 0~27 的长尾。
任何单调归一化都会扭曲「置信度」的语义：
min-max 把两路 top1 都变成 1.0（强弱抹平），z-score 又因为向量分布太窄而**放大**了它的噪声。

**`--fuse gate`：3/3，但必须标注过拟合风险**

用「BM25 top1 领先次名的幅度」当置信度信号（33.4% / 6.9% / 2.8% 完美三分），
低于 10% 就把 BM25 权重降到 0.3：

| Q | 领先幅度 | 权重 | top1 结果 |
|---|---|---|---|
| Q1 | 33.4% | 1.0 | 蘇花古道 ✅ |
| Q2 | 6.9% | 0.3 | 张克忠（top-5 全是「中国化学工程学家」）✅ |
| Q3 | 2.8% | 0.3 | 温带海洋性气候（top-5 全是气候小节）✅ |

🔴 **警告：阈值 10% 和权重 0.3 是在这 3 条 query 上拟合出来的，样本量严重不足。**
3/3 只能当**线索**，不能当**结论**。要采用它，必须先标注几十条 query 重新验证。
所以脚本里默认关（`--fuse rrf`），gate 仅作实验入口保留。**把它默认打开才是真的不负责任。**

**一个必须说清的脆弱点**：Q2 在 k=10 下的「正确」依赖 **tie-break 规则** ——
张克忠（vec#1）和八田四郎次（bm25#1）都是 0.09091，最终靠 `sorted(key=(-score, row))`
里「行号小的排前」（row=2 < row=2915764）才让张克忠胜出。
**换一个 tie-break 规则结果就翻了。** 这不是稳健的正确，是踩中的正确。
真正的解法是引入 reranker 做精排（见下方「遗留」）。

---

### [x] T8 · 落文档 —— **完成**

- [x] 操作手册新增 **§8**（8.1 三路分工 / 8.2 跑 / 8.3 实测数据 / 8.4 RRF 的 k 陷阱 / 8.5 四个坑 / 8.6 产出）
- [x] `README.md` 补**检索章节**（三路延迟表、nprobe 权衡表、5 种融合策略对比、诚实标注）
- [x] 易错点速查表新增 **AA~AE** 五条（数组解包反了 / faiss mmap / RRF k / 只测一个分支 / cc 归一化）
- [x] 第 8 步勾完

**✅ 已清理（2026-09-19）**：

| 文件 | 体积 | 状态 |
|---|---|---|
| `data\index\faiss_ivf_sq8.index` | 3.21 GB | **已于 2026-09-19 删除**（recall 天花板 0.98，被 Flat 推翻）；当前用 `faiss_ivf_flat.index` |
| `data\index\faiss_ivf_sq8_l2.index` | 3.21 GB | **已于 2026-09-19 删除**（假设被证伪的 A/B 对照产物，L2 相对 IP 无系统性优势） |
| `eval\results\vec_bench*` / `vec_smoke*` | 小 | 吞吐基准的中间产物，保留（体积可忽略） |

两个 SQ8 索引及各自 `meta.json` 共释放 **6.88 GB**，删后已做冷启动复验（见下方"删除后复验"）。
若某天要重跑两阶段对照，重建命令：`python src\build_faiss_index.py --quantizer sq8`（约 8 分钟 / 3.4 GB）。

**删除后复验（2026-09-19 22:58）**：

| 检查项 | 结果 |
|---|---|
| faiss Flat 索引完好性 | 魔数 `IxFl`（IndexFlat）、ntotal 3,316,395、meta 完整 |
| Gradio 冷启动 | ✅ 三个索引全部重新加载（faiss 13.63 GB / BM25 1,919,820 词 / 向量 331.6 万条），HTTP 200 |
| 端到端提问「張克忠的籍貫是哪裡」 | ✅ 答「天津[2]」，引用编号 [1,2,3]，繁简修复仍有效 |
| 端到端提问「蘇花古道的起點和終點在哪裡」 | ✅ 答「蘇澳、花蓮[2]」 |
| `src\probe_two_stage.py` | 默认索引已改指 `faiss_ivf_flat.index`（原硬编码指向已删的 SQ8），失依问题消除 |

**结论都写在文档里了，不需要靠保留文件来"记得"。**

---

## 质量要求

- [x] ~~不引入 Chroma / faiss，零新增依赖~~ → **被实测推翻**：
      延迟 8.2 秒触发预设出口，装了 `faiss-cpu`（唯一新增依赖），**Chroma 仍然不装**。
      决策全过程见下方「为什么最后装了 faiss」
- [x] 所有脚本可独立运行，参数走 `argparse`，默认值指向全量索引
- [x] 长耗时脚本（T6 全量建倒排）**必须带进度输出 + 可断点**（第 7 步的教训）
      —— `build_bm25.py` 加了 `line_buffering=True`（上一轮踩过 stdout 块缓冲的坑）
- [x] 每条验收都要有**实测数字**，不许写「应该差不多」

## 技术决策记录

### 为什么最后装了 faiss（决策过程被数据改写了一次，如实记录）

**第一版判断（未实测，基于估算）**：

| 方案 | 代价 | 估算收益 |
|---|---|---|
| 直接 numpy 分块内积 | 0 | 全量延迟"应该 < 1 秒" |
| faiss-cpu | 装依赖 + fp16→fp32（13.6 GB） | 加速到 ms 级 —— "但我们现在是秒级，不需要" |
| Chroma | 装依赖 + 重新导入 331.6 万条（数小时）+ 7 GB | 元数据过滤/持久化 —— 但 `ids.txt` 已解决对齐问题 |

当时的结论是「暴力检索够用，引入向量库是负收益」，
但**留了一个出口**：*如果 T5 实测延迟 > 2 秒，就上 `faiss IndexIVFFlat`。*

**实测把这个估算打穿了**：

| 环节 | 实测 | 对比估算 |
|---|---|---|
| 纯内存 fp32 内积 | 263 ms | 与"< 1 秒"相符 ✅ |
| **实际 mmap + fp16→fp32 分块扫** | **8,217 ms** | **超阈值 4 倍** ❌ |

差在哪：估算时假设"数据已经是 fp32 躺在内存里"，而我们的 `emb.npy` 是 **fp16 落盘** ——
每次查询都要做 **3.4e9 次 `fp16→fp32` 转换**（numpy 约 4e8 元素/秒），这一项就吃掉 8 秒。
**这个转换开销在估算阶段是完全不可见的。**

**触发出口，实测 faiss 之后的最终决策**：

| 方案 | 延迟 | recall@10 | 内存/磁盘 |
|---|---|---|---|
| **faiss IVF Flat + nprobe=512** | **249 ms** | **1.000** | 13.6 GB |
| faiss IVF-SQ8 + nprobe=128 | 196 ms | 0.960 | 3.44 GB |
| numpy 暴力（fp16 mmap） | 8,217 ms | 1.000 | ~0 |

→ **选 `IndexIVFFlat`（无量化）+ nprobe=512**：召回无损，比暴力快 **33 倍**。
  SQ8 省 10 GB 磁盘，但 0.98 是**存储层的硬失真、调 nprobe 救不回来** —— 对 RAG（recall 优先）不划算。

**Chroma 仍然不装** —— 理由不变：它解决的是「没有现成向量、要一条龙管理」的问题；
我们的 `ids.txt` 已经把「行号 ↔ `chunk_id`」这个全局契约解决了，再导入一遍是重复劳动。

> **这段决策过程的价值大于结论本身**：估算 → 留出口 → 实测触发出口 → 重新选型。
> 如果当初没留那句"延迟 > 2 秒就上 faiss"，就会卡在"我们决定了不引入向量库"里出不来。
> **把估算写成结论是危险的；把结论配上一个可触发的否决条件，才是安全的。**

### 为什么 BM25 要自己写

`rank_bm25` 之类库的设计前提是「语料能整份放进内存」。331.6 万块不满足，
而且**倒排索引是检索岗的核心考点**，自己建一遍比调库更有价值。
